# TODO — implementation gaps

The design under `docs/design/` is the **spec**: it describes the
language as it's supposed to be, not as it's currently built. This
file tracks where the implementation differs from that spec, plus
planned features whose design is fixed but whose code hasn't landed.

When a design file describes a feature, the assumption is "this
works." When it doesn't, look here.

---

## Road-to-real-world queue (READY for an autonomous agent to pick up)

In recommended execution order. Pick the next non-DONE / non-BLOCKED
item. See `CLAUDE.md` for "done" criteria and decision authority.

| ID | Status | Item |
|---|---|---|
| ~~A.1~~ | DONE | Channels carry arbitrary element types (struct payloads work) |
| ~~A.2~~ | DONE | `run` with oversized args (heap-box via thunk) |
| ~~A.4~~ | DONE | `for k, v := range map` |
| ~~A.7~~ | DONE | `errors.New` + sentinel pattern (no wrapping per v1 decisions) |
| ~~A.6a~~ | DONE | File I/O syscall layer: `syscall.Open` / `Close` / `ReadAll` / `WriteAll` work end-to-end. Tests: `tests-internal/file_io.volt`. |
| ~~A.6b~~ | DONE | `os.File` ergonomic wrapper (Drop-auto-close + explicit Close). Value-based struct with methods on `File`; `Open`/`Create` return `(File, error)`; `Read`/`Write`/`Close` are methods. Tests: `tests-internal/os_file.volt`. |
| ~~C.X~~ | DONE | **Cross-package method dispatch.** `Emitter.AddExternal` now scans for methods + struct types and registers them in `extMethods` + `e.structs` (marked External). `emitMethodCall` falls through to `extMethods` when local lookup fails, and mangles using the owner package name. Multi-return method calls work via `emitMultiReturnCall` delegating to `emitMethodCall` and using `resolveMethodOnSym` to learn the result types. Cross-package struct types are emitted in the IR header so receivers and return aggregates have proper layouts. |
| A.3 | BLOCKED | Auto-free on Drop for owned slice/map/string locals. Wire `volt_free` calls at scope-end in codegen. Skip frees on moved values — query the checker's move-state map (plumb via funcCtx or rewrite the AST). **Blocker:** needs new checker → codegen plumbing for per-local moved-at-end state, AND a way to distinguish heap-vs-static-rodata strings (concat results are heap-owned; literals are .rodata — same `%string` shape today). Partial impl risks double-free crashes. Revisit alongside A.5 (held borrows) — both need the same checker-state-export API. |
| A.5 | BLOCKED | Held borrows: `&x` / `*x` as expressions, cross-statement aliasing-XOR-mutation. Biggest item in Phase A. Parser + AST + checker + codegen. ~1 week. **Blocker:** explicitly out-of-scope for a single loop tick — multi-week design+impl. Revisit as a dedicated user-driven task. |
| ~~B.1a~~ | DONE | `fmt.Print` / `fmt.Println` / `fmt.Printf` to stdout (fd 1). Shared format-walk engine `emitFormatCallToFd` parameterized by fd backs both `log.*` (fd 2) and `fmt.*` (fd 1). Test: `tests-internal/fmt_pkg.volt`. |
| ~~B.1b~~ | DONE | `fmt.Sprintf(format, args...) string` and `fmt.Errorf(format, args...) error`. Implemented as a chain of `volt_string_concat` calls over format chunks + per-arg `volt_int_to_string` / `volt_bool_to_string` conversions. Errorf wraps the result in an `%error_box` via the shared `volt_errors_strerror` helper. Test: `tests-internal/fmt_sprintf.volt`. O(n²) for long chains — single-allocation accumulator deferred until that's an actual hotspot. |
| ~~B.1c~~ | DONE | `fmt.Fprintf(w Writer, format string, args...)` (int, error). Codegen intrinsic: reuses `emitFmtSprintf` to build the message string, then dispatches `w.Write(s)` via vtable index 0. Auto-boxes a concrete struct into the Writer fat pointer when the first arg isn't already a `ptr`. Multi-return ABI is `{i64, ptr}`; `emitMultiReturnCall` intercepts `fmt.Fprintf` (it has no real linked symbol). Test: `tests-internal/fmt_fprintf.volt` (covers both pointer-receiver Memo + os.File via value-receiver trampoline). |
| ~~B.2a~~ | DONE | `strings` package — scan-only operations: `Equal`, `HasPrefix`, `HasSuffix`, `Index`, `Contains`, `IndexByte`, `Count`. Test: `tests-internal/strings_pkg.volt`. Side: string indexing `s[i]` lowered to GEP+load. |
| ~~B.2b~~ | DONE | `strings` package — string-constructing operations: `ToLower`, `ToUpper`, `TrimSpace`, `Replace`, `Split`, `Join` (O(n²) `chr+concat` for now; `strings.Builder` is future fast path). Side: fixed slice element-size calc — `llvmTypeBytes` was treating `%string` as 8 bytes, so `new(N) []string{}` underallocated. Now Emitter-aware (`%string`=16, `%slice`=24, user structs sum fields). Test: `tests-internal/strings_constructors.volt`. |
| ~~B.3a~~ | DONE | `strconv.Atoi(s) (int, error)` + `strconv.Itoa(n) string`. Pure volt source using string concat + chr. Also adds string `==` / `!=` via `volt_string_eq`. Test: `tests-internal/strconv_pkg.volt`. |
| ~~B.3b~~ | DONE | `strconv.ParseInt(s, base)` and `strconv.FormatInt(n, base)` for bases 2..36. Helpers `digitValue`/`digitChar` handle the ASCII alphabet (lowercase a..z for emit; either case accepted for parse). Test: `tests-internal/strconv_base.volt`. **Float `ParseFloat`/`FormatFloat` deferred** — they need explicit `int(f)` / `float(i)` casts (or runtime helpers) to split integer/fractional parts; tracked as a separate item once those exist. |
| ~~B.4a~~ | DONE | `io` interfaces: `Reader`, `Writer`, `Closer`, `ReadWriteCloser`. New `internal/stdlib/io/io.volt` package. Codegen widened: cross-package InterfaceType registration in AddExternal; multi-return aggregate type for interface dispatch (`{T1, T2}`); interface-typed args auto-box concrete values via `emitIfaceBox`; interface-impl detection now scans `extMethods` too; vtables emitted only in the package that owns the concrete type, others `declare external`. Test: `tests-internal/io_iface.volt` (Memo type with pointer receivers satisfies `Writer`, multi-return roundtrip through interface dispatch). |
| ~~B.4b~~ | DONE | Value-receiver→vtable trampolines + `bufio` package. New `_$iface` adapter loads `%T` from the boxed `ptr` and forwards to the real method, dedup'd per (type, method). Codegen now also dispatches struct field-of-fun-type via `emitFieldFuncCall` (with multi-return support). `bufio.Buffer` holds a `fun(string)(int,error)` writer slot + accumulator; `New` / `WriteString` / `Flush`. `os.File` now satisfies `io.Writer` end-to-end through value-receiver trampoline. Tests: `tests-internal/io_iface_file.volt`, `tests-internal/bufio_basic.volt`. |
| ~~B.5a~~ | DONE | `time.Now()` / `time.Mono()` (sys_clock_gettime CLOCK_REALTIME / CLOCK_MONOTONIC). Returns i64 nanoseconds. Test: `tests-internal/time_now.volt`. |
| ~~B.5b~~ | DONE | `time.Time` value type + Y/M/D/H/M/S accessors + RFC3339 `Format()`. Built on Howard Hinnant's days-from-civil algorithm, UTC-only. Duration arithmetic `Add(d int)`, `Sub(u Time)`, `Before/After`. `Unix(sec, ns)` / `FromNano(ns)` constructors. `Format()` produces fixed `YYYY-MM-DDTHH:MM:SSZ` — magic-number layout strings (Go-style "2006-01-02") deferred. Test: `tests-internal/time_format.volt` (13 cases including epoch, 2024, far-future 2100). |
| ~~B.6~~ | DONE | `net`: TCP/IPv4 `Listen("host:port") (Listener, error)`, `Dial("host:port") (Conn, error)`, `(l Listener).Accept()`, plus `Conn` Read/Write/Close. Runtime gains direct inline-asm wrappers for `socket`/`bind`/`listen`/`accept`/`connect`/`setsockopt` on amd64+arm64, plus `volt_tcp_listen` / `volt_tcp_accept` / `volt_tcp_dial` helpers that encode sockaddr_in (`AF_INET`, big-endian port/addr, SO_REUSEADDR). New syscall surface: `syscall.TcpListen/TcpAccept/TcpDial`. Address parsing in pure volt (split on `:`, dotted-decimal IPv4). UDP / IPv6 / DNS / deadlines / SO_KEEPALIVE deferred. Test: `tests-internal/net_tcp.volt` (single-process loopback echo via `run` + chan11 sync). |
| ~~B.7~~ | DONE | `encoding/json` — `Value` tree type with `kind` tag (0=null, 1=bool, 2=int, 3=string, 4=array, 5=object), constructors `Null/Bool/Int/String/Array/Object`, accessors, full recursive `Encode`, full recursive `Decode` (primitives + array + object). `QuoteString`/`UnquoteString` for escape handling. No `\uXXXX` and int-only numbers (float deferred with float casts). Reflection-based struct marshaling deferred — volt has no reflection. **Codegen widenings landed alongside**: (a) `maybeMoveBareIdent` no longer moves a string on `var c byte = s[i]` byte index; (b) multi-assign of pointer-typed LHS marks `state.heap=true` so `ret v` after `v, err := fn()` isn't rejected; (c) `emitCallArg` accepts non-ident pointer args that emit as `ptr`; (d) `emitFieldAccess` propagates `SliceElem` for `obj.field[i]` indexing; (e) nil comparison `v == nil` bypasses emitIdent's auto-deref; (f) `emitPointerAwareExpr` helper used by `append` and multi-return ret to suppress auto-deref when the consumer wants raw `ptr`. Test: `tests-internal/json_basic.volt` (19 cases: QuoteString, primitives, array/object encode + decode, nested roundtrip, error path). |
| ~~B.8~~ | DONE | `os.Argc()` / `os.ArgAt(i)` / `os.Args()` / `os.Getenv(name)`. Both arch start stubs now capture argc/argv/envp into BSS globals at process entry; runtime exposes `volt_arg_count` / `volt_arg_at` / `volt_env_get`; codegen intrinsics + stubs in `os.volt`. Also lands slice index-assignment `s[i] = v`. Test: `tests-internal/os_args.volt`. |
| ~~S.1~~ | DONE | String concatenation `a + b` for two `%string` operands lowers to `volt_string_concat(a_ptr, a_len, b_ptr, b_len) -> %string` (heap-allocates the joined bytes). Plus `chr(b)` builtin to make a 1-byte string from a byte. Unblocks string-construction in fmt/strconv/strings constructors. Test: `tests-internal/string_concat.volt`. |
| ~~C.1a~~ | DONE | Package resolver + git fetcher. `volt mod init` already wrote `volt.mod`; this adds `volt mod get <import-path>` (shallow git clone of `https://<path>.git` into `~/.volt/pkg/<path>`, or `git pull --ff-only` if already present). The driver's `resolveAndCompile` now falls back to `./<path>/...` (local module sub-packages) then `~/.volt/pkg/<path>/...` for non-stdlib imports. Qualifier resolution was a bug: `e.imported` was keyed by full path so `import "example.com/greeter"` followed by `greeter.X` failed — now keyed by the path's last segment (basename), backward-compat with stdlib paths (no slash). Tested with a `~/.volt/pkg/example.com/greeter` fixture (hand-placed; `volt mod get` itself not exercised in CI to avoid network flake). |
| ~~C.1b1~~ | DONE | `volt.mod` parser + `volt.sum` lockfile. New `internal/modfile` package parses + serializes both formats (single-line `require <path> <version>`, comments via `//`, `h1:<base64-sha256>` for sums). `volt mod get`: clones into the cache (existing behavior), then upserts a `require` line in `./volt.mod` AND a `h1:` hash entry in `./volt.sum`. Hash is `sha256` over every `.volt` file in the package cache dir, sorted by relative path (matches Go's `go.sum h1:` spirit). Pin version is `git rev-parse --short HEAD` when the cache is a real checkout, or `v0.0.0-local` for hand-placed fixtures. Idempotent — re-running `volt mod get` reproduces the same hash. Smoke-tested with `mkdir /tmp/m && volt mod init … && volt mod get example.com/greeter` against the hand-placed fixture from C.1a. |
| ~~C.1b2~~ | DONE | Verify-on-load against `volt.sum` — the driver now hashes a cache-resolved package and prints a warning when it doesn't match the on-disk `volt.sum`. Warning only (not a fatal error) so devs can still iterate; locked-down enforcement is C.1b3. New `verifyAgainstSum(importPath, pkgDir)` is called from `loadExternalPackage` after a cache hit, with per-process memoization (`verifiedPkgs` map + cached `loadedSums` slice) so each (path, dir) pair is checked once. Smoke-tested: building with the untampered fixture is silent; appending a line to the cached `.volt` file produces `volt: warning: example.com/greeter v0.0.0-local on-disk hash X doesn't match volt.sum Y`. |
| ~~C.1b3~~ | DONE | `volt mod tidy <entry>`: walks the import graph from the entry file (parsing each touched .volt to recurse into its imports), classifies each path as stdlib / local / external, then rewrites `./volt.mod`'s require list to exactly the externals (preserving existing version pins, defaulting new ones to `v0.0.0-local`). `./volt.sum` is regenerated from the surviving requires by hashing their cache dirs; if no externals remain, the file is removed. Smoke-tested both directions: a stale `require example.com/unused` is dropped when nothing imports it; an empty volt.mod gets the necessary `require example.com/greeter` re-added — both with the matching `h1:` sum entry. |
| ~~C.1b4~~ | DONE | Three UX pieces: (a) `modfile.Parse` now accepts block-form `require ( <p> <v> ... )`, and `Format` emits the block form when there are 2+ requires (single = one-line); (b) new `volt mod verify` subcommand re-hashes every entry in `./volt.sum` and exits 1 on any mismatch (`MISMATCH` / `MISSING` / `ERROR` per line, plus a summary); (c) new `-strict` build flag escalates the verify-on-load warning to an `error:` and exits the build. Smoke-tested: block form round-trips through `volt mod tidy`; `mod verify` and `build -strict` both exit 1 on tamper, 0 on match. |
| ~~C.1b5~~ | DONE | `replace` directive: `replace <import-path> => <directory>` in volt.mod redirects the resolver to a literal on-disk path (skipping the cache + sum verification — replace targets are "trust me, use what's on disk"). Supports both single-line and block-form (`replace ( … )`). `modfile.Parse`/`Format` round-trip both. `loadExternalPackage` reads replaces from `./volt.mod` once per process (cached in `loadedReplaces`) and tries the redirect before the local-module and cache lookups. `volt mod tidy` preserves replace lines (they're independent of the require list). Smoke-tested: `replace example.com/greeter => /tmp/local_greeter` diverts the import; the local fork's modified `Add(2,3)+1000` correctly returns the changed behavior. |
| ~~C.1b6~~ | DONE | Module manifest fully wired: ~~Transitive dep walk~~ (Pass 73), ~~Semver parse + compare + MVS~~ (Pass 74), ~~Version-specific `replace` parsing + `MatchReplace`~~ (Pass 75), ~~`MatchReplace` wired into the import-resolver lookup path~~ (Pass 76), ~~Git-tag checkout of the workspace-pinned version after `volt mod get` clones (best-effort: tries direct checkout first, falls back to shallow tag fetch, surfaces error to caller as a warning)~~ (Pass 77). |
| ~~C.2a~~ | DONE | `volt lsp` subcommand — minimum-viable LSP server over stdio. Implements JSON-RPC 2.0 with Content-Length framing; handles `initialize` (advertises `textDocumentSync: Full`), `initialized`, `shutdown`, `exit`, `textDocument/didOpen|didChange|didSave|didClose`. On every buffer change parses + checks the document and pushes `textDocument/publishDiagnostics`. Error positions extracted from the standard volt `<file>:<line>:<col>:` prefix via regex; multi-line continuation lines append to the previous diagnostic. Smoke-tested via a tiny Python client: parse errors (`expected ), got ;`) and checker errors (`"x" declared but not used`) both come through with correct line/column. Codegen-level errors (unknown identifiers, type-mismatches that the checker doesn't catch yet) need codegen wired in — punted to C.2b. |
| ~~C.2b1~~ | DONE | LSP `textDocument/hover` and `textDocument/definition`. Capabilities advertised in `initialize`. Lookups re-lex+re-parse the open-buffer on every request (fine for small files; saves any cross-request state machine). New `identAt(text, line, col)` walks the token stream looking for an `Ident` whose source range covers the cursor; `lookupDecl` / `lookupDeclPos` scan top-level `FuncDecl` / `TypeDecl` for the matching name. `renderFuncSig` prints `fun (recv *T) Name(p T1, q T2) (R1, R2)`-style summaries for hover. Smoke-tested via Python client: hover over a call to `greet` returns `fun greet(name string) string`; goto-def returns the `fun` keyword's position in the same file. |
| ~~C.2b2~~ | DONE | LSP `textDocument/references` and `textDocument/completion`. `referencesResult` walks the parsed AST (`Decls` → `FuncDecl.Body.Stmts` → `Expr` tree) via three mutually-recursive walkers (`walkStmtsForIdent`/`walkStmtForIdent`/`walkExprForIdent`) collecting every `IdentExpr.P` whose `Name` matches the cursor's identifier; declaration sites (`FuncDecl.P` / `TypeDecl.P`) are included so editors highlight them together with usages. `completionResult` reads the identifier prefix immediately before the cursor (`wordBeforeCursor` walks back over `[A-Za-z0-9_]` from `(line, col)`), scans top-level decls, and emits `CompletionItem` entries (Function = kind 3, Class = kind 7) for matching names plus their hover signature as `detail`. Smoke-tested: `references` on `greet` finds 4 hits (decl + 3 call sites); typing `gre` triggers a single-item completion list with `greet`. |
| ~~C.2b3~~ | DONE | Workspace-wide LSP references. `initialize` extracts the `rootUri` (falls back to `rootPath`); `textDocument/references` now walks every `.volt` file under that root (skipping `.git`, `.tempbins`, `.volt` cache dirs), preferring open buffer text over on-disk content so unsaved edits are searched too. New `appendIdentPositions` factored out so the per-file accumulation works the same way for the cursor's buffer and every sibling. Smoke-tested with a `/tmp/lsp_ws` workspace containing `a.volt` (greet decl + call) and `b.volt` (separate call) — references on `greet` returns all 3 locations across both files. |
| ~~C.2b4~~ | DONE | LSP `textDocument/rename`. Capabilities advertise `renameProvider: true`; the handler reuses `referencesResult` to collect every workspace-wide location for the identifier under the cursor, groups them by URI, and emits a LSP `WorkspaceEdit{changes: {uri: [TextEdit{range, newText}]}}`. Empty-result (no identifier at cursor / empty newName) returns `null` so editors show "no symbol" rather than silently applying nothing. Smoke-tested in the two-file `/tmp/lsp_ws` workspace: renaming `greet → hello` produces 2 edits in a.volt + 1 edit in b.volt, all positioned correctly. |
| ~~C.2b5~~ | DONE | `textDocument/prepareRename`. `renameProvider` is now the object form `{prepareProvider: true}` so editors know to ask first. The handler reuses `identAt` to locate the identifier under the cursor and returns `{range, placeholder}` — range covers the existing name, placeholder seeds the rename UI with the current text. Whitespace / non-identifier cursor positions return `null`, which disables the editor's rename action gracefully. Smoke-tested: prepareRename on `greet` returns `{placeholder: "greet", range: {…}}`; on whitespace it returns `null`. |
| ~~C.2b6~~ | DONE | LSP `textDocument/documentSymbol` — the editor outline. `documentSymbolProvider: true` capability; the handler parses the buffer and emits `DocumentSymbol[]` for every top-level decl: FuncDecls become Function (kind 12) or Method (kind 6, has receiver), TypeDecls become Class (kind 5) with struct field children of kind Field (8). Each entry carries the hover-style signature as `detail`, plus `range` and `selectionRange` covering the identifier (the AST doesn't track decl-end positions yet so both share the same single-line span). New `symbolEntry` helper. Smoke-tested: a file with `type Point struct { x int; y int }`, `fun (p *Point) Norm()`, `fun greet(name string)`, `fun main()` outlines as Class(Point) → Field(x), Field(y); Method(Norm); Function(greet); Function(main). |
| ~~C.2b7~~ | DONE | LSP `workspace/symbol` — fuzzy symbol search across the whole project. `workspaceSymbolProvider: true` capability; the handler walks every `.volt` under `workspaceRoot` (skipping `.git`/`.tempbins`/`.volt`), parses each, and emits `SymbolInformation` for top-level decls whose name contains the query (case-insensitive substring). Empty query returns everything; open buffers take precedence over disk so unsaved edits show up. Smoke-tested in `/tmp/lsp_ws`: query `et` returns just `greet`; empty query returns `greet` + `main` + `shout` across both files. |
| ~~C.2b8~~ | DONE | LSP `textDocument/signatureHelp`. `signatureHelpProvider` advertises trigger chars `(` and `,` (retrigger on `,`). Implementation: re-lex the buffer up to the cursor while tracking a stack of "open calls" — each `(` pushes (remembering the most-recent Ident as the function name); `)` pops; `,` increments the top frame's comma counter. At the cursor, the top of stack tells us which call we're inside and which argument we're on (commas count = active parameter index). The function name is resolved against top-level `FuncDecl`s; params are emitted with `<name> <type>` labels. Returns `null` outside any call. Smoke-tested: cursor at three positions inside `add(1, 2, 3)` correctly reports `activeParameter` 0 → 1 → 2 with the full signature `fun add(a int, b int, c int) int`. |
| ~~C.2b9~~ | DONE | LSP `textDocument/semanticTokens/full` — server-driven syntax highlighting. `semanticTokensProvider` advertises a legend with 4 token types: `keyword`, `string`, `number`, `variable`. The handler re-lexes the buffer; each lexer token is classified (`KwAtomic`..`KwWaitgroup` → keyword, `String` → string, `Int`/`Float` → number, `Ident` → variable; punctuation skipped) and emitted as the LSP-required delta-encoded quintuple `(deltaLine, deltaStart, length, tokenType, modifier)`. Smoke-tested: `fun main() int { var n int = 42; var s string = "hi" }` decodes back to "fun" keyword, "main"/"int"/"n"/"s"/"string" variable, "42" number, "hi" string with correct positions. |
| ~~C.2b10~~ | DONE | Richer semantic-token classification — legend extended with `function` and `type`. Three rules promote `variable` to a more specific kind: (1) any Ident immediately after a `type` keyword is a `type` (declaration site); (2) subsequent occurrences of any name seen as a type decl get the same classification (so `var p Point = …` highlights `Point` as type); (3) Idents immediately followed by `(` become `function` — works for function definitions, method names between `)…(`, and call sites including unresolved cross-package ones. Smoke-tested: a file with `type Point struct {…}`, `fun greet(name string) string`, `fun main() int { var s string = greet("hi") }` decodes with `Point` as type, `greet`/`main` as function (at both decl and call sites), keywords/strings/numbers correctly. |
| ~~C.2b11~~ | DONE | (a) Builtin type names (`int`/`int8`/…/`uint64`, `byte`, `bool`, `float`/`float32`/`float64`, `string`, `error`, `any`) seeded into the semantic-token classifier's `typeNames` set; `var x int = 1` now highlights `int` as type, not variable. (b) New `foldingRangeProvider: true` capability + `textDocument/foldingRange` handler — re-lexes the buffer, pairs each `{` with its matching `}` (depth stack), emits `FoldingRange{startLine, endLine: line-of-`}`-minus-1, kind: region}` for every multi-line block. Smoke-tested: a file with a struct, two functions, and a nested if produces 4 folding ranges covering each block; builtin types render correctly in the semantic-token output. |
| ~~C.2b12~~ | DONE | Incremental document sync (`textDocumentSync.change: 2`). The capability is now the structured form `{openClose, change: 2, save: true}`. `didChange` is handled by new `applyChangeParams(raw, open)` which: keeps the previously-known buffer for the URI, applies each `ContentChange` in order — a change without `range` replaces the whole document (back-compat for Full-sync clients), a change WITH `range` is spliced into the buffer via byte offsets computed from `lspPosition`. `offsetForPos` walks lines counting bytes (ASCII assumption — UTF-16 code-unit math is a future widening for non-ASCII sources). Removed obsolete `changeParams` helper. Smoke-tested: opening a `fun greet(…)` buffer then sending an incremental change replacing the range covering "greet" with "salute" makes hover on the new identifier return `fun salute(name string) string`. |
| ~~C.2b13~~ | DONE | LSP `textDocument/documentLink` — Ctrl-click navigation on `import "path"` literals. `documentLinkProvider: {resolveProvider: false}` capability; the handler walks `File.Imports`, resolves each path via new `resolveImportPath` (local module → user package cache; stdlib has no on-disk source so returns no link), and finds the actual `"path"` string-literal range via `importStringPos` (which re-lexes since `Import.P` points at the `import` keyword, not the string). Returns LSP `DocumentLink{range, target, tooltip: "Open <path>"}` array. Smoke-tested: a file importing both `"log"` (stdlib, no link) and `"example.com/greeter"` (cached) returns 1 link pointing at the file:// URI of the cache copy. |
| ~~LANG.3~~ | DONE | Qualified type names `pkg.Type` in type position. Parser previously rejected `*testing.T` with "expected ), got ." — only bare type names were accepted. New parser branch in `parseType`: after consuming an Ident, if the next two tokens look like `. Ident`, consume both and use the second Ident as the type name (dropping the qualifier). Volt's type/interface namespace is global so cross-package types register under their bare name; this widening is stylistic (lets users write Go-style qualified signatures). Future work tracks the qualifier through resolution for disambiguation. Test: `tests-internal/qualified_type.volt` (two test functions written `fun foo(t *testing.T)`, registered + executed via `testing.Run`, 2/2 pass). |
| ~~LANG.2~~ | DONE | Cross-package top-level const access. Codegen previously errored "X.Y referenced as a value (only call form supported)" for any imported package's const. New `extConsts map[pkg]map[name]ast.Expr` registry populated by `AddExternal`; `emitFieldAccess` consults it after the time-unit specials and substitutes the literal expression at the use site (mirrors how same-package consts work in `emitIdent`). Unblocks idiomatic patterns like `math.MaxInt`, `math.MinInt`, `fnv.Offset32`, etc. Test: `tests-internal/cross_pkg_const.volt` (5 cases — direct compare, var bind, two consts in one expr, const as function arg, arithmetic with a const). |
| ~~LANG.1~~ | DONE | Bitwise + shift binary operators: `&`, `|`, `^`, `<<`, `>>`. Already lexed; parser's `binaryOp` table didn't recognize them. Added at Go-style precedences (multiplicative-tier for `&`/`<<`/`>>`; additive-tier for `|`/`^`). Codegen `emitBinary` lowers to LLVM `and` / `or` / `xor` / `shl` / `ashr` (arithmetic right shift — `int` is signed). Unblocks bit-twiddling stdlib code. |
| ~~STDLIB.1~~ | DONE | `hash/fnv` package — FNV-1a 32-bit (`HashStr32`) and 64-bit (`HashStr64`). Pure volt over the new bitwise/shift ops. Constants `Offset32` / `Prime32` / `Offset64` / `Prime64` exposed for users who want a streaming or seeded variant. `stdlib.Source` now handles multi-segment package paths (`hash/fnv` → `hash/fnv/fnv.volt` — last segment as basename); was previously hard-coded to `pkg/pkg.volt`. Test: `tests-internal/hash_fnv.volt` (empty/"a"/"foobar"/determinism/avalanche; 9/9 against Python-cross-checked vectors). |
| ~~C.2b21~~ | DONE | LSP go-to-definition for method calls on local variables — the Pass-129 hover machinery extended to F12. New `methodDefinitionLocation(uri, file, typeName, methodName)`: scans the open file's FuncDecls for a matching `(receiver) name` and returns the local URI; on miss, walks all 28 stdlib packages and on hit materializes `<pkg>` to `/tmp/volt-stdlib/<path>/<basename>.volt` (same cache strategy as Pass 125) before returning the Location. `definitionResult` chains: local FuncDecl/TypeDecl → stdlib package symbol (Pass 125) → method-on-local-var-type (this pass). Three new whitebox tests in `cmd/volt/lsp_method_definition_test.go`: F12 on `b.WriteByte` (b is `*bytes.Builder`) lands in `/tmp/volt-stdlib/bytes/bytes.volt`; F12 on `p.X()` (Point in same file) returns the local URI not stdlib; F12 on `b.Nonexistent()` returns empty array (graceful miss). **The LSP now fully navigates methods**: completion (Pass 68), hover (Pass 129), goto-def (Pass 130) — for both locally-defined struct methods AND stdlib-type methods through transparent source materialization. |
| ~~C.2b20~~ | DONE | LSP hover for **method calls** on local variables. Previously hovering on `b.WriteByte` (where `b` is a local `*bytes.Builder`) returned nil because the SelectorExpr's qualifier `b` didn't resolve as a stdlib package, and the local-only lookup couldn't see methods. Now `hoverResult` chains: (1) try the stdlib-package path (Pass 113); (2) on miss, parse the file, resolve the qualifier as a local var via `resolveReceiverType` (the Pass-68 method-completion helper), and call new `lookupMethodDecl(file, typeName, methodName)`. lookupMethodDecl first scans the open file's FuncDecls for a matching `(receiver Type) Name`, then walks all 28 stdlib packages (cheap — linear scan of small files) so methods on stdlib types like `*bytes.Builder` resolve too. Both `b.WriteByte` (stdlib) and `p.X()` where `Point` is locally-defined work. Two whitebox tests in `cmd/volt/lsp_method_hover_test.go`: `TestMethodHoverStdlib` (cursor on `WriteByte` in `b.WriteByte(72)` where b is *bytes.Builder), `TestMethodHoverLocalStruct` (cursor on `X` in `p.X()` where Point is in the same file). The same machinery could be reused for goto-def on methods — natural follow-up. |
| ~~DOC.2~~ | DONE | `readme.md` rewritten to reflect the current state of the project. Was a 23-line "super early ALPHA" stub from 2026-05-22; now a proper landing page with: a quick-start that builds the compiler and runs a sample, a status table (28 stdlib packages, 139 tests, 0 crashes, three-script regression suite), the feature matrix (language, memory, concurrency, stdlib, tooling, LSP, modules, build artifacts), how-to-learn pointers (docs/samples + docs/design + `volt doc <pkg>` + tests-internal), and the honest "what's not yet here" list (race detector, lock-free MPMC, macOS, self-hosting, float stdlib). The "in short" line `Go clothes, Rust spine, C sigils, Zig bare-metal posture` survives. Quick-start example verified by manually building and running the suggested 5-line hello world (exit=42). |
| ~~REFAC.1~~ | DONE | Consolidate the stdlib-package list to a single source of truth in `internal/stdlib`. Previously the 28-package list was hardcoded **three times** (Pass-126 `volt env` list, Pass-124 `docStdlibShortName` map in cmd/volt/main.go, Pass-110 `stdlibImportPaths` map in cmd/volt/lsp.go) — adding a new stdlib package required three coordinated edits. New `stdlib.List() []string` walks the embedded FS and returns all `.volt` parent directories sorted alphabetically (28 paths today). New `stdlib.ShortNameToPath() map[string]string` builds the basename → full-path map from List() in one place. **Consumers updated**: `volt env` uses `stdlib.List()` + `sort.Strings`; `docStdlibShortName` is a one-line wrapper over `stdlib.ShortNameToPath()`; `stdlibImportPaths` in lsp.go is now a package-level `var stdlibImportPaths = stdlib.ShortNameToPath()` initialized once at startup. The downstream consumers (`isKnownStdlibPath`, `nearestStdlibPath`, `stdlibPackageCompletions`, `lookupStdlibDecl`, `stdlibDefinitionLocation`) work unchanged since they only read from `stdlibImportPaths`. **Net effect**: adding a new stdlib package (extend the go:embed line + drop a .volt) now propagates automatically to env, doc, completion, hover, goto-def, typo suggestions, missing-import quick fixes — no manual sync. All 139 tests + 8 cmd/volt unit tests still pass under the three-script regression. |
| ~~CLI.4~~ | DONE | `volt env` stdlib-package list refreshed + help one-liner clarified. The env command's hardcoded list had drifted 13 packages behind reality (still showed 13 names from Pass 31's surface; missing bytes, crypto/*, encoding/*, hash/fnv, maps, math, path/filepath, slices, sort, unicode). Updated to the full 28-package set, sorted alphabetically, one-per-line for readability. Header now shows `stdlib packages:    28 total`. Help string for `volt doc` updated from `<file>` to `<file|stdlib-pkg>` reflecting the Pass-124 widening. |
| ~~C.2b19~~ | DONE | LSP go-to-definition for stdlib symbols. Previously F12 (or equivalent) on a stdlib call returned `[]` because `lookupDeclPos` only searched the local file. Now `definitionResult` falls through to a stdlib lookup when the local one fails: detects `pkg.<name>` via the existing `selectorPkgPrefix` helper, materializes the stdlib source to a stable cache path `/tmp/volt-stdlib/<full-path>/<basename>.volt` (the file is rewritten on every call — cheap and keeps it current with the embedded source on compiler upgrades), then locates the declaration within. Returns an LSP `Location{uri: file:///tmp/volt-stdlib/.../foo.volt, range: ...}` the editor can open + scroll to. New `stdlibDefinitionLocation(pkgShort, name)` helper; shared `makeLocation` factored out of the local + stdlib branches. Three whitebox tests in `cmd/volt/lsp_stdlib_definition_test.go`: definition on `strings.HasPrefix` lands in `/tmp/volt-stdlib/strings/strings.volt`; multi-segment-path resolution via `sha256` → `crypto/sha256`; local symbols still win over stdlib (a user-defined `HasPrefix` in main shadows the stdlib one). **Pass 110 (completion) + Pass 113 (hover) + Pass 125 (goto-def)**: the LSP now covers all three core navigation features for the full stdlib surface. |
| ~~CLI.3~~ | DONE | `volt doc <pkg>` now accepts stdlib package names in addition to file paths. New `docStdlibShortName` helper maps the 28 short-names (`strings` → `strings`, `sha256` → `crypto/sha256`, `filepath` → `path/filepath`, etc.) to their full paths. `runDoc` tries: (1) if arg doesn't end in `.volt`, look up via `stdlib.Source(arg)` directly (works for `crypto/sha256` etc.); (2) on miss, try the short-name map; (3) fall back to reading from disk. **Three forms now work**: `volt doc strings`, `volt doc crypto/sha256`, `volt doc bytes` — each prints the package's exported functions + struct types + methods. File-path form (`volt doc tests-internal/foo.volt`) preserved for non-stdlib lookups. Test: manual smoke `volt doc strings` prints 21 exports; `volt doc bytes` shows the Builder struct + 6 methods. |
| ~~DOC.1~~ | DONE | Two user-facing sample programs under `docs/samples/`: (1) `json_format.volt` — decodes a hardcoded JSON payload with `json.Decode`, pretty-prints via `json.EncodePretty(doc, "  ")`, also accesses fields programmatically via `doc.ObjectLen()`. Output matches Python's `json.dumps(..., indent=2)`. (2) `parallel_sum.volt` — divide-and-conquer summation across 4 worker threads via `run` + `chan1N int`. Computes `sum(0..999) = 499500` in ~5 ms; demonstrates that `chan1N` is the standard primitive for many-writers-one-reader patterns and that the channel send/receive happens-before edge orders the writes (no explicit waitgroup/atomic needed). Both samples exit-42 on success per the project's regression convention. |
| ~~PERF.5~~ | DONE | Stress test `tests-internal/perf_string_build.volt` validates the PERF.1..4 O(n) claims. 13 cases at 1 KB..10 KB sizes: `strings.Repeat("x", 10000)`, `strings.ToLower` on 4.5 KB upper-case, `hex.EncodeToString` of 2 KB → 4 KB hex, `strings.Replace` "a"→"BB" on 3 KB src (output 6 KB), `sha256.SumHex` of 1 KB blob (Python-cross-checked). Completes in ~5 ms total. **Pre-refactor would have timed out the 10s `run_smoke.sh` budget** — this test is itself the perf claim. |
| ~~PERF.4~~ | DONE | Rewrite `path/filepath` (Base + Dir + Ext + Join) and `time.Format` on top of `bytes.Builder`. filepath uses `bld` as the builder variable to dodge the `b string` parameter clash in Join. time.Format chains 13 pieces (pad4 year + 5 pad2 fields + delimiters + 'Z') through one Builder. All 138 tests still pass. **Combined PERF.1..4**: O(n) end-to-end across **27 functions in 13 packages**. |
| ~~PERF.3~~ | DONE | Rewrite `json` (Encode + QuoteString + UnquoteString) and `strconv` (Itoa + FormatInt) on top of `bytes.Builder`. Itoa now delegates to `bytes.Builder.WriteInt` directly (which already handles MinInt). FormatInt for arbitrary bases keeps its own LSB-first scratch-buffer trick but writes to a Builder, not a string. QuoteString rewritten with an if-ladder for escape chars + WriteByte (was a single chained-else with `out + "\\n"` etc.). UnquoteString likewise reads escape chars and WriteBytes the decoded byte. Encode of arrays/objects builds via WriteByte for separators + WriteString for nested Encode results. All 138 tests still pass under the three-script regression. **Combined PERF.1+2+3**: encoding+crypto+strings+json+strconv stack is now O(n) end-to-end — a 10 KB string operation through Repeat or ToLower drops from ~100M byte-copies to ~10 KB. |
| ~~PERF.2~~ | DONE | Rewrite `strings` package builders on top of `bytes.Builder`. Refactored 9 functions: `ToLower`, `ToUpper`, `TrimSpace`, `Replace`, `Split` (both piece and tail builders), `Join`, `Repeat`, `TrimPrefix`, `TrimSuffix`, `SplitN` (both piece and tail builders), `Trim`. Each O(n²) `out + chr(b)` loop becomes amortized O(n) via `b.WriteByte`. Strings using a movable parameter (`s`, `sep`, `repl`, `prefix`, `suffix`) use the `"" + ...` BinaryExpr trick (Pass 36) to keep the parameter binding alive across the loop. `strings` package now imports `bytes`. All 138 tests still pass under the three-script regression — `strings` is consumed by nearly every other stdlib package + most tests, so this is a high-confidence change. |
| ~~PERF.1~~ | DONE | Rewrite encoding + crypto string builders on top of `bytes.Builder` (Pass 108) for O(n) instead of O(n²). Refactored 6 packages: `encoding/hex.EncodeToString` + `DecodeString`, `encoding/base64.EncodeToString` + `DecodeString`, `crypto/sha256.SumHex`, `crypto/sha1.SumHex`, `crypto/md5.SumHex`, `crypto/hmac.hexDigest`, `crypto/rand.Hex`. Each replaces an `out = out + chr(b)` per-byte loop (each iteration heap-allocates + memcpys the entire accumulator so far) with `b.WriteByte(b)` (amortized O(1) push). Pure refactor — no behavior change. All 138 tests still pass under the three-script regression (build / run / fmt-roundtrip), confirming the Builder API contract holds. Real-world gain: encoding a 1 KB file via hex.EncodeToString drops from ~2M byte-copies to ~2 KB. |
| ~~LANG.8~~ | DONE | Slice-of-slice double-indexing (`s[0][1]` on `[][]T`). `emitIndex` now propagates the inner element AST type via a new `indexedElemAst(x)` helper that walks IdentExpr / IndexExpr / SelectorExpr sources, peeling one SliceType layer at each level. The result of indexing a `%slice` value now carries `SliceElem` set so further indexing works. Test: `tests-internal/slice_of_slice.volt` (9 cases — `[][]byte` and `[][]int`). Unblocks future stdlib helpers over `[][]byte`. |
| ~~LANG.9~~ | DONE | `map[K]SliceType` support — slice-valued maps (`map[string][]int`, `map[string][]string`, etc.). New `mapValueIsSlice` helper; emitMapSet boxes the 24-byte `%slice` via heap-alloc + ptrtoint; emitMapGet unboxes via inttoptr + load with missing-key → zero-initialized slice. Result carries SliceElem so `m[k][i]` indexing works. Test: `tests-internal/map_slice_value.volt` (10 cases). |
| ~~STDLIB.84~~ | DONE | `slices.GroupByStringInt(s, key)` + `GroupByStringString(s, key)`. LINQ-style group-by-computed-key returning `map[string][]T`. Worked around checker's move-on-map-read rule via a three-pass write-only build (compute keys → dedup → per-key collect). Test: `tests-internal/slices_groupby.volt` (15 cases). |
| ~~UX.21~~ | DONE | Relaxed `maybeMoveBareIdent`'s map-read rule. For `map[K]V` where V is a Copy primitive (int, bool, byte, ...) the read no longer moves the map. New `isMapWithCopyValue(t)` predicate. Movable-V map reads (slice/string/struct) still move conservatively. Test: `tests-internal/map_copy_read.volt` (9 cases). |
| ~~STDLIB.85~~ | DONE | `slices.WindowInts(s, size) [][]int` + `WindowStrings(s, size) [][]string`. Overlapping sliding windows for n-gram / sequence-pattern analysis. size ≤ 0 / > len → empty. Test: `tests-internal/slices_window.volt` (15 cases). |
| ~~STDLIB.86~~ | DONE | `slices.RotateInts(s, k)` + `RotateStrings(s, k)` + `ProductInts(s)`. Cyclic rotation (negative k rotates right; k modulo n); product of all elements (empty → 1). Test: `tests-internal/slices_rotate_product.volt` (19 cases). |
| ~~LANG.10~~ | DONE | `for k, v := range m` over `map[K][]T` (slice-valued maps). Companion to LANG.9: new `valIsSlice` branch in `emitRangeOverMap` allocates a `%slice` for v, registers the binding with SliceElem set, and emits the inttoptr + load %slice unbox. Test: `tests-internal/range_map_slice_value.volt` (5 cases). |
| ~~STDLIB.87~~ | DONE | `bytes.Builder.WriteBytes(s []byte)` + `bytes.Builder.Bytes() []byte`. WriteBytes parallels WriteString for byte-slice input; Bytes returns a fresh copy of accumulated bytes (Builder retains internal buffer). Test: `tests-internal/bytes_builder_extras.volt` (9 cases). |
| ~~STDLIB.88~~ | DONE | `sort.IntsDesc(s) []int` + `sort.StringsDesc(s) []string`. Descending insertion sort; returns slice for rebinding. StringsDesc uses existing `stringLeq` helper. Test: `tests-internal/sort_desc.volt` (15 cases). |
| ~~STDLIB.89~~ | DONE | `testing.AssertEqInt` + `AssertEqString` + `AssertTrue` + `AssertFalse` + `AssertNil` + `AssertNotNil`. Each calls `t.Error(...)` on mismatch + returns bool for short-circuit. Test: `tests-internal/testing_asserts.volt` (10 cases). |
| ~~STDLIB.90~~ | DONE | `slices.IntsToStrings(s) []string` + `slices.StringsToInts(s) ([]int, error)`. Type-conversion helpers via strconv.Itoa / Atoi; StringsToInts short-circuits on first parse error. Test: `tests-internal/slices_int_string_conv.volt` (13 cases). |
| ~~STDLIB.91~~ | DONE | `bufio.SplitLines(s) []string`. Cross-platform line splitter — splits on `\n`, strips trailing `\r` from each piece. New `bytes` import in bufio. Test: `tests-internal/bufio_splitlines.volt` (18 cases). |
| ~~STDLIB.92~~ | DONE | `strings.Capitalize(s) string` + `strings.Reverse(s) string`. Byte-level: Capitalize uppercases the first byte if it's lowercase; Reverse reverses byte order. Test: `tests-internal/strings_capitalize_reverse.volt` (13 cases). |
| ~~DEMO.4~~ | DONE | End-to-end log parser integration test. Multi-line input → bufio.SplitLines → strings.Cut on ": " → slices.GroupByStringString → counts via for-range → sort.StringsAsc → IntsToStrings + Join. Validates the stdlib as a coherent whole. Test: `tests-internal/demo_log_parser.volt` (13 cases). |
| ~~STDLIB.93~~ | DONE | `bytes.String(b) string`. Wrapper over `syscall.BytesToString(b, len(b))` — convert byte-slice to immutable string by copying. Test: `tests-internal/bytes_string.volt` (7 cases). |
| ~~STDLIB.94~~ | DONE | `strings.Lines(s) []string`. Top-level line splitter parallel to bufio.SplitLines — handles LF and CRLF. Test: `tests-internal/strings_lines.volt` (11 cases). |
| ~~STDLIB.95~~ | DONE | `time.FormatDuration(ns) string`. Human-readable duration rendering — picks coarsest unit (ns/us/ms/s/Xm Ys/Xh Ym). Negative prefix. Test: `tests-internal/time_format_duration.volt` (15 cases). |
| ~~STDLIB.96~~ | DONE | `slices.HeadInts(s, n)` + `TailInts(s, n)` + `HeadStrings` + `TailStrings`. Pagination/preview helpers; n > len → copy, n ≤ 0 → empty. Test: `tests-internal/slices_head_tail.volt` (17 cases). |
| ~~STDLIB.97~~ | DONE | `slices.SampleInts(s, k)` + `SampleStrings(s, k)`. Random sample without replacement via partial Fisher-Yates from `crypto/rand`. k > len → shuffled copy. Test: `tests-internal/slices_sample.volt` (9 cases). |
| ~~STDLIB.98~~ | DONE | `slices.StringsToSet(s) map[string]bool` + `SetContainsString(set, key) bool`. Set-via-map helpers for O(1) membership. Test: `tests-internal/slices_strings_set.volt` (13 cases). |
| ~~STDLIB.99~~ | DONE | `slices.SetUnionStrings` + `SetIntersectionStrings` + `SetDifferenceStrings`. Set-algebra over `map[string]bool` sets. Test: `tests-internal/slices_set_ops.volt` (12 cases). |
| ~~STDLIB.100~~ | DONE | `(t Time).AddDate(years, months, days) Time`. Civil-calendar date arithmetic; preserves time-of-day. Month overflow normalized via 0-indexed divmod. Test: `tests-internal/time_adddate.volt` (12 cases). |
| ~~STDLIB.101~~ | DONE | `(t Time).Truncate(d) Time` + `(t Time).Round(d) Time`. Bucket-timestamp helpers; Truncate rounds toward zero, Round nearest with ties-away-from-zero. d ≤ 0 → unchanged. Test: `tests-internal/time_truncate_round.volt` (11 cases). |
| ~~STDLIB.102~~ | DONE | `(t Time).Weekday() int` + `WeekdayName() string`. Day-of-week accessor (0=Sun..6=Sat) via `(daysSinceEpoch + 4) mod 7`. Test: `tests-internal/time_weekday.volt` (13 cases). |
| ~~STDLIB.103~~ | DONE | `time.IsLeapYear(y) bool` + `time.DaysInMonth(y, m) int`. Gregorian calendar utilities — leap-year rule (div 4 not 100 unless 400); month lookup with Feb leap-year branch; out-of-range month → 0. Test: `tests-internal/time_leap_days.volt` (18 cases). |
| ~~STDLIB.104~~ | DONE | `(t Time).YearDay() int`. Day-of-year accessor (1..366). Computed via `daysSinceEpoch - daysFromCivil(year, 1, 1) + 1`. Test: `tests-internal/time_yearday.volt` (10 cases). |
| ~~STDLIB.105~~ | DONE | `time.MonthName(m) string` + `(t Time).IsZero() bool`. Month name lookup (1..12 → "January".."December", out-of-range → ""); zero-Time check (ns == 0). Test: `tests-internal/time_monthname_iszero.volt` (12 cases). |
| ~~STDLIB.106~~ | DONE | `(t Time).Equal(u) bool` + `(t Time).IsLeapYear() bool`. Convenience methods — instant equality + leap-year check on Time directly. Test: `tests-internal/time_equal_leap.volt` (8 cases). |
| ~~STDLIB.107~~ | DONE | `slices.MinByInt(s, key) int` / `MaxByInt(s, key) int` / `MinByString(s, key) string` / `MaxByString(s, key) string`. Key-projection min/max — caller supplies `key fun(T) int` and the function returns the element with the smallest (or largest) projected key. Ties resolve to first occurrence. Empty slice returns zero value. Test: `tests-internal/slices_minmax_by.volt` (17 cases). |
| ~~STDLIB.108~~ | DONE | `strings.PadLeft(s, n, c byte) string` + `PadRight(s, n, c byte) string`. Byte-level padding to total length n with pad byte c. If len(s) ≥ n, returns s unchanged. n ≤ 0 short-circuits. Test: `tests-internal/strings_pad.volt` (14 cases). |
| ~~STDLIB.109~~ | DONE | `strings.Title(s) string`. Capitalizes the first byte of each whitespace-separated word (whitespace = ' '/'\t'/'\n'/'\r'). Non-letter word-starts pass through. Byte-level. Test: `tests-internal/strings_title.volt` (15 cases). |
| ~~STDLIB.110~~ | DONE | `slices.IntersperseInt(s, sep int) []int` + `IntersperseString(s, sep string) []string`. Returns a new slice with `sep` inserted between adjacent elements. Result length 2n-1 for n≥2; passthrough for n≤1. Test: `tests-internal/slices_intersperse.volt` (21 cases). |
| ~~STDLIB.111~~ | DONE | `slices.ReduceInt(s, init int, fn fun(int, int) int) int` + `ReduceString(s, init string, fn fun(string, string) string) string`. Left-fold. Empty slice returns init. Useful for sums, products, max-with-default, custom accumulators. Test: `tests-internal/slices_reduce.volt` (12 cases). |
| ~~STDLIB.112~~ | DONE | `slices.AccumulateInt(s) []int` + `AccumulateMaxInt(s) []int`. Prefix sums + prefix maxima. Result same length as input. Test: `tests-internal/slices_accumulate.volt` (21 cases). |
| ~~STDLIB.113~~ | DONE | `sort.IntsAreSortedDesc(s) bool` + `StringsAreSortedDesc(s) bool`. Non-increasing-order predicates that complete the sort surface alongside the existing ascending IsSorted helpers. Test: `tests-internal/sort_desc_predicate.volt` (14 cases). |
| ~~STDLIB.114~~ | DONE | `strings.Center(s, n int, c byte) string`. Symmetric pad to width n; odd remainder goes to the right (Python-compat). Companion to STDLIB.108 PadLeft/PadRight. Test: `tests-internal/strings_center.volt` (13 cases). |
| ~~STDLIB.115~~ | DONE | `slices.MedianInt(s) int`. Returns the median of s (middle of sorted copy for odd n; avg of two middles for even n, truncated). Empty → 0. Input not mutated. Adds `import "sort"` to slices. Test: `tests-internal/slices_median.volt` (12 cases). |
| ~~STDLIB.116~~ | DONE | `slices.ModeInt(s) int` + `ModeString(s) string`. Most-frequent element; ties resolve to first occurrence. Empty → zero value. O(n²). Test: `tests-internal/slices_mode.volt` (12 cases). |
| ~~STDLIB.117~~ | DONE | `(t Time).IsWeekend() bool` + `(t Time).IsWeekday() bool`. Saturday/Sunday vs Monday-through-Friday predicates delegating to Weekday(). Test: `tests-internal/time_weekend.volt` (14 cases). |
| ~~STDLIB.118~~ | DONE | `maps.KeyOfMaxStringInt(m) string` + `KeyOfMinStringInt(m) string`. Argmax/argmin over `map[string]int`; ties resolve to first-encountered-key (order-undefined-but-deterministic). Empty → "". Test: `tests-internal/maps_keyof_extremum.volt` (12 cases). |
| ~~STDLIB.119~~ | DONE | `strings.IsBlank(s) bool` + `strings.CountByte(s, c byte) int`. Empty/whitespace-only predicate + single-byte counter. Test: `tests-internal/strings_blank_countbyte.volt` (16 cases). |
| ~~STDLIB.120~~ | DONE | `slices.HistogramString(s []string) map[string]int`. Frequency table — distinct string → count. Companion to ModeString that returns the full distribution. Test: `tests-internal/slices_histogram.volt` (13 cases). |
| ~~STDLIB.121~~ | DONE | `strings.TrimLeft(s, cutset) string` + `strings.TrimRight(s, cutset) string`. Side-only variants of the existing two-sided Trim; matches `bytes.TrimLeft`/`TrimRight`. Test: `tests-internal/strings_trim_sides.volt` (15 cases). |
| ~~STDLIB.122~~ | DONE | `slices.LongestRunInt(s) int` + `LongestRunString(s) int`. Length of the longest consecutive-equal run. Empty → 0, all-distinct → 1, all-same → n. Useful for streak detection / RLE analysis. Test: `tests-internal/slices_longestrun.volt` (13 cases). |
| ~~STDLIB.123~~ | DONE | `strings.IsAscii(s) bool` + `slices.MaxMinInt(s) (int, int)`. ASCII-range predicate (all bytes ≤ 127) + single-pass (max, min) two-return. Test: `tests-internal/strings_isascii_maxmin.volt` (19 cases). |
| ~~STDLIB.124~~ | DONE | `math.IsPrime(n) bool` + `math.NumDigits(n) int`. Trial-division primality up to sqrt(n) + decimal digit count of AbsInt(n) (sign not counted, NumDigits(0) = 1). Test: `tests-internal/math_isprime_numdigits.volt` (29 cases). |
| ~~STDLIB.125~~ | DONE | `math.PrimeFactorsInt(n) []int` + `math.IsPowerOfTwo(n) bool`. Ascending prime factorization with multiplicity + bitwise power-of-two predicate. Test: `tests-internal/math_prime_factors.volt` (31 cases). |
| ~~STDLIB.126~~ | DONE | `slices.AccumulateMinInt(s) []int` + `slices.AccumulateProductInt(s) []int`. Running min + running product, completing the prefix-aggregate surface alongside STDLIB.112 sum/max. Test: `tests-internal/slices_accumulate_min_prod.volt` (25 cases). |
| ~~STDLIB.127~~ | DONE | `slices.MovingAverageInt(s []int, k int) []int`. Sliding-window mean over k consecutive elements. Length n - k + 1; O(n) via running sum. k ≤ 0 or n < k → empty. Test: `tests-internal/slices_movingavg.volt` (19 cases). |
| ~~STDLIB.128~~ | DONE | `(t Time).DaysBetween(u Time) int`. Civil days from u to t (floor-div); time-of-day ignored. Test: `tests-internal/time_days_between.volt` (11 cases). |
| ~~STDLIB.129~~ | DONE | `math.IsqrtCeil(n) int` + `math.NextPrime(n) int`. Ceiling integer square root + next-prime-strictly-greater. Test: `tests-internal/math_isqrt_ceil_next_prime.volt` (27 cases). |
| ~~STDLIB.130~~ | DONE | `slices.SumIntFunc(s, fn) int` + `slices.SumStringFunc(s, fn) int` + `slices.PartitionAtInt(s, i) ([]int, []int)` + `slices.PartitionAtString(s, i) ([]string, []string)`. Fold-sum with int-projection + split-at-index with clamping. Test: `tests-internal/slices_sumfunc_partition_at.volt` (27 cases). |
| ~~STDLIB.131~~ | DONE | `math.Factorial(n) int` (n ∈ [0, 20], else 0) + `math.Binomial(n, k) int` (iterative, overflow-safe via symmetry + interleaved mul/div). Test: `tests-internal/math_factorial_binomial.volt` (25 cases). |
| ~~STDLIB.132~~ | DONE | `slices.MovingMaxInt(s []int, k int) []int` + `slices.MovingMinInt(s []int, k int) []int`. Sliding-window extrema (companion to STDLIB.127 MovingAverageInt). O(n*k) — small k is the common case. Test: `tests-internal/slices_moving_max_min.volt` (28 cases). |
| ~~STDLIB.133~~ | DONE | `strings.ReplaceFirst(s, old, repl) string` + `strings.ReplaceN(s, old, repl, n int) string`. Bounded-count replacements alongside the existing all-occurrences Replace. n < 0 means "all" (Go-compat). Test: `tests-internal/strings_replace_first_n.volt` (18 cases). |
| ~~STDLIB.134~~ | DONE | `strings.NormalizeWhitespace(s) string`. Trims leading/trailing AND collapses internal whitespace runs (space/tab/lf/cr/vt/ff) into a single space byte. Test: `tests-internal/strings_normalize_whitespace.volt` (15 cases). |
| ~~STDLIB.135~~ | DONE | `math.GcdInts(s) int` + `math.LcmInts(s) int`. n-ary GCD/LCM via folding the existing binary helpers. Early-exit GCD on hitting 1; LCM short-circuits on zero input. Test: `tests-internal/math_gcd_lcm_ints.volt` (15 cases). |
| ~~STDLIB.136~~ | DONE | `slices.SortIndicesInt(s) []int` + `slices.SortIndicesString(s) []int`. Stable permutation that would sort s; input not mutated. Useful for parallel-array sorting. Test: `tests-internal/slices_sort_indices.volt` (27 cases). |
| ~~STDLIB.137~~ | DONE | `strings.EscapeHTML(s) string` + `strings.UnescapeHTML(s) string`. Five-entity HTML safety (`<>&\"'` + `&apos;` alias for unescape). Unknown entity-like tokens pass through verbatim. Test: `tests-internal/strings_escape_html.volt` (26 cases). |
| ~~STDLIB.138~~ | DONE | `(t Time).HoursBetween(u Time) int` + `(t Time).MinutesBetween(u Time) int`. Finer-grained companions to DaysBetween (floor-div on nanosecond delta). Test: `tests-internal/time_hours_minutes_between.volt` (17 cases). |
| ~~STDLIB.139~~ | DONE | `strings.URLEncode(s) string` + `strings.URLDecode(s) (string, error)`. RFC 3986 percent-encoding; uppercase hex; space → %20 (not +). New private helpers + `import "errors"`. Test: `tests-internal/strings_url_encode.volt` (24 cases). |
| ~~STDLIB.140~~ | DONE | `math.NextPow2(n) int` + `(t Time).SecondsBetween(u Time) int`. Next-power-of-two-up (overflow → 0) + seconds-between (completes the floor-div between-quartet). Test: `tests-internal/math_nextpow2_secs_between.volt` (22 cases). |
| ~~STDLIB.141~~ | DONE | `slices.AllSameInt(s) bool` + `slices.AllSameString(s) bool`. Reports whether every element equals s[0]; empty/single → true. Test: `tests-internal/slices_allsame.volt` (13 cases). |
| ~~STDLIB.142~~ | DONE | `slices.UniqueByInt(s, key fun(int) int) []int` + `slices.UniqueByString(s, key fun(string) int) []string` + `slices.UniqueByStringKey(s, key fun(string) string) []string`. Projection-keyed dedup preserving first occurrence. Test: `tests-internal/slices_unique_by.volt` (19 cases). |
| ~~STDLIB.143~~ | DONE | `strings.HasAnyPrefix(s, prefixes []string) bool` + `strings.HasAnySuffix(s, suffixes []string) bool`. Test against a candidate set. Test: `tests-internal/strings_has_any_affix.volt` (16 cases). |
| ~~STDLIB.144~~ | DONE | `slices.DropInts(s, n int) []int` + `slices.DropStrings(s, n int) []string`. Drop first n elements; complement to Head/Tail. Test: `tests-internal/slices_drop.volt` (22 cases). |
| ~~STDLIB.145~~ | DONE | `math.IsPerfectSquare(n) bool` + `math.IsPowerOf(value, base int) bool`. Sqrt-back square check + arbitrary-base power test. Test: `tests-internal/math_perfect_square_powerof.volt` (35 cases). |
| ~~STDLIB.146~~ | DONE | `slices.LongestStreakInt(s, pred fun(int) bool) int` + `LongestStreakString(s, pred fun(string) bool) int` + `slices.GroupAdjacentInt(s) [][]int`. Predicate-based longest-run + adjacency-grouping. Test: `tests-internal/slices_streak_groupadj.volt` (22 cases). |
| ~~STDLIB.147~~ | DONE | `(t Time).AddMillis(n) Time` + `AddSeconds(n) Time` + `AddMinutes(n) Time` + `AddHours(n) Time`. Convenience wrappers over Add(ns). Test: `tests-internal/time_add_units.volt` (14 cases). |
| ~~STDLIB.148~~ | DONE | `slices.GroupAdjacentString(s) [][]string` + `math.IsCoprime(a, b int) bool`. String adjacency-grouping + GCD-coprimality test. Test: `tests-internal/slices_groupadj_str_coprime.volt` (27 cases). |
| ~~STDLIB.149~~ | DONE | `strings.LongestCommonPrefix(strs []string) string` + `strings.LongestCommonSuffix(strs []string) string`. Longest shared prefix/suffix across every element. Test: `tests-internal/strings_longest_common.volt` (16 cases). |
| ~~STDLIB.150~~ | DONE | `math.SumOfDigits(n) int` + `math.PowMod(base, exp, m int) int`. Decimal-digit sum + modular exponentiation via square-and-multiply. Test: `tests-internal/math_sumofdigits_powmod.volt` (24 cases). |
| ~~STDLIB.151~~ | DONE | `slices.FindInt(s, pred) (int, bool)` + `slices.FindString(s, pred) (string, bool)`. First-match-with-found-flag — returns value + ok; complements IndexFuncInt's index-only return. Test: `tests-internal/slices_find.volt` (16 cases). |
| ~~STDLIB.152~~ | DONE | `math.DigitalRoot(n) int` + `slices.IsSubsetInt(a, b) bool` + `slices.IsSubsetString(a, b) bool`. Repeated digit sum + set-style subset predicate (multiplicity ignored). Test: `tests-internal/math_digital_root_subset.volt` (22 cases). |
| ~~STDLIB.153~~ | DONE | `slices.IntersectInt(a, b) []int` + `slices.IsDisjointInt(a, b) bool` + `slices.IsDisjointString(a, b) bool`. Set intersection (distinct, order-from-a) + symmetric no-overlap predicate. Test: `tests-internal/slices_intersect_disjoint.volt` (19 cases). |
| ~~STDLIB.154~~ | DONE | `slices.UnionInt(a, b) []int` + `slices.DifferenceInt(a, b) []int`. Completes the set-ops triad (alongside STDLIB.153 Intersect). Test: `tests-internal/slices_union_difference.volt` (18 cases). |
| ~~STDLIB.155~~ | DONE | `slices.IntersectString(a, b) []string` + `slices.UnionString(a, b) []string` + `slices.DifferenceString(a, b) []string`. String parity for the set-ops triad. Test: `tests-internal/slices_string_setops.volt` (23 cases). |
| ~~STDLIB.156~~ | DONE | `(t Time).IsBetween(lo, hi Time) bool` + `math.IsArmstrong(n) bool`. Closed-interval Time check + narcissistic-number predicate. Test: `tests-internal/time_isbetween_armstrong.volt` (25 cases). |
| ~~STDLIB.157~~ | DONE | `math.IsPalindromeInt(n) bool` + `strings.IsPalindrome(s) bool`. Decimal-digit + byte palindrome predicates. Test: `tests-internal/palindrome.volt` (26 cases). |
| ~~STDLIB.158~~ | DONE | `(t Time).IsAtOrBefore(u) bool` + `IsAtOrAfter(u) bool` + `Min(u) Time` + `Max(u) Time`. Inclusive comparisons + pairwise extremes; composes to `t.Max(floor).Min(ceil)` clamp. Test: `tests-internal/time_inclusive_compare.volt` (15 cases). |
| ~~STDLIB.159~~ | DONE | `strings.IsNumeric(s) bool` + `strings.IsAlpha(s) bool` + `strings.IsAlphanumeric(s) bool`. Whole-string ASCII content classifiers; empty → false. Test: `tests-internal/strings_classifiers.volt` (28 cases). |
| ~~STDLIB.160~~ | DONE | `strings.IsHex(s) bool` + `(t Time).MonthShortName() string`. Hex-only predicate + 3-letter month abbrev ("Jan".."Dec"). Test: `tests-internal/strings_ishex_monthshort.volt` (29 cases). |
| ~~STDLIB.161~~ | DONE | `(t Time).WeekdayShortName() string` + `strings.IsBinary(s) bool`. 3-letter weekday abbrev ("Sun".."Sat") + 0/1-only predicate. Test: `tests-internal/time_weekday_short_isbinary.volt` (21 cases). |
| ~~STDLIB.162~~ | DONE | `math.NextMultipleOf(n, m) int` + `math.PrevMultipleOf(n, m) int`. Integer alignment up/down to nearest multiple. Test: `tests-internal/math_multiple_of.volt` (26 cases). |
| ~~STDLIB.163~~ | DONE | `(t Time).IsSameDay(u) bool` + `IsSameMonth(u) bool` + `IsSameYear(u) bool`. Calendar-equality predicates that ignore time-of-day. Test: `tests-internal/time_issame_day_month_year.volt` (20 cases). |
| ~~STDLIB.164~~ | DONE | `math.SumDivisors(n) int` + `math.IsPerfect(n) bool`. Divisor sum (O(sqrt n), sqrt not double-counted) + perfect-number predicate. Test: `tests-internal/math_divisors_perfect.volt` (27 cases). |
| ~~STDLIB.165~~ | DONE | `math.IsAbundant(n) bool` + `math.IsDeficient(n) bool`. Completes the divisor-classification triad alongside IsPerfect. Test: `tests-internal/math_abundant_deficient.volt` (30 cases). |
| ~~STDLIB.166~~ | DONE | `(t Time).StartOfDay() Time` + `EndOfDay() Time` + `StartOfMonth() Time` + `StartOfYear() Time`. Calendar-boundary helpers (midnight, last-ns, first-of-month, Jan-1). Test: `tests-internal/time_startof.volt` (28 cases). |
| ~~STDLIB.167~~ | DONE | `(t Time).EndOfMonth() Time` + `EndOfYear() Time`. Completes the calendar-boundary surface. EndOfMonth uses DaysInMonth for leap-correct Feb. Test: `tests-internal/time_endof.volt` (22 cases). |
| ~~STDLIB.168~~ | DONE | `slices.WindowedSumInt(s, k) []int` + `math.LogIntBase(n, base) int`. Sliding window sum (companion to MovingAverage, no divide) + integer log base. Test: `tests-internal/slices_winsum_math_logbase.volt` (32 cases). |
| ~~STDLIB.169~~ | DONE | `strings.IsValidIdentifier(s) bool` + `bytes.Reverse(s []byte) []byte`. Go-style identifier predicate + byte-order reversal for []byte. Test: `tests-internal/strings_ident_bytes_reverse.volt` (29 cases). |
| ~~STDLIB.170~~ | DONE | `strings.Indent(s, prefix) string` + `(t Time).Quarter() int`. Per-line prefix indenter + calendar quarter (1..4). Test: `tests-internal/strings_indent_time_quarter.volt` (20 cases). |
| ~~STDLIB.171~~ | DONE | `(t Time).StartOfQuarter() Time` + `EndOfQuarter() Time`. Quarter-boundary helpers (Jan 1 / Apr 1 / Jul 1 / Oct 1 → end of Mar / Jun / Sep / Dec). Test: `tests-internal/time_startend_quarter.volt` (21 cases). |
| ~~STDLIB.172~~ | DONE | `math.TriangularInt(n) int` + `math.FibInt(n) int`. Closed-form n*(n+1)/2 + iterative Fibonacci (F(50) fits int64). Test: `tests-internal/math_triangular_fib.volt` (26 cases). |
| ~~STDLIB.173~~ | DONE | `slices.ScanInt(s, init, fn) []int` + `slices.ScanString(s, init, fn) []string`. Running-history Reduce — returns the accumulator at every step. Test: `tests-internal/slices_scan.volt` (23 cases). |
| ~~STDLIB.174~~ | DONE | `math.SumSquaresDigits(n) int` + `math.IsHappyNumber(n) bool`. Digit-squares sum + happy-number predicate via Floyd's cycle detection. Test: `tests-internal/math_happy_number.volt` (31 cases). |
| ~~STDLIB.175~~ | DONE | `strings.SplitMap(s, sep, fn) []string` + `(t Time).IsLastDayOfMonth() bool`. Split-then-transform pipeline + last-day-of-month predicate (leap-Feb correct via DaysInMonth). Test: `tests-internal/strings_splitmap_time_lastday.volt` (20 cases). |
| ~~STDLIB.176~~ | DONE | `math.LerpInt(a, b, t, denom) int` + `slices.ZipStringInt(keys, vals) []string` + `slices.ZipStringString(keys, vals) []string`. Integer linear interpolation + key=value zip pair. Test: `tests-internal/math_lerp_slices_zip.volt` (21 cases). |
| ~~STDLIB.177~~ | DONE | `math.MapRange(x, srcLo, srcHi, dstLo, dstHi) int` + `(t Time).DayOfQuarter() int`. Linear range projection + 1-based day-of-quarter. Test: `tests-internal/math_maprange_time_dayq.volt` (22 cases). |
| ~~STDLIB.178~~ | DONE | `math.CeilDivInt(a, b) int` + `math.RoundDivInt(a, b) int`. Ceiling + half-up integer division for non-negative a + positive b. Test: `tests-internal/math_ceil_round_div.volt` (24 cases). |
| ~~STDLIB.179~~ | DONE | `strings.WordWrap(s, width) string`. Greedy word-boundary wrap. Test: `tests-internal/strings_wordwrap.volt` (14 cases). |
| ~~STDLIB.180~~ | DONE | `strings.Dedent(s) string`. Remove longest common leading-whitespace prefix from every non-blank line (Python textwrap.dedent shape). Inverse of Indent. Test: `tests-internal/strings_dedent.volt` (13 cases). |
| ~~STDLIB.181~~ | DONE | `strings.LineCount(s) int` + `strings.LineAt(s, idx) string`. Trailing-newline-aware line count + single-line lookup without full Lines() materialization. Test: `tests-internal/strings_linecount_lineat.volt` (25 cases). |
| ~~STDLIB.182~~ | DONE | `slices.AdjacentDiffsInt(s) []int` + `slices.AdjacentEqualsInt(s) int`. Pairwise differences (len n-1) + adjacent-equal-pair count. Test: `tests-internal/slices_adjacent.volt` (21 cases). |
| ~~STDLIB.183~~ | DONE | `slices.IsArithmeticInt(s) bool` + `slices.IsGeometricInt(s) bool`. Progression predicates (constant diff / constant integer ratio). Test: `tests-internal/slices_arith_geom.volt` (21 cases). |
| ~~STDLIB.184~~ | DONE | `slices.IsStrictlyIncreasingInt(s) bool` + `slices.IsStrictlyDecreasingInt(s) bool`. Strict-monotonic predicates (no equals allowed); complements sort.IntsAreSorted variants. Test: `tests-internal/slices_strict_monotonic.volt` (17 cases). |
| ~~STDLIB.185~~ | DONE | `math.IsTriangularInt(n) bool` + `math.IsFibonacciInt(n) bool`. Perfect-square-identity number-property predicates. Test: `tests-internal/math_is_triangular_fib.volt` (36 cases). |
| ~~STDLIB.186~~ | DONE | `slices.MaxIndexInt(s) int` + `MinIndexInt(s) int` + `MaxIndexString(s) int` + `MinIndexString(s) int`. Argmax/argmin — index of extremum; ties to first occurrence; empty → -1. Test: `tests-internal/slices_argmax_argmin.volt` (22 cases). |
| ~~STDLIB.187~~ | DONE | `math.IsPentagonalInt(n) bool` + `math.IsHexagonalInt(n) bool`. Polygonal-number predicates extending IsTriangular/IsFibonacci via perfect-square identities. Test: `tests-internal/math_pentagonal_hexagonal.volt` (38 cases). |
| ~~STDLIB.188~~ | DONE | `math.IcbrtFloor(n) int` + `math.IsPerfectCube(n) bool`. Integer cube root (binary-search bounded by 2097151) + perfect-cube predicate. Test: `tests-internal/math_cbrt_perfect_cube.volt` (32 cases). |
| ~~STDLIB.189~~ | DONE | `strings.ContainsAll(s, subs []string) bool` + `strings.ContainsAnyStr(s, subs []string) bool`. Substring-set membership (every / at-least-one). Test: `tests-internal/strings_contains_all_any_str.volt` (18 cases). |
| ~~STDLIB.190~~ | DONE | `(t Time).IsFirstDayOfMonth() bool` + `IsFirstDayOfYear() bool` + `IsLastDayOfYear() bool`. Symmetric to IsLastDayOfMonth — period-boundary predicates. Test: `tests-internal/time_firstlast_day.volt` (20 cases). |
| ~~STDLIB.191~~ | DONE | `slices.SwapInt(s, i, j) []int` + `SwapString(s, i, j) []string` + `strings.CommonPrefixLen(a, b) int`. In-place swap (no-op on OOB) + cheap byte-prefix-length probe. Test: `tests-internal/slices_swap_strings_prefixlen.volt` (22 cases). |
| ~~STDLIB.192~~ | DONE | `strings.CommonSuffixLen(a, b) int` + `slices.CompareInt(a, b) int` + `slices.CompareString(a, b) int`. Suffix-length probe + tri-state slice lex comparators. Test: `tests-internal/strings_suffixlen_slices_cmp.volt` (22 cases). |
| ~~STDLIB.193~~ | DONE | `slices.MinMaxByInt(s, key) (int, int)` + `slices.PadInt(s, n, v) []int` + `slices.PadString(s, n, v) []string`. Single-pass argmin+argmax-by-projection + truncate-or-extend to exact length. Test: `tests-internal/slices_minmaxby_pad.volt` (27 cases). |
| ~~STDLIB.194~~ | DONE | `strings.IsValidUtf8(s) bool` + `strings.RuneCount(s) int`. First UTF-8 awareness — well-formedness predicate + code-point count. Uses `& 255` byte-widen trick (signed-byte sign-extension would otherwise misclassify 0xC0-0xFF leads). Test: `tests-internal/strings_utf8.volt` (21 cases). |
| ~~STDLIB.195~~ | DONE | `strings.Truncate(s, maxBytes, ellipsis) string` + `strings.FirstRune(s) string` + `strings.LastRune(s) string`. UI clamping + first/last rune extraction (UTF-8-aware, byte-walking with continuation skip). Test: `tests-internal/strings_truncate_runes.volt` (21 cases). |
| ~~STDLIB.196~~ | DONE | `strings.TruncateRunes(s, maxRunes, ellipsis) string` + `strings.RuneAt(s, idx) int`. UTF-8-aware truncation + rune-index extraction. Test: `tests-internal/strings_truncate_runes_rune_at.volt` (23 cases). |
| ~~STDLIB.197~~ | DONE | `strings.RuneSlice(s, lo, hi) string`. Rune-indexed half-open substring; UTF-8 boundary-safe. Test: `tests-internal/strings_rune_slice.volt` (19 cases). |
| ~~STDLIB.198~~ | DONE | `strings.RuneReverse(s) string` + `strings.ByteIndexRune(s, runeIdx) int`. Rune-order reversal (bytes within rune kept intact) + rune→byte index conversion. Test: `tests-internal/strings_rune_reverse_byte_index.volt` (23 cases). |
| ~~STDLIB.199~~ | DONE | `strings.RuneSplit(s) []string` + `strings.RuneIndexFromByte(s, byteIdx) int`. One-rune-per-entry slice + byte→rune index inverse. Completes bidirectional rune/byte index conversion. Test: `tests-internal/strings_rune_split_idx.volt` (24 cases). |
| ~~STDLIB.200~~ | DONE | `strings.IndexRune(s, r) int` + `strings.ContainsRune(s, r) bool`. Rune-aware membership / position lookup. Test: `tests-internal/strings_index_contains_rune.volt` (17 cases). **MILESTONE: 200th STDLIB widening.** |
| ~~STDLIB.201~~ | DONE | `strings.CountRune(s, r) int` + `strings.LastIndexRune(s, r) int`. Rune-count + last-occurrence; matches Go's empty-substring conventions. Test: `tests-internal/strings_count_lastindex_rune.volt` (21 cases). |
| ~~STDLIB.202~~ | DONE | `strings.ReplaceRune(s, old, repl) string`. Per-rune substitution; replacement may be wider or narrower than the rune it replaces. Test: `tests-internal/strings_replace_rune.volt` (13 cases). |
| ~~STDLIB.203~~ | DONE | `slices.MaxMinString(s) (string, string)` + `slices.VarianceIntScaled(s) int`. String single-pass (max, min) + integer sum-of-squared-deviations (caller divides). Test: `tests-internal/slices_maxminstr_variance.volt` (16 cases). |
| ~~STDLIB.204~~ | DONE | `slices.SortedCopyInt(s) []int` + `SortedDescCopyInt(s) []int` + `SortedCopyString(s) []string` + `SortedDescCopyString(s) []string`. Non-destructive sort variants — clones then sorts, originals untouched. Test: `tests-internal/slices_sorted_copy.volt` (21 cases). |
| ~~STDLIB.205~~ | DONE | `bytes.IsValidUtf8(s) bool` + `bytes.RuneCount(s) int`. []byte counterparts to the strings UTF-8 helpers. Test: `tests-internal/bytes_utf8.volt` (16 cases). |
| ~~STDLIB.206~~ | DONE | `(t Time).NextWeekday(w) Time` + `(t Time).PrevWeekday(w) Time`. Find next/prev calendar-weekday strictly after/before t (same-day advances by 7); w normalized mod 7. Test: `tests-internal/time_next_prev_weekday.volt` (20 cases). |
| ~~STDLIB.207~~ | DONE | `slices.UniqueSortedInt(s) []int` + `slices.UniqueSortedString(s) []string`. Sort + adjacent-dedup combined. Test: `tests-internal/slices_unique_sorted.volt` (23 cases). |
| ~~STDLIB.208~~ | DONE | `slices.RemoveFirstInt(s, v) []int` + `RemoveFirstString` + `RemoveAllInt(s, v) []int` + `RemoveAllString`. Value-based removal (first / every). Test: `tests-internal/slices_remove_first_all.volt` (23 cases). |
| ~~STDLIB.209~~ | DONE | `slices.InsertSortedInt(s, v) []int` + `InsertSortedString` + `slices.UpdateAtInt(s, i, v) []int` + `UpdateAtString`. Sorted-position insert + bounds-safe set-at-index. Test: `tests-internal/slices_insert_sorted_update.volt` (27 cases). |
| ~~STDLIB.210~~ | DONE | `slices.RemoveAtInt(s, idx) []int` + `slices.RemoveAtString(s, idx) []string`. Index-based removal (counterpart to value-based RemoveFirstInt). OOB returns a fresh copy. Test: `tests-internal/slices_remove_at.volt` (19 cases). |
| ~~STDLIB.211~~ | DONE | `slices.MoveInt(s, from, to) []int` + `MoveString` + `(t Time).HMS() (int, int, int)`. In-place element move with gap-fill shift + hour/minute/second tuple extractor. Test: `tests-internal/slices_move_time_hms.volt` (26 cases). |
| ~~STDLIB.212~~ | DONE | `(t Time).YMD() (int, int, int)` + `(t Time).Datetime() (int, int, int, int, int, int)`. Calendar-component tuple extractors (year/month/day + full 6-tuple). Test: `tests-internal/time_ymd_datetime.volt` (24 cases). |
| ~~STDLIB.213~~ | DONE | `strings.HumanCount(n) string`. K/M/B suffix formatting with optional one-decimal precision; recurses for negatives. Adds `import "strconv"` to strings. Test: `tests-internal/strings_humancount.volt` (21 cases). |
| ~~STDLIB.214~~ | DONE | `strings.HumanBytes(n) string`. IEC 1024-based B/KB/MB/GB/TB byte-size formatting with optional one-decimal precision. Test: `tests-internal/strings_humanbytes.volt` (14 cases). |
| ~~STDLIB.215~~ | DONE | `path/filepath.SplitExt(path) (string, string)`. Split into base + extension (final dot, extension includes the dot). Hidden basenames treated as having no extension. Test: `tests-internal/filepath_splitext.volt` (23 cases). |
| ~~STDLIB.216~~ | DONE | `path/filepath.Components(path) []string`. Split path on '/' into directory components; strips empty pieces. Test: `tests-internal/filepath_components.volt` (24 cases). |
| ~~STDLIB.217~~ | DONE | `slices.RotateRightInt(s, k) []int` + `RotateRightString` + `slices.IsAllPositiveInt(s) bool` + `slices.IsAllNegativeInt(s) bool`. Right-rotation wrapper + strict-sign-class predicates. Test: `tests-internal/slices_rotate_right_sign_preds.volt` (24 cases). |
| ~~STDLIB.218~~ | DONE | `slices.IsAllZeroInt(s) bool` + `slices.AnyZeroInt(s) bool` + `(t Time).IsLeapDay() bool`. Zero-presence predicates over int slices + Feb-29 calendar check. Test: `tests-internal/slices_zero_preds_time_leapday.volt` (20 cases). |
| ~~STDLIB.219~~ | DONE | `math.IsEven(n) bool` + `math.IsOdd(n) bool` + `math.NextEvenInt(n) int` + `math.NextOddInt(n) int`. Parity predicates + next-integer-with-target-parity. Test: `tests-internal/math_parity.volt` (28 cases). |
| ~~STDLIB.220~~ | DONE | `math.PrevEvenInt(n) int` + `math.PrevOddInt(n) int` + `math.IsMultipleOf(a, b) bool`. Symmetric Prev-parity + multiple-of-divisor predicate. Test: `tests-internal/math_prev_parity_multipleof.volt` (27 cases). |
| ~~STDLIB.221~~ | DONE | `slices.IsSetInt(s) bool` + `slices.IsSetString(s) bool`. All-distinct predicates via O(n) seen-set. Test: `tests-internal/slices_isset.volt` (16 cases). |
| ~~STDLIB.222~~ | DONE | `strings.NormalizeNewlines(s) string` + `(t Time).WeekOfMonth() int`. CRLF/CR→LF normalization + 1-based week-of-month index (Sunday-starting weeks). Test: `tests-internal/strings_normalize_newlines_time_wom.volt` (18 cases). |
| ~~STDLIB.223~~ | DONE | `(t Time).DayOfWeekISO() int` + `strings.RepeatRune(r, n) string`. ISO 8601 weekday (Mon=1..Sun=7) + UTF-8-aware character repeat. Test: `tests-internal/time_iso_strings_repeatrune.volt` (17 cases). |
| ~~STDLIB.224~~ | DONE | `strings.Decapitalize(s) string` + `strings.SwapCase(s) string`. Lower-first-byte (camelCase generator) + ASCII case flip. Test: `tests-internal/strings_decapitalize_swapcase.volt` (20 cases). |
| ~~STDLIB.225~~ | DONE | `strings.SnakeCase(s) string` + `strings.KebabCase(s) string`. Case-style converters with acronym-aware boundary detection (HTTPRequest → http_request). Test: `tests-internal/strings_case_converters.volt` (17 cases). |
| ~~STDLIB.226~~ | DONE | `strings.PascalCase(s) string` + `strings.CamelCase(s) string`. Inverse of Snake/Kebab — separators ('_', '-', ' ') drop out, the next letter is uppercased. CamelCase = Decapitalize(PascalCase). Roundtrips with SnakeCase. Test: `tests-internal/strings_pascal_camel_case.volt` (18 cases). |
| ~~STDLIB.227~~ | DONE | `strings.CommonPrefix(a, b) string` + `strings.CommonSuffix(a, b) string`. Longest shared byte sequence at the start / end of two strings. Useful for collapsing shared directory prefixes, finding shared file extensions. Test: `tests-internal/strings_common_prefix_suffix.volt` (21 cases). |
| ~~STDLIB.228~~ | DONE | `strings.Levenshtein(a, b) int` + `strings.HammingDistance(a, b) int`. Two classic string-distance metrics: Hamming for equal-length inputs (byte-mismatch count, -1 on length mismatch), Levenshtein for general edit distance (two-row DP). Byte-level for v1. Test: `tests-internal/strings_levenshtein_hamming.volt` (23 cases). |
| ~~STDLIB.229~~ | DONE | `math.SumDigits(n) int` + `math.ReverseDigits(n) int`. Base-10 digit utilities — sum and reverse. Sign dropped for sum, preserved for reverse. Trailing zeros drop on reverse (120 → 21). Useful for Harshad number tests, palindrome-digit checks. Test: `tests-internal/math_sumdigits_reverse.volt` (23 cases). |
| ~~STDLIB.230~~ | DONE | `strings.NGrams(s, n) []string` + `strings.IsAnagram(a, b) bool`. Sliding n-byte-substring window + 256-bucket byte-frequency anagram check. Useful for fuzzy matching and anagram puzzles. **Milestone: 380 buildable tests.** Test: `tests-internal/strings_ngrams_anagram.volt` (26 cases). |
| ~~STDLIB.231~~ | DONE | `math.IsAutomorphic(n) bool` + `math.IsHarshad(n) bool`. Classical number-theory predicates — n² ends with n's digits (5/6/25/76/376/625); n divisible by its digit sum (every 1..9, then 10/12/18/21/24/27/30). Test: `tests-internal/math_automorphic_harshad.volt` (27 cases). |
| ~~STDLIB.232~~ | DONE | `math.IsKaprekar(n) bool` + `math.IsPandigital(n) bool`. Kaprekar: n² splits into two parts summing to n (9/45/55/99/297/703/999); Pandigital: contains every digit 0..9 at least once (min 10-digit 1023456789). Test: `tests-internal/math_kaprekar_pandigital.volt` (24 cases). |
| ~~STDLIB.233~~ | DONE | `(t Time).ISOWeek() int` + `(t Time).ISOWeekYear() int`. ISO 8601 week numbering (Mon-start, week containing first Thursday is week 1). Year boundary cases: 2027-01-01 = ISO week 53 of 2026. Test: `tests-internal/time_isoweek.volt` (20 cases). |
| ~~STDLIB.234~~ | DONE | `math.Fibonacci(n) int` + `math.Catalan(n) int`. Two classical combinatorial sequences with explicit overflow caps (F(92) max, C(33) max). Iterative implementations dodge factorial intermediates. Test: `tests-internal/math_fibonacci_catalan.volt` (29 cases). |
| ~~STDLIB.235~~ | DONE | `(t Time).Yesterday() Time` + `(t Time).Tomorrow() Time`. AddDate(0, 0, ±1) wrappers preserving wall-clock; handle month/year/leap boundaries correctly. Test: `tests-internal/time_yesterday_tomorrow.volt` (23 cases). |
| ~~STDLIB.236~~ | DONE | `math.Lucas(n) int` + `math.Tribonacci(n) int`. Two Fibonacci-family sequences. Lucas: L(0)=2, L(1)=1 (companion to Fibonacci, L(n)=F(n-1)+F(n+1) identity). Tribonacci: T(0)=0, T(1)=1, T(2)=1 (OEIS A000073). Overflow caps L(90), T(75). Test: `tests-internal/math_lucas_tribonacci.volt` (33 cases). |
| ~~STDLIB.237~~ | DONE | `math.PrevPrime(n) int` + `math.NthPrime(n) int`. Prime navigators on top of IsPrime/NextPrime. PrevPrime walks down from n-1 (returns 0 for n ≤ 2). NthPrime is 1-indexed (1→2, 25→97, 100→541, 168→997). Test: `tests-internal/math_prevprime_nthprime.volt` (28 cases). |
| ~~STDLIB.238~~ | DONE | `(t Time).IsToday() bool` + `(t Time).IsThisYear() bool`. Predicates against current wall-clock Now(). Useful for "today" / "year-to-date" dashboard filters. Test: `tests-internal/time_istoday_isthisyear.volt` (10 cases). |
| ~~STDLIB.239~~ | DONE | `filepath.WithoutExt(path) string` + `filepath.WithExt(path, ext) string`. Filename-transform helpers on SplitExt. Auto-leading-dot ergonomics — `WithExt("foo.txt", "md")` = `WithExt("foo.txt", ".md")` = `"foo.md"`. Multi-extension only touches final segment. Test: `tests-internal/filepath_withext.volt` (17 cases). |
| ~~STDLIB.240~~ | DONE | `(t Time).IsThisMonth() bool` + `(t Time).IsThisWeek() bool`. Completes the "current period" predicate family alongside IsToday/IsThisYear. IsThisWeek uses ISO-week semantics. **Milestone: 390 buildable tests.** Test: `tests-internal/time_isthismonth_isthisweek.volt` (14 cases). |
| ~~STDLIB.241~~ | DONE | `math.PopCount(n) int` + `math.TrailingZeros(n) int`. Bit-manipulation primitives. PopCount uses Kernighan's `v & (v-1)` trick. TrailingZeros(0)=0 (convention). Both treat negatives as `|n|`. Test: `tests-internal/math_popcount_trailingzeros.volt` (34 cases). |
| ~~STDLIB.242~~ | DONE | `math.BitLen(n) int` + `math.LeadingZeros(n) int`. Companion bit primitives. BitLen = floor(log2(|n|))+1; LeadingZeros = 64 - BitLen. Identity: `BitLen(n) + LeadingZeros(n) == 64`. Test: `tests-internal/math_bitlen_leadingzeros.volt` (32 cases). |
| ~~STDLIB.243~~ | DONE | `math.RotateLeft64(n, k) int` + `math.RotateRight64(n, k) int`. Full-64-bit two's-complement bit rotation. k reduced mod 64; high bit wraps to low (and vice versa). Useful for hash rounds, crypto bit permutations. Test: `tests-internal/math_rotate64.volt` (26 cases). |
| ~~STDLIB.244~~ | DONE | `math.ReverseBits64(n) int` + `math.SwapBytes64(n) int`. Bit-reversal (bit i ↔ bit 63-i) and byte-reversal (endianness flip). Both involutions; 0 and -1 are fixed points. Useful for FFT bit-reverse, endian conversion, hash mixing. Test: `tests-internal/math_reversebits_swapbytes.volt` (18 cases). |
| ~~STDLIB.245~~ | DONE | `strings.AbbreviateMiddle(s, maxBytes, ellipsis) string` + `strings.Pluralize(word, n) string`. Middle-truncation (e.g. `/very/long/path/file.txt` → `/very....txt`) + naive English pluralization (word vs word+"s" based on |n|≠1). Test: `tests-internal/strings_abbreviate_pluralize.volt` (19 cases). |
| ~~STDLIB.246~~ | DONE | `strings.WordCount(s) int` + `strings.NumLines(s) int`. Allocation-free counters paralleling `len(Fields(s))` and the line-count semantics of bufio. Test: `tests-internal/strings_wordcount_numlines.volt` (22 cases). |
| ~~STDLIB.247~~ | DONE | `(t Time).StartOfWeek() Time` + `(t Time).EndOfWeek() Time`. Completes the week-boundary set (Mon-Sun per ISO 8601). Handles cross-month and cross-year week spans. Test: `tests-internal/time_startofweek_endofweek.volt` (25 cases). |
| ~~STDLIB.248~~ | DONE | `slices.MinMaxInts(s) (int, int)` + `slices.MinMaxStrings(s) (string, string)`. Single-pass (min, max) — cheaper than separate Min+Max walks. Useful for range-of / normalize / sparkline. Test: `tests-internal/slices_minmax.volt` (24 cases). |
| ~~STDLIB.249~~ | DONE | `filepath.WithName(path, name) string` + `filepath.IsHidden(path) bool`. Companion to WithExt — swap whole basename instead of just extension. IsHidden = leading-dot basename. Test: `tests-internal/filepath_withname_ishidden.volt` (21 cases). |
| ~~STDLIB.250~~ | DONE | `(t Time).WeeksBetween(u) int` + `(t Time).MonthsBetween(u) int`. Coarser duration helpers. Weeks = DaysBetween/7; Months handles partial-month-not-counted. **Milestone: 400 buildable tests.** Test: `tests-internal/time_weeks_months_between.volt` (17 cases). |
| ~~STDLIB.251~~ | DONE | `strings.EnsurePrefix(s, prefix) string` + `strings.EnsureSuffix(s, suffix) string`. Idempotent prepend/append — only act when affix isn't already there. Counterparts to TrimPrefix/TrimSuffix. Test: `tests-internal/strings_ensure_prefix_suffix.volt` (19 cases). |
| ~~STDLIB.252~~ | DONE | `(t Time).AddDays/AddWeeks/AddMonths/AddYears(n) Time`. Convenience AddDate wrappers. Underlying Hinnant alg does NOT clamp — Jan 31 + 1 month → early March, not Feb 28/29. Test: `tests-internal/time_add_calendar_units.volt` (25 cases). |
| ~~STDLIB.253~~ | DONE | `bytes.HammingDistance(a, b []byte) int` + `bytes.IsAnagram(a, b []byte) bool`. `[]byte` analogs of strings.HammingDistance / strings.IsAnagram. Test: `tests-internal/bytes_hamming_anagram.volt` (15 cases). |
| ~~STDLIB.254~~ | DONE | `math.MedianOfThree(a, b, c) int` + `math.MidpointInt(a, b) int`. Quickselect/quicksort pivot helper + overflow-safe binary-search midpoint. Test: `tests-internal/math_medianof3_midpoint.volt` (26 cases). |
| ~~STDLIB.255~~ | DONE | `maps.InvertStringString(m) map[string]string` + `maps.MapValuesStringString(m, fn) map[string]string`. Key↔value swap + value-projection helpers for string-string maps. Test: `tests-internal/maps_invert_mapvalues.volt` (17 cases). |
| ~~STDLIB.256~~ | DONE | `slices.ReplaceAllInt(s, old, repl) []int` + `slices.ReplaceAllString(s, old, repl) []string`. Element substitution preserving order + length. Useful for sentinel rewrites. Test: `tests-internal/slices_replaceall.volt` (25 cases). |
| ~~STDLIB.257~~ | DONE | `maps.MapKeysStringInt(m, fn)` + `maps.MapKeysStringString(m, fn)`. Key-transform helpers (opposite axis to MapValues*). Useful for case normalization, prefix stripping, namespace remapping. Test: `tests-internal/maps_mapkeys.volt` (18 cases). |
| ~~STDLIB.258~~ | DONE | `(t Time).StartOfHour() Time` + `(t Time).EndOfHour() Time`. Hourly bucketing for metrics / log entries. Test: `tests-internal/time_startofhour_endofhour.volt` (22 cases). |
| ~~STDLIB.259~~ | DONE | `(t Time).StartOfMinute() Time` + `(t Time).EndOfMinute() Time`. Finest-grain bucketing helper — closes out the StartOf*/EndOf* family. Test: `tests-internal/time_startofminute_endofminute.volt` (22 cases). |
| ~~STDLIB.260~~ | DONE | `math.SortedPair(a, b) (int, int)` + `math.SortedTriple(a, b, c) (int, int, int)`. Optimal fixed-size sort-network helpers for n=2/n=3. Returns (min, max) / (min, median, max). Test: `tests-internal/math_sortedpair_triple.volt` (33 cases). |
| ~~STDLIB.261~~ | DONE | `strings.Translate(s, from, to) string` + `strings.RemoveChars(s, chars) string`. Byte-mapping helpers: tr-like position-mapped substitution (including -d-style delete) + interior-and-edge char removal. Test: `tests-internal/strings_translate_removechars.volt` (20 cases). |
| ~~STDLIB.262~~ | DONE | `bytes.IsAscii(s []byte) bool` + `bytes.Translate(s, from, to []byte) []byte`. `[]byte` analogs of strings.IsAscii / strings.Translate. Test: `tests-internal/bytes_isascii_translate.volt` (21 cases). |
| ~~STDLIB.263~~ | DONE | `slices.RunLengthEncodeInt(s) ([]int, []int)` + `slices.RunLengthDecodeInt(values, counts) []int`. Classic RLE compression + inverse. Test: `tests-internal/slices_rle.volt` (38 cases). |
| ~~STDLIB.264~~ | DONE | `math.Log2Floor(n) int` + `math.Log2Ceil(n) int`. Power-of-two-capacity rounding helpers. Test: `tests-internal/math_log2_floor_ceil.volt` (34 cases). |
| ~~STDLIB.265~~ | DONE | `math.AlignUp(n, k) int` + `math.AlignDown(n, k) int`. Multiple-of-k alignment for memory layout / page sizes / cache lines. Test: `tests-internal/math_align.volt` (35 cases). |
| ~~STDLIB.266~~ | DONE | `slices.RunLengthEncodeString(s) ([]string, []int)` + `slices.RunLengthDecodeString(values, counts) []string`. `[]string` analog of the Int RLE pair. Test: `tests-internal/slices_rle_string.volt` (29 cases). |
| ~~STDLIB.267~~ | DONE | `bytes.RemoveChars(s, chars []byte) []byte` + `bytes.Levenshtein(a, b []byte) int`. `[]byte` analogs of strings.RemoveChars / strings.Levenshtein. Test: `tests-internal/bytes_removechars_levenshtein.volt` (19 cases). |
| ~~STDLIB.268~~ | DONE | `filepath.HeadN(path, n) string` + `filepath.TailN(path, n) string`. First / last n path components, rejoined. HeadN preserves leading `/`. Test: `tests-internal/filepath_head_tail.volt` (20 cases). |
| ~~STDLIB.269~~ | DONE | `slices.PrefixSumInt(s) []int` + `slices.SuffixSumInt(s) []int`. Cumulative-sum tables for O(1) range-sum queries. Test: `tests-internal/slices_prefixsum_suffixsum.volt` (23 cases). |
| ~~STDLIB.270~~ | DONE | `slices.PrefixMaxInt(s) []int` + `slices.PrefixMinInt(s) []int`. Running cumulative max/min ("best so far" / "worst so far"). Test: `tests-internal/slices_prefixmax_prefixmin.volt` (32 cases). |
| ~~STDLIB.271~~ | DONE | `slices.DifferencesInt(s) []int` + `slices.IsMonotonicInt(s) bool`. Discrete derivative + non-strict monotonicity check. Test: `tests-internal/slices_diff_monotonic.volt` (25 cases). |
| ~~STDLIB.272~~ | DONE | `slices.SymmetricDifferenceInt(a, b) []int` + `slices.DisjointInt(a, b) bool`. Set-XOR + disjointness predicate. Cardinality identity: \|a△b\| == \|a∪b\| - \|a∩b\|. Test: `tests-internal/slices_symdiff_disjoint.volt` (19 cases). |
| ~~STDLIB.273~~ | DONE | `slices.SymmetricDifferenceString(a, b) []string` + `slices.DisjointString(a, b) bool`. `[]string` mirror of the int versions. Test: `tests-internal/slices_symdiff_disjoint_string.volt` (18 cases). |
| ~~STDLIB.274~~ | DONE | `strings.Frequencies(s) []int` + `strings.MostCommonByte(s) byte`. 256-bucket byte histogram + dominant-byte extractor. Test: `tests-internal/strings_frequencies_mostcommon.volt` (18 cases). |
| ~~STDLIB.275~~ | DONE | `bytes.Frequencies(s []byte) []int` + `bytes.MostCommonByte(s []byte) byte`. `[]byte` analogs of the strings.* versions. Test: `tests-internal/bytes_frequencies_mostcommon.volt` (18 cases). |
| ~~STDLIB.276~~ | DONE | `slices.PrefixProductInt(s) []int` + `slices.SuffixProductInt(s) []int`. Cumulative-product tables (companion to the Sum tables). Test: `tests-internal/slices_prefixprod_suffixprod.volt` (26 cases). |
| ~~STDLIB.277~~ | DONE | `slices.ScaleInt(s, factor) []int` + `slices.ShiftInt(s, offset) []int`. Element-wise multiply / add (length-preserving). Useful for unit conversion / signal scaling / coordinate rebase. Test: `tests-internal/slices_scale_shift.volt` (27 cases). |
| ~~STDLIB.278~~ | DONE | `slices.AddInt(a, b) []int` + `slices.DotInt(a, b) int`. Pairwise-vector add + inner product. Test: `tests-internal/slices_add_dot.volt` (21 cases). |
| ~~STDLIB.279~~ | DONE | `slices.SubInt(a, b) []int` + `slices.MulInt(a, b) []int`. Element-wise subtraction + Hadamard product. Test: `tests-internal/slices_sub_mul.volt` (27 cases). |
| ~~STDLIB.280~~ | DONE | `slices.NegateInt(s) []int` + `slices.AbsValuesInt(s) []int`. Unary element-wise sign-flip + magnitude. Test: `tests-internal/slices_negate_abs.volt` (27 cases). |
| ~~STDLIB.281~~ | DONE | `slices.SquareInt(s) []int` + `slices.SignInt(s) []int`. Element-wise square + sign-extraction (-1/0/1). Test: `tests-internal/slices_square_sign.volt` (27 cases). |
| ~~STDLIB.282~~ | DONE | `math.FloorDivInt(a, b) int` + `math.FloorModInt(a, b) int`. Python-style floor-div/mod handling negatives correctly. Test: `tests-internal/math_floordiv_floormod.volt` (24 cases). |
| ~~STDLIB.283~~ | DONE | `(t Time).DaysInThisMonth() int` + `time.DaysInYear(year) int` + `(t Time).DaysInThisYear() int`. Calendar-capacity helpers. Test: `tests-internal/time_daysinthismonth_year.volt` (15 cases). |
| ~~STDLIB.284~~ | DONE | `math.NumDivisors(n) int` + `math.Divisors(n) []int`. Divisor enumeration via O(sqrt(n)) pair-iteration. Test: `tests-internal/math_numdivisors_divisors.volt` (34 cases). |
| ~~STDLIB.285~~ | DONE | `math.DistinctPrimeFactors(n) []int` + `math.Totient(n) int`. Number-theory utilities — distinct primes + Euler's φ via product formula. Test: `tests-internal/math_totient_distinctprimes.volt` (32 cases). |
| ~~STDLIB.286~~ | DONE | `math.IsSquarefree(n) bool` + `math.RadicalInt(n) int`. Squarefree predicate + product-of-distinct-primes. Test: `tests-internal/math_issquarefree_radical.volt` (34 cases). |
| ~~STDLIB.287~~ | DONE | `math.MoebiusInt(n) int` + `math.LittleOmega(n) int`. Möbius μ(n) (-1/0/+1) + distinct-prime count ω(n). Test: `tests-internal/math_moebius_littleomega.volt` (32 cases). |
| ~~STDLIB.288~~ | DONE | `math.BigOmega(n) int` + `math.LiouvilleInt(n) int`. Ω(n) (with-multiplicity prime count) + λ(n) Liouville function. Test: `tests-internal/math_bigomega_liouville.volt` (32 cases). |
| ~~STDLIB.289~~ | DONE | `slices.RankInt(s) []int` + `slices.IsPermutationInt(s) bool`. Competition-rank ordering + permutation-of-{1..n} predicate. Test: `tests-internal/slices_rank_ispermutation.volt` (24 cases). |
| ~~STDLIB.290~~ | DONE | `slices.LowerBoundInt(s, v) int` + `slices.UpperBoundInt(s, v) int`. Binary-search insertion-point helpers mirroring C++ STL. Upper - Lower = frequency of v. Test: `tests-internal/slices_lower_upper_bound.volt` (23 cases). |
| ~~STDLIB.291~~ | DONE | `slices.LowerBoundString(s, v) int` + `slices.UpperBoundString(s, v) int`. `[]string` mirror of LowerBoundInt/UpperBoundInt. Test: `tests-internal/slices_lower_upper_bound_string.volt` (23 cases). |
| ~~STDLIB.292~~ | DONE | `slices.CountSortedInt(s, v) int` + `slices.CountSortedString(s, v) int`. O(log n) frequency on sorted slices (Upper-Lower wrapper). Test: `tests-internal/slices_countsorted.volt` (17 cases). |
| ~~STDLIB.293~~ | DONE | `strings.OnlyDigits(s) string` + `strings.OnlyAlphanumeric(s) string`. Filter helpers — keep ASCII digits / alphanumerics only. Test: `tests-internal/strings_only_digits_alnum.volt` (18 cases). |
| ~~STDLIB.294~~ | DONE | `strings.OnlyLetters(s) string` + `strings.MaskMiddle(s, ks, ke, mask byte) string`. Letter-only filter + PII redaction (credit-card / phone-number style). Test: `tests-internal/strings_onlyletters_mask.volt` (18 cases). |
| ~~STDLIB.295~~ | DONE | `bytes.OnlyDigits(s) []byte` + `bytes.OnlyAlphanumeric(s) []byte`. `[]byte` analogs of strings.OnlyDigits / OnlyAlphanumeric. Test: `tests-internal/bytes_only_digits_alnum.volt` (20 cases). |
| ~~STDLIB.296~~ | DONE | `(t Time).Hour12() int` + `(t Time).Meridiem() string`. 12-hour clock support — hour in 1..12 + AM/PM. Test: `tests-internal/time_hour12_meridiem.volt` (18 cases). |
| ~~STDLIB.297~~ | DONE | `(t Time).Format12Hour() string` + `(t Time).Format12HourFull() string`. "h:mm AM/PM" and "h:mm:ss AM/PM" formatters. Test: `tests-internal/time_format12hour.volt` (15 cases). |
| ~~STDLIB.298~~ | DONE | `(t Time).TimeString() string` + `(t Time).TimeStringShort() string`. 24-hour "HH:MM:SS" / "HH:MM" formatters. Test: `tests-internal/time_timestring.volt` (13 cases). |
| ~~STDLIB.299~~ | DONE | `(t Time).DateTimeString() string` + `(t Time).IsoCompact() string`. "YYYY-MM-DD HH:MM:SS" + filename-safe "YYYYMMDDTHHMMSSZ". Test: `tests-internal/time_datetimestr_isocompact.volt` (15 cases). |
| ~~STDLIB.300~~ | DONE | `(t Time).HumanDate() string` + `(t Time).ShortDate() string`. "Saturday, June 15, 2024" + "Jun 15, 2024" calendar formatters. **Milestone: STDLIB.300 / 450 buildable tests.** Test: `tests-internal/time_humandate_shortdate.volt` (15 cases). |
| ~~STDLIB.301~~ | DONE | `slices.WindowsInt(s, k) [][]int` + `slices.WindowsString(s, k) [][]string`. Sliding-window enumeration (Chunks overlap step=1). Test: `tests-internal/slices_windows.volt` (23 cases). |
| ~~STDLIB.302~~ | DONE | `slices.IndicesOfInt(s, v) []int` + `slices.IndicesOfString(s, v) []int`. All-positions lookup (companion to ContainsInt / IndexInt). Test: `tests-internal/slices_indicesof.volt` (18 cases). |
| ~~STDLIB.303~~ | DONE | `strings.IndicesOf(s, sub) []int` + `bytes.IndicesOf(s, sub []byte) []int`. Substring-position enumeration (non-overlapping). Test: `tests-internal/strings_bytes_indicesof.volt` (22 cases). |
| ~~STDLIB.304~~ | DONE | `slices.TopNInt(s, n) []int` + `slices.BottomNInt(s, n) []int`. Top/bottom-N extraction (selection-style O(n·k)). Test: `tests-internal/slices_topn_bottomn.volt` (21 cases). |
| ~~STDLIB.305~~ | DONE | `slices.TopNString(s, n) []string` + `slices.BottomNString(s, n) []string`. `[]string` mirror, byte-wise lex order. Test: `tests-internal/slices_topn_bottomn_string.volt` (19 cases). |
| ~~STDLIB.306~~ | DONE | `(t Time).RoundToMinute() Time` + `(t Time).RoundToHour() Time`. Round() convenience wrappers for clock-aligned bucketing. Test: `tests-internal/time_roundtohour_minute.volt` (16 cases). |
| ~~STDLIB.307~~ | DONE | `(t Time).TruncateToMinute() Time` + `(t Time).TruncateToHour() Time`. Floor-bucketing — counterpart to RoundTo* (never rounds up). Test: `tests-internal/time_truncatetohour_minute.volt` (16 cases). |
| ~~STDLIB.308~~ | DONE | `(t Time).RoundToSecond() Time` + `(t Time).TruncateToSecond() Time`. Sub-second precision stripping. Test: `tests-internal/time_round_truncate_second.volt` (12 cases). |
| ~~STDLIB.309~~ | DONE | `filepath.NormalizeSlashes(path) string` + `filepath.IsRoot(path) bool`. Slash-run collapsing + root predicate (lighter than Clean). Test: `tests-internal/filepath_normalize_isroot.volt` (20 cases). |
| ~~STDLIB.310~~ | DONE | `filepath.Depth(path) int` + `filepath.IsParent(parent, child) bool`. Component-count + ancestor-containment with proper boundary handling. **Milestone: Pass 500.** Test: `tests-internal/filepath_depth_isparent.volt` (24 cases). |
| ~~STDLIB.311~~ | DONE | `slices.PairwiseSumInt(s) []int` + `slices.PairwiseMaxInt(s) []int`. k=2 sliding aggregates (adjacent sum / max). Test: `tests-internal/slices_pairwise_sum_max.volt` (23 cases). |
| ~~STDLIB.312~~ | DONE | `strings.CutLast(s, sep) (string, string, bool)` + `bytes.CutLast(s, sep) ([]byte, []byte, bool)`. Right-anchored Cut — splits at LAST sep. Test: `tests-internal/strings_bytes_cutlast.volt` (21 cases). |
| ~~STDLIB.313~~ | DONE | `slices.PairwiseMinInt(s) []int` + `slices.PairwiseAvgInt(s) []int`. Completes the Pairwise* family (Sum/Max/Min/Avg). Avg uses overflow-safe midpoint. Test: `tests-internal/slices_pairwise_min_avg.volt` (24 cases). |
| ~~STDLIB.314~~ | DONE | `strings.IndicesOfByte(s, c byte) []int` + `bytes.IndicesOfByte(s, c byte) []int`. Single-byte position enumeration. Test: `tests-internal/strings_bytes_indicesofbyte.volt` (18 cases). |
| ~~STDLIB.315~~ | DONE | `(t Time).WithTime(h, m, s) Time` + `(t Time).WithDate(y, mo, d) Time`. Reconstruct-with-half helpers — swap one half, keep the other. Test: `tests-internal/time_withtime_withdate.volt` (24 cases). |
| ~~STDLIB.316~~ | DONE | `strings.OnlyHex(s) string` + `bytes.OnlyHex(s []byte) []byte`. Hex-digit filter (preserves mixed case). Test: `tests-internal/strings_bytes_onlyhex.volt` (17 cases). |
| ~~STDLIB.317~~ | DONE | `(t Time).WithHour(h) Time` + `(t Time).WithYear(y) Time`. Single-field setters preserving everything else. Test: `tests-internal/time_withhour_withyear.volt` (24 cases). |
| ~~STDLIB.318~~ | DONE | `(t Time).WithMinute(m) Time` + `(t Time).WithSecond(s) Time`. Continues the single-field setter family. Test: `tests-internal/time_withminute_withsecond.volt` (22 cases). |
| ~~STDLIB.319~~ | DONE | `(t Time).WithMonth(mo) Time` + `(t Time).WithDay(d) Time`. Completes the per-component setter family. Inherits AddDate no-clamp overflow semantics. Test: `tests-internal/time_withmonth_withday.volt` (24 cases). |
| ~~STDLIB.320~~ | DONE | `slices.SplitByInt(s, sep) [][]int` + `slices.SplitByString(s, sep) [][]string`. Split slice into runs around sentinel (strings.Split semantics for slices). Test: `tests-internal/slices_splitby.volt` (25 cases). |
| ~~STDLIB.321~~ | DONE | `slices.JoinByInt(parts, sep) []int` + `slices.JoinByString(parts, sep) []string`. Inverse of SplitBy* — roundtrip identity holds. Test: `tests-internal/slices_joinby.volt` (23 cases). |
| ~~STDLIB.322~~ | DONE | `slices.TakeWhileInt(s, pred) []int` + `slices.DropWhileInt(s, pred) []int`. Classic functional prefix/suffix predicates; partition identity holds. Test: `tests-internal/slices_takewhile_dropwhile.volt` (18 cases). |
| ~~STDLIB.323~~ | DONE | `slices.TakeWhileString(s, pred) []string` + `slices.DropWhileString(s, pred) []string`. `[]string` mirror of Pass-322. Test: `tests-internal/slices_takewhile_dropwhile_string.volt` (16 cases). |
| ~~STDLIB.324~~ | DONE | `slices.LongestCommonPrefixInt(a, b) int` + `slices.LongestCommonSuffixInt(a, b) int`. Length of shared prefix/suffix between two int slices. Test: `tests-internal/slices_lcp_lcs_int.volt` (17 cases). |
| ~~STDLIB.325~~ | DONE | `slices.LongestCommonPrefixString(a, b) int` + `slices.LongestCommonSuffixString(a, b) int`. `[]string` mirror of Pass-324. Test: `tests-internal/slices_lcp_lcs_string.volt` (13 cases). |
| ~~STDLIB.326~~ | DONE | `bytes.CommonPrefix(a, b []byte) []byte` + `bytes.CommonSuffix(a, b []byte) []byte`. `[]byte` analog of strings.CommonPrefix/Suffix. Test: `tests-internal/bytes_common_prefix_suffix.volt` (19 cases). |
| ~~STDLIB.327~~ | DONE | `strings.CommonPrefixAll(strs) string` + `strings.CommonSuffixAll(strs) string`. n-ary fold over CommonPrefix/Suffix. Test: `tests-internal/strings_common_prefix_suffix_all.volt` (14 cases). |
| ~~STDLIB.328~~ | DONE | `strings.RemoveAll(s, subs []string) string` + `strings.ReplaceMany(s, pairs []string) string`. Batch text-cleaning — bulk-remove substrings + flat-paired old/new replacements. Test: `tests-internal/strings_removeall_replacemany.volt` (13 cases). |
| ~~STDLIB.329~~ | DONE | `strings.AbbreviateLeft(s, maxBytes, ellipsis) string`. Left-truncation counterpart to Truncate — prepend ellipsis, keep rightmost. Test: `tests-internal/strings_abbreviate_left.volt` (11 cases). |
| ~~STDLIB.330~~ | DONE | `bytes.Truncate(s, maxBytes, ellipsis) []byte` + `bytes.AbbreviateLeft(s, maxBytes, ellipsis) []byte`. `[]byte` analogs of strings.Truncate / strings.AbbreviateLeft. Test: `tests-internal/bytes_truncate_abbreviateleft.volt` (21 cases). |
| ~~STDLIB.331~~ | DONE | `bytes.AbbreviateMiddle(s, maxBytes, ellipsis) []byte`. `[]byte` analog of strings.AbbreviateMiddle — closes out the bytes Truncate/AbbreviateLeft/AbbreviateMiddle triple. Test: `tests-internal/bytes_abbreviatemiddle.volt` (23 cases). |
| ~~STDLIB.332~~ | DONE | `strings.JoinNonEmpty(parts, sep) string` + `strings.JoinLast(parts, sep, lastSep) string`. Join variants — skip-empties + Oxford-comma-style enumeration. Test: `tests-internal/strings_join_nonempty_last.volt` (13 cases). |
| ~~STDLIB.333~~ | DONE | `(t Time).NextBusinessDay() Time` + `(t Time).PrevBusinessDay() Time`. Skip-weekends day navigation (Mon-Fri only). Test: `tests-internal/time_business_day.volt` (19 cases). |
| ~~STDLIB.334~~ | DONE | `(t Time).AddBusinessDays(n) Time` + `(t Time).BusinessDaysBetween(u) int`. Business-day arithmetic — advance N weekdays / count weekdays between. Test: `tests-internal/time_add_businessdays.volt` (16 cases). |
| ~~STDLIB.335~~ | DONE | `strings.JoinInts(s []int, sep) string` + `strings.JoinIntsBracket(s, sep, open, close) string`. Int-slice → delimited string + bracket-wrapped variant. Test: `tests-internal/strings_join_ints.volt` (12 cases). |
| ~~STDLIB.336~~ | DONE | `strings.SplitToInts(s, sep) ([]int, error)` + `strings.SplitToIntsLossy(s, sep) []int`. Inverse of JoinInts — strict-or-lossy parsing of delimited int lists. Test: `tests-internal/strings_splittoints.volt` (21 cases). |
| ~~STDLIB.337~~ | DONE | `strings.LongestLine(s) int` + `strings.ShortestLine(s) int`. Per-line byte-length statistics. Test: `tests-internal/strings_longest_shortest_line.volt` (15 cases). |
| ~~STDLIB.338~~ | DONE | `(t Time).IsAM() bool` + `(t Time).IsPM() bool`. Quick AM/PM predicates (noon → PM boundary). Test: `tests-internal/time_isam_ispm.volt` (17 cases). |
| ~~STDLIB.339~~ | DONE | `bytes.LongestLine(s) int` + `bytes.ShortestLine(s) int`. `[]byte` analog of strings.LongestLine/ShortestLine. Test: `tests-internal/bytes_longest_shortest_line.volt` (14 cases). |
| ~~STDLIB.340~~ | DONE | `strings.HtmlEscape(s) string` + `strings.HtmlUnescape(s) string`. HTML entity encode/decode for the 5 metachars + `&apos;` + `&#34;`. Test: `tests-internal/strings_htmlescape.volt` (22 cases). |
| ~~STDLIB.341~~ | DONE | `strings.UrlEscape(s) string` + `strings.UrlUnescape(s) string`. Lowercase-hex RFC 3986 percent-encoding pair (distinct from URLEncode/URLDecode which use uppercase + error return). Bad-hex-tolerant unescape. Test: `tests-internal/strings_url_escape.volt` (20 cases). |
| ~~STDLIB.342~~ | DONE | `strings.CsvEscape(s) string` + `strings.CsvUnescape(s) string`. RFC 4180 CSV cell encoding (quotes only when needed, internal-quote doubling). Test: `tests-internal/strings_csv_escape.volt` (18 cases). |
| ~~STDLIB.343~~ | DONE | `strings.CsvJoinRow(cells) string` + `strings.CsvSplitRow(row) []string`. Row-level RFC 4180 CSV codec. Test: `tests-internal/strings_csv_row.volt` (24 cases). |
| ~~STDLIB.344~~ | DONE | `strings.JsonEscape(s) string` + `strings.JsonQuote(s) string`. JSON-string-literal encoding (mandatory escapes + `\u00HH` for low controls). Test: `tests-internal/strings_json_escape.volt` (20 cases). |
| ~~STDLIB.345~~ | DONE | `strings.JsonUnescape(s) string`. Inverse of JsonEscape — decodes mandatory backslash escapes plus `\uHHHH` Unicode (1-3 byte UTF-8 up to U+FFFF; bad/truncated escapes pass through literally). Roundtrip identity with JsonEscape. Test: `tests-internal/strings_json_unescape.volt` (23 cases). |
| ~~STDLIB.346~~ | DONE | `strings.ExpandTabs(s, tabSize) string`. Tab → space alignment to next tab-stop multiple; column resets on `\n` / `\r`; `tabSize <= 0` passes through. Test: `tests-internal/strings_expandtabs.volt` (19 cases). |
| ~~STDLIB.347~~ | DONE | `strings.StripAnsi(s) string`. Removes ANSI escape sequences (CSI ends at 0x40..0x7E final byte; other ESC drops ESC+next; lone trailing ESC dropped). Preserves newlines / tabs / other controls. Test: `tests-internal/strings_stripansi.volt` (17 cases). |
| ~~STDLIB.348~~ | DONE | `filepath.CommonPath(paths []string) string`. Longest component-wise common path prefix; rooted-ness must match; all-rooted-no-common → "/". Test: `tests-internal/filepath_commonpath.volt` (12 cases). |
| ~~STDLIB.349~~ | DONE | `maps.SumValuesStringInt(m)` + `MaxValueStringInt(m)` + `MinValueStringInt(m)`. Value-aggregate helpers for `map[string]int`; companions to existing KeyOfMax/Min. Empty map → 0 for all three. Test: `tests-internal/maps_value_aggregates.volt` (18 cases). |
| ~~STDLIB.350~~ | DONE | `strconv.AtoiOr(s, defaultValue) int` + `ParseBoolOr(s, defaultValue) bool`. Default-fallback parse helpers — invalid input returns defaultValue. Test: `tests-internal/strconv_or_defaults.volt` (23 cases). 500-test milestone. |
| ~~STDLIB.351~~ | DONE | `strings.LastIndexAny(s, chars) int` + `bytes.LastIndexAny(s, chars) int`. Right-to-left counterpart of IndexAny. Test: `tests-internal/strings_lastindexany.volt` (17 cases). |
| ~~STDLIB.352~~ | DONE | `os.GetenvOr(name, defaultValue) string`. Env-var read with default for unset / empty. Completes "Or"-fallback config trio with strconv.AtoiOr/ParseBoolOr. Test: `tests-internal/os_getenvor.volt` (6 cases). |
| ~~STDLIB.353~~ | DONE | `crypto/rand.String(n, alphabet) string` + `AlphanumString(n) string`. Random token helpers (mild ~0.4% modulo bias documented; not for crypto-grade ballot selection). Test: `tests-internal/rand_string.volt` (14 cases). |
| ~~STDLIB.354~~ | DONE | `unicode.DigitValue(b) int` + `HexDigitValue(b) int`. Byte → 0..9 / 0..15 value extractors; -1 on miss. Test: `tests-internal/unicode_digit_value.volt` (24 cases). |
| ~~STDLIB.355~~ | DONE | `strings.LastIndexByte(s, c) int`. Right-to-left byte scan; mirror of bytes.LastIndexByte. Test: `tests-internal/strings_lastindexbyte.volt` (15 cases). |
| ~~STDLIB.356~~ | DONE | `io.Copy(dst Writer, src Reader) (int, error)`. First non-interface helper in io; src.Read → dst.Write with error plumbing. Test: `tests-internal/io_copy.volt` (8 cases). |
| ~~STDLIB.357~~ | DONE | `filepath.JoinAll(parts []string) string`. N-way path join (fills gap left by 2-arg Join); preserves leading-slash rootedness, collapses inter-element double-slashes, skips all-slash/empty mid-elements. Test: `tests-internal/filepath_joinall.volt` (16 cases). |
| ~~STDLIB.358~~ | DONE | `bytes.Lines(s []byte) [][]byte`. Byte-slice mirror of strings.Lines (LF/CRLF aware, no trailing empty entry on `\n` end). Test: `tests-internal/bytes_lines.volt` (24 cases). |
| ~~STDLIB.359~~ | DONE | `strings.IndexAfter(s, sub, from int) int`. Offset-aware substring search; returns absolute index ≥ from, or -1. Test: `tests-internal/strings_indexafter.volt` (22 cases). |
| ~~STDLIB.360~~ | DONE | `bytes.IndexAfter(s, sub, from int) int`. Byte-slice mirror of strings.IndexAfter. Test: `tests-internal/bytes_indexafter.volt` (17 cases). |
| ~~STDLIB.361~~ | DONE | `time.UnixSec() int` + `time.MonoSec() int`. Seconds-precision clock readouts; complete the {Sec, Ms, Us} precision trio. Test: `tests-internal/time_unix_sec_mono_sec.volt` (13 cases). |
| ~~STDLIB.362~~ | DONE | `slices.LastIndexInt(s, v) int` + `slices.LastIndexString(s, v) int`. Right-to-left value-based search; counterparts to IndexInt/IndexString. Test: `tests-internal/slices_lastindex.volt` (16 cases). |
| ~~STDLIB.363~~ | DONE | `strings.FieldsFunc(s, fn fun(byte) bool) []string`. Predicate-driven split (separator runs collapse, leading/trailing trimmed). Test: `tests-internal/strings_fieldsfunc.volt` (24 cases). |
| ~~STDLIB.364~~ | DONE | `bytes.FieldsFunc(s, fn fun(byte) bool) [][]byte`. Byte-slice mirror of strings.FieldsFunc. Test: `tests-internal/bytes_fieldsfunc.volt` (20 cases). |
| ~~STDLIB.365~~ | DONE | `bufio.Buffer.Len() int` + `Reset()` + `String() string`. Stateful-buffer inspection + abandon. Test: `tests-internal/bufio_buffer_ops.volt` (20 cases). |
| ~~STDLIB.366~~ | DONE | `bytes.Builder.WriteRepeat(c, count)`. Padding/fill primitive with single up-front grow. Test: `tests-internal/bytes_builder_writerepeat.volt` (15 cases). |
| ~~STDLIB.367~~ | DONE | `strings.Builder.WriteRepeat(c, count)`. Padding/fill primitive on strings.Builder; parity with bytes.Builder. Test: `tests-internal/strings_builder_writerepeat.volt` (14 cases). |
| ~~STDLIB.368~~ | DONE | `Time.IsFuture()` + `Time.IsPast()`. Now-relative strict comparison predicates. Test: `tests-internal/time_isfuture_ispast.volt` (11 cases). |
| ~~STDLIB.369~~ | DONE | `strings.FirstNonEmpty(parts []string) string`. Config-fallback chain "||" idiom; "" if all empty. Test: `tests-internal/strings_firstnonempty.volt` (10 cases). |
| ~~STDLIB.370~~ | DONE | `slices.FirstNonZeroInt(s []int) int`. Int counterpart to FirstNonEmpty; 0 if all zero. Test: `tests-internal/slices_firstnonzero_int.volt` (10 cases). |
| ~~STDLIB.371~~ | DONE | `os.MkdirAll(path, mode) int`. Recursive parent-dir creation (`mkdir -p`); skips existing prefixes; EEXIST absorbed mid-walk. Test: `tests-internal/os_mkdirall.volt` (16 cases). |
| ~~STDLIB.372~~ | DONE | `slices.NoneInt(s, pred)` + `slices.NoneString(s, pred)`. Completes Any/All/None predicate trio. Test: `tests-internal/slices_none.volt` (13 cases). |
| ~~STDLIB.373~~ | DONE | `time.ParseDate(s string) (Time, error)`. Date-only `YYYY-MM-DD` parser; midnight UTC. Round-trips with DateString. Test: `tests-internal/time_parsedate.volt` (22 cases). |
| ~~STDLIB.374~~ | DONE | `os.AppendFile(name, data, mode) error`. Append-or-create; counterpart to WriteFile. Test: `tests-internal/os_appendfile.volt` (17 cases). |
| ~~STDLIB.375~~ | DONE | `slices.FindLastInt(s, pred)` + `slices.FindLastString(s, pred)`. Right-to-left value+found search. Test: `tests-internal/slices_findlast.volt` (14 cases). |
| ~~STDLIB.376~~ | DONE | `maps.AnyStringInt(m, pred)` + `AllStringInt(m, pred)` + `CountStringInt(m, pred)`. Predicate-driven traversal trio for map[string]int. Test: `tests-internal/maps_any_all_count.volt` (14 cases). |
| ~~STDLIB.377~~ | DONE | `maps.AnyStringString(m, pred)` + `AllStringString(m, pred)` + `CountStringString(m, pred)`. String-valued counterpart. Test: `tests-internal/maps_any_all_count_string.volt` (12 cases). |
| ~~STDLIB.378~~ | DONE | `encoding/hex.EncodeToStringUpper(src) string` + `EncodedLen(n) int` + `DecodedLen(n) int`. Uppercase variant + buffer-sizing helpers. Test: `tests-internal/hex_extra.volt` (19 cases). |
| ~~STDLIB.379~~ | DONE | `encoding/base64.EncodeToStringURL(src) string` + `DecodeStringURL(src) (string, error)`. URL-safe RFC 4648 §5; decoder re-pads missing `=` for raw-URL inputs. Test: `tests-internal/base64_url.volt` (18 cases). |
| ~~STDLIB.380~~ | DONE | `time.Today() Time`. Midnight UTC on the current civil date. Test: `tests-internal/time_today.volt` (9 cases). |
| ~~STDLIB.381~~ | DONE | `filepath.HasExt(path, ext) bool`. Filter-friendly extension predicate; leading-dot optional, case-sensitive. Test: `tests-internal/filepath_hasext.volt` (18 cases). |
| ~~STDLIB.382~~ | DONE | `time.Yesterday() Time` + `time.Tomorrow() Time`. Free-function shorthands for `Today().Yesterday()` / `Today().Tomorrow()`. Test: `tests-internal/time_free_yesterday_tomorrow.volt` (12 cases). |
| ~~STDLIB.383~~ | DONE | `bytes.IsBlank(s []byte) bool`. Byte-slice mirror of strings.IsBlank. Test: `tests-internal/bytes_isblank.volt` (13 cases). |
| ~~STDLIB.384~~ | DONE | `sort.IntsAreSortedBy(s, less)` + `sort.StringsAreSortedBy(s, less)`. Predicate-comparator sortedness check. Test: `tests-internal/sort_aresortedby.volt` (14 cases). |
| ~~STDLIB.385~~ | DONE | `strings.TrimNewline(s) string`. Trailing single-newline strip (CRLF / LF / CR priority); chomp-style. Test: `tests-internal/strings_trimnewline.volt` (16 cases). |
| ~~STDLIB.386~~ | DONE | `bytes.TrimNewline(s []byte) []byte`. Byte-slice mirror of strings.TrimNewline. Test: `tests-internal/bytes_trimnewline.volt` (18 cases). |
| ~~STDLIB.387~~ | DONE | `strings.Slice(s, lo, hi) string`. Byte-index substring with bounds clamping. Test: `tests-internal/strings_slice.volt` (19 cases). |
| ~~STDLIB.388~~ | DONE | `bytes.Slice(s []byte, lo, hi int) []byte`. Byte-slice mirror of strings.Slice. Test: `tests-internal/bytes_slice.volt` (20 cases). |
| ~~STDLIB.389~~ | DONE | `math.MinInt3(a, b, c) int` + `math.MaxInt3(a, b, c) int`. Three-argument min/max. Test: `tests-internal/math_min_max_int3.volt` (16 cases). |
| ~~STDLIB.390~~ | DONE | `strings.IsUpper(s)` + `strings.IsLower(s)`. Case-classification predicates (Python-style; non-letter neutral; ≥1 letter required). Test: `tests-internal/strings_isupper_islower.volt` (28 cases). |
| ~~STDLIB.391~~ | DONE | `slices.HistogramInt(s []int) map[string]int`. Int counterpart to HistogramString; keys are Itoa'd. Test: `tests-internal/slices_histogram_int.volt` (18 cases). |
| ~~STDLIB.392~~ | DONE | `time.MinTime(a, b)` + `time.MaxTime(a, b)`. Earlier/later picker; left-bias on equality. Test: `tests-internal/time_min_max.volt` (13 cases). |
| ~~STDLIB.393~~ | DONE | `strings.LineCol(s, byteIdx) (int, int)`. Byte-index → 1-based (line, col); canonical lexer-error position helper. Test: `tests-internal/strings_linecol.volt` (25 cases). |
| ~~STDLIB.394~~ | DONE | `strings.LineSpan(s, byteIdx) (int, int)`. Byte-index → enclosing-line [lo, hi); excerpt-the-offending-line helper. Test: `tests-internal/strings_linespan.volt` (25 cases). |
| ~~STDLIB.395~~ | DONE | `unicode.SwapCase(b) byte`. Per-byte case flip; involution. Test: `tests-internal/unicode_swapcase.volt` (16 cases). |
| ~~STDLIB.396~~ | DONE | `time.FromUnixSec(sec)` + `FromUnixMs(ms)` + `FromUnixUs(us)`. Precision-explicit constructors; inverse of UnixSec/UnixMs/UnixUs trio. Test: `tests-internal/time_from_unix_x.volt` (20 cases). |
| ~~STDLIB.397~~ | DONE | `strconv.ParseIntOr(s, base, defaultValue) int`. Base-aware ParseInt with default fallback. Test: `tests-internal/strconv_parseintor.volt` (19 cases). |
| ~~STDLIB.398~~ | DONE | `slices.FirstInt` + `LastInt` + `FirstString` + `LastString`. Safe-access (element, ok) accessors. Test: `tests-internal/slices_first_last.volt` (20 cases). |
| ~~STDLIB.399~~ | DONE | `net.ParseIPv4(s) (int, error)` + `net.IPv4String(ip) string`. Public IPv4 ↔ host-order-int codec. Test: `tests-internal/net_parse_ipv4.volt` (20 cases). |
| ~~STDLIB.400~~ | DONE | `slices.FlatMapInt(s, fn) []int` + `slices.FlatMapString(s, fn) []string`. "Expand and concatenate" combinator. Test: `tests-internal/slices_flatmap.volt` (21 cases). 🎯 STDLIB.400 milestone. |
| ~~STDLIB.401~~ | DONE | `slices.ChunkByInt(s, key)` + `slices.ChunkByString(s, key)`. Group consecutive elements sharing `key(x)`. Test: `tests-internal/slices_chunkby.volt` (29 cases). |
| ~~STDLIB.402~~ | DONE | `time.ParseDuration(s) (int, error)`. Single-unit parser (ns/us/ms/s/m/h); inverse of FormatDuration single-unit forms. Test: `tests-internal/time_parseduration.volt` (26 cases). |
| ~~STDLIB.403~~ | DONE | `slices.ZipWithInt(a, b, fn) []int`. Pairwise combine; generalizes Add/Sub/Mul to any fn(int,int)→int. Test: `tests-internal/slices_zipwith_int.volt` (21 cases). |
| ~~STDLIB.404~~ | DONE | `slices.ZipWithString(a, b, fn) []string`. String-slice counterpart; "format each pair" idiom. Test: `tests-internal/slices_zipwith_string.volt` (16 cases). |
| ~~STDLIB.405~~ | DONE | `bytes.IsNumeric` + `bytes.IsAlpha` + `bytes.IsAlphanumeric`. Byte-slice mirrors of strings.Is* trio. Test: `tests-internal/bytes_classifiers.volt` (16 cases). |
| ~~STDLIB.406~~ | DONE | `time.SinceTime(t) int` + `time.UntilTime(t) int`. Time-value-taking ergonomic free funcs (Now-relative). Test: `tests-internal/time_since_until_time.volt` (12 cases). |
| ~~STDLIB.407~~ | DONE | `os.UserCacheDir() (string, error)`. Completes XDG-user-dirs trio with UserHomeDir/UserConfigDir. Test: `tests-internal/os_usercachedir.volt` (6 cases). |
| ~~STDLIB.408~~ | DONE | `slices.HasPrefixInts` + `HasSuffixInts` + `HasPrefixStrings` + `HasSuffixStrings`. Sequence-level prefix/suffix matching. Test: `tests-internal/slices_haspresuffix.volt` (18 cases). |
| ~~STDLIB.409~~ | DONE | `strconv.FormatHex(n)` + `FormatBinary(n)` + `FormatOctal(n)`. Intent-revealing aliases for FormatInt with fixed base. Test: `tests-internal/strconv_format_base.volt` (21 cases). |
| ~~STDLIB.410~~ | DONE | `Time.UnixMilli()` + `Time.UnixMicro()`. Per-Time-method ms/us readouts; completes Sec/Ms/Us/Ns quartet. Test: `tests-internal/time_unix_milli_micro.volt` (16 cases). 🎯 Pass 600 milestone. |
| ~~STDLIB.411~~ | DONE | `strings.SortChars(s) string`. Counting sort of bytes; anagram-key generator. Test: `tests-internal/strings_sortchars.volt` (20 cases). |
| ~~STDLIB.412~~ | DONE | `bytes.SortBytes(s []byte) []byte`. Byte-slice mirror of strings.SortChars. Test: `tests-internal/bytes_sortbytes.volt` (24 cases). |
| ~~STDLIB.413~~ | DONE | `strings.ReverseWords(s) string`. Word-level token reversal (Fields-split, single-space-rejoin). Test: `tests-internal/strings_reversewords.volt` (12 cases). |
| ~~STDLIB.414~~ | DONE | `strings.IndexByteAfter(s, c, from) int`. Single-byte offset-aware search. Test: `tests-internal/strings_indexbyteafter.volt` (18 cases). |
| ~~STDLIB.415~~ | DONE | `bytes.IndexByteAfter(s, c, from) int`. Byte-slice mirror of strings.IndexByteAfter. Test: `tests-internal/bytes_indexbyteafter.volt` (14 cases). |
| ~~STDLIB.416~~ | DONE | `slices.MinMaxByString(s, key) (string, string)`. String counterpart to MinMaxByInt. Test: `tests-internal/slices_minmax_bystring.volt` (12 cases). |
| ~~STDLIB.417~~ | DONE | `slices.TabulateInt(n, fn)` + `slices.TabulateString(n, fn)`. Sequence generator: `[fn(0)..fn(n-1)]`. Test: `tests-internal/slices_tabulate.volt` (21 cases). |
| ~~STDLIB.418~~ | DONE | `slices.InterleaveInts(a, b)` + `slices.InterleaveStrings(a, b)`. Two-slice weaver; unequal-length appends tail. Test: `tests-internal/slices_interleave.volt` (36 cases). |
| ~~STDLIB.419~~ | DONE | `time.MonthAbbrev(month) string`. 3-letter English month abbreviation; "" out of range. Test: `tests-internal/time_month_abbrev.volt` (18 cases). |
| ~~STDLIB.420~~ | DONE | `time.WeekdayName(w)` + `time.WeekdayAbbrev(w)`. Free-function weekday lookups (0=Sun..6=Sat). Test: `tests-internal/time_weekday_name_abbrev.volt` (20 cases). |
| ~~STDLIB.421~~ | DONE | `filepath.Stem(path) string`. Basename minus extension; Python's Path.stem. Test: `tests-internal/filepath_stem.volt` (14 cases). |
| ~~STDLIB.422~~ | DONE | `strings.Builder.WriteInt(n)`. Strings-side counterpart to bytes.Builder.WriteInt. Test: `tests-internal/strings_builder_writeint.volt` (11 cases). |
| ~~STDLIB.423~~ | DONE | `bytes.Builder.WriteBool(v)` + `strings.Builder.WriteBool(v)`. Boolean-text accumulator on both Builders. Test: `tests-internal/builder_writebool.volt` (10 cases). |
| ~~STDLIB.424~~ | DONE | `slices.IndicesWhereInt(s, pred)` + `slices.IndicesWhereString(s, pred)`. Predicate-based variant of IndicesOf. Test: `tests-internal/slices_indices_where.volt` (21 cases). |
| ~~STDLIB.425~~ | DONE | `strings.OnlyAscii(s) string`. Strip bytes >= 128. Test: `tests-internal/strings_onlyascii.volt` (13 cases). |
| ~~STDLIB.426~~ | DONE | `bytes.OnlyAscii(s []byte) []byte`. Byte-slice mirror of strings.OnlyAscii. Test: `tests-internal/bytes_onlyascii.volt` (16 cases). |
| ~~STDLIB.427~~ | DONE | `slices.AtOrInt(s, i, default)` + `slices.AtOrString(s, i, default)`. Bounds-checked access with default. Test: `tests-internal/slices_ator.volt` (18 cases). |
| ~~STDLIB.428~~ | DONE | `bufio.Buffer.WriteByte` + `WriteInt` + `WriteBool`. Builder-style methods on bufio.Buffer. Test: `tests-internal/bufio_buffer_writers.volt` (13 cases). |
| ~~STDLIB.429~~ | DONE | `time.UTCNow() Time`. Convenience over FromNano(Now()). Test: `tests-internal/time_utcnow.volt` (11 cases). |
| ~~STDLIB.430~~ | DONE | `strings.Bracket(s, open, close)` + `strings.Surround(s, wrap)`. Display-format wrappers; no escape processing. Test: `tests-internal/strings_bracket_surround.volt` (16 cases). |
| ~~STDLIB.431~~ | DONE | `hash/fnv.HashInt32(n) int` + `HashInt64(n) int`. FNV-1a hash of an int's 8 LE bytes. Test: `tests-internal/fnv_hashint.volt` (15 cases). |
| ~~STDLIB.432~~ | DONE | `slices.DropLastInts(s, n)` + `slices.DropLastStrings(s, n)`. Tail-trim counterpart to DropInts. Test: `tests-internal/slices_droplast.volt` (17 cases). |
| ~~STDLIB.433~~ | DONE | `strings.ToBytes(s) []byte`. Inverse of bytes.String; fresh defensive copy. Test: `tests-internal/strings_tobytes.volt` (13 cases). |
| ~~STDLIB.434~~ | DONE | `strings.RepeatTo(s, totalLen) string`. Pattern-fill with truncation; for bars / hatches / banners. Test: `tests-internal/strings_repeatto.volt` (16 cases). |
| ~~STDLIB.435~~ | DONE | `crypto/rand.IntRange(lo, hi) int` + `crypto/rand.Bool() bool`. Range and bool conveniences over Int. Test: `tests-internal/rand_intrange_bool.volt` (10 cases). |
| ~~STDLIB.436~~ | DONE | `time.EarliestOf(times)` + `time.LatestOf(times)`. Single-pass min/max over a []Time. Test: `tests-internal/time_earliest_latest_of.volt` (14 cases). |
| ~~STDLIB.437~~ | DONE | `slices.FilterMapInt(s, keep, mapper)` + `slices.FilterMapString(s, keep, mapper)`. Filter+transform combinator (split predicate/mapper due to no multi-return fn-type values in volt). Test: `tests-internal/slices_filtermap.volt` (15 cases). |
| ~~STDLIB.438~~ | DONE | `slices.CountUniqueInt(s)` + `slices.CountUniqueString(s)`. Cardinality count via map-based dedup; O(n). Test: `tests-internal/slices_countunique.volt` (12 cases). |
| ~~STDLIB.439~~ | DONE | `strings.LongestRun(s, c) int`. Longest run of byte c; single linear pass. Test: `tests-internal/strings_longestrun.volt` (14 cases). |
| ~~STDLIB.440~~ | DONE | `bytes.LongestRun(s []byte, c byte) int`. Byte-slice mirror of strings.LongestRun. Test: `tests-internal/bytes_longestrun.volt` (14 cases). |
| ~~STDLIB.441~~ | DONE | `slices.MinIndexByInt` + `MaxIndexByInt` + `MinIndexByString` + `MaxIndexByString`. Index-returning counterparts to MinBy/MaxBy. Test: `tests-internal/slices_index_by.volt` (15 cases). |
| ~~STDLIB.442~~ | DONE | `time.SleepMs(ms)` + `time.SleepSec(sec)`. Precision-explicit Sleep wrappers; non-positive no-op. Test: `tests-internal/time_sleep_ms_sec.volt` (8 cases). |
| ~~STDLIB.443~~ | DONE | `bytes.PadLeft(s, n, c)` + `bytes.PadRight(s, n, c)`. Byte-slice mirrors of strings.PadLeft/PadRight. Test: `tests-internal/bytes_pad.volt` (19 cases). |
| ~~STDLIB.444~~ | DONE | `bytes.Center(s, n, c)`. Symmetric padding (extra byte goes right on odd). Test: `tests-internal/bytes_center.volt` (24 cases). |
| ~~STDLIB.445~~ | DONE | `slices.GatherInt(s, indices)` + `slices.GatherString(s, indices)`. Index-based projection; out-of-bounds emits zero/"". Test: `tests-internal/slices_gather.volt` (25 cases). |
| ~~STDLIB.446~~ | DONE | `math.ManhattanDist(x1,y1,x2,y2)` + `math.ChebyshevDist(x1,y1,x2,y2)`. L1 & L∞ 2D-point distances. Test: `tests-internal/math_distance.volt` (17 cases). |
| ~~STDLIB.447~~ | DONE | `slices.SameMultisetInt(a, b)` + `slices.SameMultisetString(a, b)`. Order-independent value-count equality. Test: `tests-internal/slices_samemultiset.volt` (13 cases). |
| ~~STDLIB.448~~ | DONE | `slices.EqualFuncInt(a, b, eq)` + `slices.EqualFuncString(a, b, eq)`. Predicate-based positional equality (fuzzy / case-insens). Test: `tests-internal/slices_equalfunc.volt` (11 cases). |
| ~~STDLIB.449~~ | DONE | `strings.IsAsciiPrintable(s) bool`. Every byte in [0x20, 0x7E]; stricter than IsAscii. Test: `tests-internal/strings_isasciiprintable.volt` (17 cases). |
| ~~STDLIB.450~~ | DONE | `bytes.IsAsciiPrintable(s []byte) bool`. Byte-slice mirror of strings.IsAsciiPrintable. Test: `tests-internal/bytes_isasciiprintable.volt` (13 cases). 🎯 600-test milestone. |
| ~~STDLIB.451~~ | DONE | `strings.LongestRunFunc(s, pred)`. Predicate-driven longest-run length. Test: `tests-internal/strings_longestrunfunc.volt` (13 cases). |
| ~~STDLIB.452~~ | DONE | `bytes.LongestRunFunc(s, pred)`. Byte-slice mirror of strings.LongestRunFunc. Test: `tests-internal/bytes_longestrunfunc.volt` (10 cases). |
| ~~STDLIB.453~~ | DONE | `slices.ContainsAnyInt(s, candidates)` + `slices.ContainsAnyString(s, candidates)`. Set-intersection non-empty predicate. Test: `tests-internal/slices_containsany.volt` (11 cases). |
| ~~STDLIB.454~~ | DONE | `strings.CountAny(s, chars) int`. Total occurrence count of any byte in chars-set; O(n+m) via 256-byte lookup. Test: `tests-internal/strings_countany.volt` (13 cases). |
| ~~STDLIB.455~~ | DONE | `bytes.CountAny(s, chars) int`. Byte-slice mirror of strings.CountAny. Test: `tests-internal/bytes_countany.volt` (10 cases). |
| ~~STDLIB.456~~ | DONE | `time.TimeIsBetween(t, lo, hi) bool`. Inclusive-range Time membership; degenerate range false. Test: `tests-internal/time_isbetween.volt` (10 cases). |
| ~~STDLIB.457~~ | DONE | `math.InRange(v, lo, hi) bool`. Int counterpart of TimeIsBetween; predicate to ClampInt. Test: `tests-internal/math_inrange.volt` (18 cases). |
| ~~STDLIB.458~~ | DONE | `strings.HasOnly(s, chars) bool`. Whitelist validation: every byte of s ∈ chars-set. O(n+m) via 256-byte membership lookup. Empty s → true (vacuous); empty chars + non-empty s → false. Test: `tests-internal/strings_hasonly.volt` (16 cases). |
| ~~STDLIB.459~~ | DONE | `bytes.HasOnly(s, chars) bool`. Byte-slice mirror of strings.HasOnly. Test: `tests-internal/bytes_hasonly.volt` (13 cases incl. high-bit bytes). |
| ~~STDLIB.460~~ | DONE | `strings.HasNone(s, chars) bool`. Blacklist counterpart to HasOnly — true iff no byte of s is in chars. Empty s → true; empty chars → true (nothing forbidden). O(n+m) via 256-byte membership lookup. Test: `tests-internal/strings_hasnone.volt` (16 cases incl. ContainsAny complement). |
| ~~STDLIB.461~~ | DONE | `bytes.HasNone(s, chars) bool`. Byte-slice mirror of strings.HasNone. Test: `tests-internal/bytes_hasnone.volt` (13 cases incl. control-byte + high-bit cases). |
| ~~STDLIB.462~~ | DONE | `strings.KeepOnly(s, chars) string`. Whitelist filter — drop every byte not in chars. Empty chars → "". O(n+m) via 256-byte membership lookup. Cross-property: `HasOnly(KeepOnly(s, c), c)` always true. Test: `tests-internal/strings_keeponly.volt` (15 cases incl. digits-only + hex-only filters). |
| ~~STDLIB.463~~ | DONE | `bytes.KeepOnly(s, chars) []byte`. Byte-slice mirror of strings.KeepOnly. Test: `tests-internal/bytes_keeponly.volt` (11 cases incl. high-bit-byte + HasOnly cross-property). |
| ~~STDLIB.464~~ | DONE | `math.WrapInt(v, lo, hi) int`. Cyclic counterpart to ClampInt — wrap v into [lo,hi] via modular arithmetic. Built on FloorModInt so handles negative v cleanly. Useful for angles (0..359), day-of-week, circular buffer indices. Test: `tests-internal/math_wrap.volt` (30 cases incl. multi-cycle wraparound, negative range, InRange cross-property). |
| ~~STDLIB.465~~ | DONE | `math.RoundToMultiple(n, k) int`. Round n to nearest multiple of k (round-half-up). Distinct from AlignUp/AlignDown (always-up / always-down) — picks the closer surrounding multiple. Symmetric for negative n. k<=0 → unchanged. Test: `tests-internal/math_roundto.volt` (34 cases incl. snap-to-grid, audio sample-rate, tie-breaking, mod-k divisibility). |
| ~~STDLIB.466~~ | DONE | `maps.GetOrStringInt(m, key, default)` + `maps.GetOrStringString(m, key, default)`. Get-with-fallback; saves the ContainsKey-then-lookup dance. Present-but-zero/empty key returns the stored zero (not default). Test: `tests-internal/maps_getor.volt` (19 cases incl. config-style use case). |
| ~~STDLIB.467~~ | DONE | `strings.CountFunc(s, pred fun(byte) bool) int`. Predicate-driven byte-count counterpart to IndexFunc/LastIndexFunc/TrimFunc. Useful for ad-hoc tallies (digits, vowels, uppercase, etc.) without pre-building a chars-set. Test: `tests-internal/strings_countfunc.volt` (18 cases). |
| ~~STDLIB.468~~ | DONE | `bytes.CountByte(s, c byte) int` + `bytes.CountFunc(s, pred) int`. Byte-slice mirrors of strings.CountByte / CountFunc. Test: `tests-internal/bytes_countfunc.volt` (15 cases incl. control-byte + high-bit-byte scans). |
| ~~STDLIB.469~~ | DONE | `slices.PercentileInt(s, p) int`. Nearest-rank percentile (C=1 def; no interpolation, so the result is always an actual element of s). p<=0 → min; p>=100 → max; empty → 0. Doesn't mutate s. Test: `tests-internal/slices_percentile.volt` (26 cases incl. latency p50/p95/p99 + no-mutate verification). |
| ~~STDLIB.470~~ | DONE | `slices.IQRInt(s) int` + `slices.SpreadInt(s) int`. IQR = P75-P25 (robust spread; built on PercentileInt). Spread = max-min (simple O(n) dispersion). Both 0 for n<2. Test: `tests-internal/slices_spread_iqr.volt` (20 cases incl. outlier-robustness demo where IQR stays small while Spread balloons). |
| ~~STDLIB.471~~ | DONE | `time.AddDays(t, n) Time` + `time.AddHours(t, n) Time` + `time.DiffDays(t, u) int` + `time.DiffHours(t, u) int`. Top-level calendar arithmetic shortcuts (n=86400s / 3600s flat — DST/leap-sec not modeled). Diff*-funcs truncate toward zero; sign-preserving. Test: `tests-internal/time_add_diff.volt` (26 cases incl. month/year rollover + round-trip identity). |
| ~~STDLIB.472~~ | DONE | `strconv.FormatHexPad(n, width)` + `strconv.FormatBinaryPad(n, width)`. Zero-padded hex / binary with sign preservation (negative keeps '-' before padded digits). width <= len → unpadded. Useful for hex dumps, fixed-width addresses, MAC octets, bitmask debug. Test: `tests-internal/strconv_pad.volt` (24 cases). |
| ~~STDLIB.473~~ | DONE | `slices.DotProductInt(a, b) int`. Standard linear-algebra dot product: Σ a[i]*b[i] up to min(len(a),len(b)); longer tail ignored. Useful for weighted sums, geometric inner products, ML feature scoring. Test: `tests-internal/slices_dotproduct.volt` (12 cases incl. commutativity + orthogonality + Cauchy-Schwarz sanity). |
| ~~STDLIB.474~~ | DONE | `slices.SumAbsInt(s) int` + `slices.SumSquaresInt(s) int`. L1 norm (Σ\|x\|) and squared L2 norm (Σx²). Empty → 0. Useful for total-magnitude / movement-from-zero / signal-energy stats; SumSquares matches DotProduct(s,s) without needing a clone. Test: `tests-internal/slices_norms.volt` (21 cases incl. Pythagorean triple + triangle-inequality sanity + DotProduct equivalence). |
| ~~STDLIB.475~~ | DONE | `math.EuclideanSquaredDist(x1,y1,x2,y2)` + `math.EuclideanDist(x1,y1,x2,y2)`. Squared L2 stays in pure int (preserves ordering for comparisons); floored Euclidean uses IsqrtFloor. Completes the 2D distance trio with ManhattanDist + ChebyshevDist. Test: `tests-internal/math_euclidean.volt` (26 cases incl. (3,4,5)/(5,12,13)/(8,15,17) Pythagorean triples + metric cross-inequalities). |
| ~~STDLIB.476~~ | DONE | `slices.KthSmallestInt(s, k) int` + `slices.KthLargestInt(s, k) int`. 0-indexed selection: KthSmallest(s,0)==min, KthLargest(s,0)==max. Sorts a copy, doesn't mutate s. k out of range → 0. Test: `tests-internal/slices_kth.volt` (27 cases incl. silver-medal use case + dup handling + KthSmallest⇄KthLargest mirror property). |
| ~~STDLIB.477~~ | DONE | `slices.KthSmallestString(s, k) string` + `slices.KthLargestString(s, k) string`. String counterparts to KthSmallestInt/KthLargestInt under byte-wise lex order; k out of range → "". Test: `tests-internal/slices_kth_string.volt` (25 cases incl. "second-newest log file" use case + mirror property). |
| ~~STDLIB.478~~ | DONE | `slices.MedianString(s) string`. Lex-order median; even-n picks the LOWER middle (strings don't average). Empty → "". Doesn't mutate s. Test: `tests-internal/slices_median_string.volt` (16 cases incl. KthSmallest equivalence cross-property + "median username" audit use case). |
| ~~STDLIB.479~~ | DONE | `crypto/hmac.Equal(a, b string) bool`. Constant-time byte comparison: comparison loop does NOT short-circuit on mismatch, defeating timing-side-channel attacks. Different-length inputs return false. Drop-in for MAC-tag / CSRF-token / session-token verification — DO NOT use `==` for those. Test: `tests-internal/hmac_equal.volt` (21 cases incl. HMAC tag round-trip + high-bit-byte handling). |
| ~~STDLIB.480~~ | DONE | `strings.TrimSpaceEachLine(s) string`. Split by line, TrimSpace each, rejoin. Preserves blank lines as "" (not dropped). Trailing newline preserved iff input had one. Useful for cleaning indented heredocs, user-pasted blocks, log captures. Test: `tests-internal/strings_trimspace_each.volt` (15 cases incl. CRLF handling + idempotence + indent-strip + tabs-as-whitespace). |
| ~~STDLIB.481~~ | DONE | `strings.Left(s, n) string` + `strings.Right(s, n) string`. Take prefix / suffix (byte-counted). n<=0 → ""; n>=len → s. Use cases: file-extension extraction, log-line timestamp prefix, token masking (`Left(t,4)+"..."+Right(t,4)`). Test: `tests-internal/strings_left_right.volt` (23 cases). |
| ~~STDLIB.482~~ | DONE | `bytes.Left(s, n) []byte` + `bytes.Right(s, n) []byte`. Byte-slice mirrors of strings.Left/Right; return a fresh slice. Test: `tests-internal/bytes_left_right.volt` (15 cases incl. high-bit-byte preservation). |
| ~~STDLIB.483~~ | DONE | `slices.SplitAtInt(s, n) ([]int,[]int)` + `slices.SplitAtString(s, n) ([]string,[]string)`. Single-call (head, tail) split at index n; n clamped to [0, len(s)]. Doesn't mutate s. Test: `tests-internal/slices_split_at.volt` (31 cases incl. split-in-half use case + out-of-range clamping). |
| ~~STDLIB.484~~ | DONE | `slices.HasDuplicatesInt(s) bool` + `slices.HasDuplicatesString(s) bool`. Early-exit duplicate detector; faster than `len(Unique(s)) != len(s)` on slices that contain dups. O(n²). Empty / single-element → false. Test: `tests-internal/slices_has_dups.volt` (20 cases incl. Unique-equivalence cross-property + distinct-ID validation use case). |
| ~~STDLIB.485~~ | DONE | `slices.FirstDuplicateInt(s) (int, bool)` + `slices.FirstDuplicateString(s) (string, bool)`. Returns first duplicate value + found flag — surfaces the offending element for validation error messages ("duplicate user_id: 42"). Empty/distinct → (zero, false). Test: `tests-internal/slices_first_dup.volt` (26 cases incl. HasDuplicates cross-property). |
| ~~STDLIB.486~~ | DONE | `slices.NumDistinctInt(s) int` + `slices.NumDistinctString(s) int`. Count of distinct values; equivalent to `len(Unique(s))` but doesn't materialize the unique slice (alloc-free count). O(n²). Test: `tests-internal/slices_num_distinct.volt` (20 cases incl. Unique-len cross-property + HasDuplicates-equivalent check). |
| ~~STDLIB.487~~ | DONE | `strings.HeadLines(s, n) string` + `strings.TailLines(s, n) string`. First n / last n lines, rejoined with '\n' (CRLF normalized via Lines). n<=0 → ""; n >= line-count → all. Use cases: `head`-style preview, `tail`-style log inspection. Test: `tests-internal/strings_head_tail_lines.volt` (23 cases). |
| ~~STDLIB.488~~ | DONE | `strings.NumberLines(s) string` + `strings.ReverseLines(s) string`. NumberLines prefixes each line with 1-indexed N + tab; ReverseLines flips line order. Both CRLF-normalized via Lines. NumberLines uses a tiny inlined int→decimal helper (`intDecimalStr`) so the strings package keeps zero external deps. Use cases: code-listing pretty-print, error-context dump, log tail-first display. Test: `tests-internal/strings_number_reverse_lines.volt` (19 cases incl. multi-digit numbers + idempotent double-reverse). |
| ~~STDLIB.489~~ | DONE | `bytes.HeadLines(s, n) []byte` + `bytes.TailLines(s, n) []byte`. Byte-slice mirrors of strings.HeadLines/TailLines; return fresh slice; CRLF normalized via Lines. Test: `tests-internal/bytes_head_tail_lines.volt` (14 cases incl. high-bit-byte preservation). |
| ~~STDLIB.490~~ | DONE | `slices.AllPositiveInt(s) bool` + `slices.AllNegativeInt(s) bool` + `slices.AllNonNegativeInt(s) bool` + `slices.AllNonZeroInt(s) bool`. Sign-class predicates for input validation: "all amounts positive", "no zeros (divide-by-zero pre-flight)", "no negatives". Empty → true vacuously. Test: `tests-internal/slices_sign_checks.volt` (26 cases incl. divide-by-zero pre-flight + positive-amounts use cases). 🎯 640-test milestone. |
| ~~STDLIB.491~~ | DONE | `slices.CountPositiveInt(s)` + `CountNegativeInt(s)` + `CountZeroInt(s)` + `SumPositiveInt(s)` + `SumNegativeInt(s)`. Sign-stratified count + sum quintet — companions to the AllPositive/Negative predicates. Use cases: deposits/withdrawals split, sparsity stats. Cross-property: counts add to len(s); SumPositive+SumNegative == Sum. Test: `tests-internal/slices_sign_stats.volt` (26 cases). |
| ~~STDLIB.492~~ | DONE | `slices.MaxAbsInt(s) int` + `slices.MinAbsInt(s) int`. max\|x\| / min\|x\| over s; empty → 0. Use cases: peak-amplitude signal detection (MaxAbs), closest-to-zero pick (MinAbs), symmetric normalization. Test: `tests-internal/slices_abs_minmax.volt` (22 cases incl. no-mutation invariant). |
| ~~STDLIB.493~~ | DONE | `slices.ClampEachInt(s, lo, hi) []int`. Element-wise clamp via math.ClampInt semantics (degenerate hi<lo → every element = lo). Length preserved. Doesn't mutate. Use cases: color-channel saturation [0..255], audio dB limits, gauge clipping. Test: `tests-internal/slices_clamp_each.volt` (41 cases incl. math.InRange cross-property + color-channel use case). |
| ~~STDLIB.494~~ | DONE | `slices.AddVecInt(a, b)` + `SubVecInt(a, b)` + `MulVecInt(a, b)` — element-wise (Hadamard) vector arithmetic; output length = min(len(a),len(b)), longer tail dropped. Distinct from DotProductInt which is the inner product. Use cases: time-series superposition (Add), differencing (Sub), masking by 0/1 vector (Mul). Test: `tests-internal/slices_vec_arith.volt` (34 cases incl. Add-then-Sub identity round-trip + mask-multiply use case). |
| ~~STDLIB.495~~ | DONE | `slices.MovingSumInt(s, k) []int`. Rolling k-element sum via O(n) sliding-window. Pairs with the pre-existing MovingAverage/MovingMin/MovingMax (verified the latter two were already present — saved myself a redundant edit). Useful for sliding-window counts, signal energy windows. Test: `tests-internal/slices_moving.volt` (33 cases incl. MovingMin ≤ MovingAvg ≤ MovingMax cross-property). |
| ~~STDLIB.496~~ | DONE | `slices.CountInRangeInt(s, lo, hi) int` + `slices.FilterInRangeInt(s, lo, hi) []int`. Inclusive-range count + filter; degenerate hi<lo → 0/empty. Boundary-inclusive on both ends. Use case: histogram-bin tally (`CountInRangeInt(ages, 18, 29)`). Test: `tests-internal/slices_in_range.volt` (29 cases incl. CountInRange == len(FilterInRange) cross-property). |
| ~~STDLIB.497~~ | DONE | `strings.MapLines(s, fn fun(string) string) string` + `strings.FilterLines(s, keep fun(string) bool) string`. Per-line map / filter with auto rejoin on '\n' (CRLF normalized via Lines). Use cases: log-level filter, blank-line strip, line-by-line transform (case-fold, prepend, etc.). Test: `tests-internal/strings_map_filter_lines.volt` (17 cases incl. composition filter-then-map + error-line extraction). |
| ~~STDLIB.498~~ | DONE | `strings.UniqueLines(s) string` + `strings.SortLines(s) string`. UniqueLines dedupes preserving first-occurrence order (awk `!seen[$0]++` semantics); SortLines lex-sorts ascending. Both rejoin with '\n' and normalize CRLF. SortLines uses an inlined insertion sort to avoid taking a sort dep on the strings package. Test: `tests-internal/strings_unique_sort_lines.volt` (16 cases incl. log-dedup use case + sort-then-unique composition). |
| ~~STDLIB.499~~ | DONE | `strings.GrepLines(s, pat) string` + `strings.GrepLinesNot(s, pat) string`. Substring-grep (`grep` / `grep -v`) per line, rejoined with '\n'. CRLF normalized. Empty pat keeps all (Grep) / drops all (GrepNot) matching Unix semantics. Use case: log-level filtering. Test: `tests-internal/strings_grep_lines.volt` (18 cases incl. Grep + GrepNot partition cross-check). |
| ~~STDLIB.500~~ | DONE | `slices.LongestRunValueInt(s) (int, int)` + `slices.LongestRunValueString(s) (string, int)`. Returns `(value, length)` of the longest run — surfaces WHAT formed the streak alongside its length (existing LongestRun*Int returned only length). Ties go to the earlier run (stable). Use case: longest win-streak in a game log. Test: `tests-internal/slices_longest_run_value.volt` (23 cases incl. cross-check that length matches LongestRunInt). 🎯 STDLIB.500 + 650-test milestone. |
| ~~STDLIB.501~~ | DONE | `slices.PartitionBySignInt(s) ([]int, []int, []int)`. Three-way split into (positives, zeros, negatives) in original order — saves three FilterInts calls. Use case: ledger split into credits/zero/debits. Cross-property: lengths sum to len(s). Test: `tests-internal/slices_partition_by_sign.volt` (32 cases). |
| ~~STDLIB.502~~ | DONE | `slices.CommonPrefixLenInt(a, b) int` + `slices.CommonSuffixLenInt(a, b) int` + `slices.CommonPrefixLenString(a, b) int` + `slices.CommonSuffixLenString(a, b) int`. Element-wise common prefix / suffix length. Bounded by min(len(a),len(b)). Use cases: path-prefix length (shared dir components), trie-style comparison, diff anchor detection. Cross-property: `HasPrefixInts(s,p) ⇔ CommonPrefixLenInt(s,p) >= len(p)`. Test: `tests-internal/slices_common_prefix_suffix.volt` (18 cases). |
| ~~STDLIB.503~~ | DONE | `slices.PadLeftInt(s, n, v) []int` + `slices.PadRightInt(s, n, v) []int`. Pad with value v on left/right to reach length n; n <= len → unchanged copy. Use cases: fixed-width buffer prep, batching to a fixed length, 8-byte boundary alignment. Test: `tests-internal/slices_pad.volt` (32 cases). |
| ~~STDLIB.504~~ | DONE | `slices.PadLeftString(s, n, v) []string` + `slices.PadRightString(s, n, v) []string`. String counterparts of PadLeftInt/PadRightInt. Test: `tests-internal/slices_pad_string.volt` (29 cases). |
| ~~STDLIB.505~~ | DONE | `slices.TrimLeftInt(s, v) []int` + `slices.TrimRightInt(s, v) []int` + `slices.TrimInt(s, v) []int`. Strip leading / trailing / both occurrences of value v. Interior values preserved. Use cases: leading-zero stripping, padding cleanup. Test: `tests-internal/slices_trim_value.volt` (33 cases incl. TrimInt == TrimRight∘TrimLeft cross-property). |
| ~~STDLIB.506~~ | DONE | `slices.TrimLeftString(s, v) []string` + `slices.TrimRightString(s, v) []string` + `slices.TrimString(s, v) []string`. String counterparts of TrimLeftInt/TrimRightInt/TrimInt. Use case: CSV-cell cleanup (strip leading/trailing blank cells). Test: `tests-internal/slices_trim_string.volt` (32 cases). |
| ~~STDLIB.507~~ | DONE | `slices.IndexNthInt(s, v, n) int` + `slices.IndexNthString(s, v, n) int`. 0-indexed n-th occurrence of v; -1 if fewer than n+1 occurrences. Saves the `IndicesOf(s, v)[n]` materialize-and-index pattern. Use case: "second tab separator". Cross-property: `IndexNthInt(s, v, n) == IndicesOfInt(s, v)[n]` when valid. Test: `tests-internal/slices_index_nth.volt` (19 cases). |
| ~~STDLIB.508~~ | DONE | `strings.IndexNthByte(s, c, n) int` + `strings.IndexNth(s, sub, n) int`. String counterparts: 0-indexed n-th occurrence of byte / substring; -1 on miss. Substring matches are non-overlapping (`IndexNth("aaaa","aa",1)==2`). Empty sub returns -1. Use case: parse N-th column from a tab-separated record. Test: `tests-internal/strings_index_nth.volt` (23 cases incl. IndicesOf equivalence cross-property). |
| ~~STDLIB.509~~ | DONE | `bytes.IndexNthByte(s, c, n) int` + `bytes.IndexNth(s, sub, n) int`. Byte-slice mirrors of strings.IndexNthByte / IndexNth. Test: `tests-internal/bytes_index_nth.volt` (20 cases incl. high-bit-byte preservation). |
| ~~STDLIB.510~~ | DONE | `strings.NthField(s, n) string` + `strings.NthFieldSep(s, sep, n) string`. awk-style 0-indexed field access — NthField on whitespace (collapses runs), NthFieldSep on a literal byte separator (preserves empty fields like CSV). Out-of-range n → "". Cross-property: `NthField(s, n) == Fields(s)[n]` when valid. Test: `tests-internal/strings_nth_field.volt` (29 cases). 🎯 660-test milestone. |
| ~~STDLIB.511~~ | DONE | `slices.LongestString(s) string` + `slices.ShortestString(s) string` + `slices.MaxLenString(s) int` + `slices.MinLenString(s) int`. Direct longest/shortest-by-byte-length quartet. Stable tie-breaking (earlier wins). Use case: table column-width sizing. Test: `tests-internal/slices_longest_string.volt` (26 cases incl. `len(LongestString) == MaxLenString` cross-property). |
| ~~STDLIB.512~~ | DONE | `slices.LengthsString(s) []int` + `slices.TotalLenString(s) int`. Length distribution projection + total byte count. Use cases: buffer pre-allocation pre-flight, length histogram. Cross-property: TotalLenString == SumInts(LengthsString); MaxLenString == MaxInts(LengthsString). Test: `tests-internal/slices_string_lens.volt` (25 cases). |
| ~~STDLIB.513~~ | DONE | `slices.IndexOfLongestString(s) int` + `slices.IndexOfShortestString(s) int`. Position of longest/shortest string. -1 on empty. Stable tie-breaking (earliest wins). Companion to LongestString/ShortestString — useful when caller needs to look up a sibling field at the same index. Test: `tests-internal/slices_index_of_longest.volt` (20 cases incl. `s[IndexOfLongest] == LongestString` cross-property). |
| ~~STDLIB.514~~ | DONE | `strings.OrdinalSuffix(n) string` + `strings.Ordinal(n) string`. English ordinal rendering — handles teen rule (11/12/13 → "th") and negative n via absolute value. Use case: rank display ("1st place", "112th try"). Test: `tests-internal/strings_ordinal.volt` (35 cases). |
| ~~STDLIB.515~~ | DONE | `strings.WithCommas(n) string`. Render n with US thousand-separator commas ("1,234,567"). Negative n preserves leading '-'. Companion to HumanCount (which abbreviates) for cases where exact values are needed. Test: `tests-internal/strings_with_commas.volt` (24 cases). |
| ~~STDLIB.516~~ | DONE | `time.HumanizeDuration(ns) string`. Human-readable duration in the largest fitting unit ("3 hours", "1 day"). Singular form drops 's'. Sub-second rounds up to "1 second". Negative durations get '-' prefix. Use case: UI "active 2 hours ago"-style display. Test: `tests-internal/time_humanize_duration.volt` (21 cases). |
| ~~STDLIB.517~~ | DONE | `time.HumanAgo(t) string` + `time.HumanAgoFromNanos(delta) string`. Returns "X ago" for past, "in X" for future, "just now" for exact equality. HumanAgo uses Now() for the reference point; HumanAgoFromNanos is the deterministic delta-based form (preferred for testing / non-wall-clock measurements). Use case: activity-feed "last seen". Test: `tests-internal/time_human_ago.volt` (17 cases). |
| ~~STDLIB.518~~ | DONE | `time.IsSameDay(a, b) bool` + `time.IsToday(t) bool` + `time.IsYesterday(t) bool` + `time.IsTomorrow(t) bool`. UTC-day equality (ignores time-of-day); Today/Yesterday/Tomorrow use Now() as the reference. Use case: "is this from today's batch?" checks. Test: `tests-internal/time_same_day.volt` (14 cases incl. leap-day + month/year boundaries). |
| ~~STDLIB.519~~ | DONE | `time.IsWeekend(t) bool` + `time.IsWeekday(t) bool`. Top-level companions to the existing `(t Time).IsWeekend()` / `IsWeekday()` methods — pair them so call-site style choice is open. Test: `tests-internal/time_is_weekend.volt` (15 cases incl. equivalence cross-check with method form + 7-day-walk weekend/weekday count). |
| ~~STDLIB.520~~ | DONE | `time.NextWeekday(t) Time` + `time.PrevWeekday(t) Time`. Next/previous weekday strictly after/before t. Fri→Mon (skipping weekend); Mon→prev Fri. Time-of-day preserved. Use case: "next business day delivery" scheduling. Test: `tests-internal/time_next_weekday.volt` (18 cases incl. NextWeekday∘PrevWeekday identity on weekdays). 🎯 670-test milestone. |
| ~~STDLIB.521~~ | DONE | `time.StartOfDay(t) Time` + `time.EndOfDay(t) Time` + `time.StartOfMonth(t) Time` + `time.EndOfMonth(t) Time`. Calendar-aligned bounds — midnight to end-of-day, first-of-month to last-of-month. EndOfMonth uses DaysInMonth for 28/29/30/31 handling (leap-year aware). Use case: range queries for "today's events" / "this month's transactions". Test: `tests-internal/time_bounds.volt` (27 cases incl. Feb leap-year). |
| ~~STDLIB.522~~ | DONE | `time.StartOfYear(t) Time` + `time.EndOfYear(t) Time` + `time.StartOfHour(t) Time` + `time.EndOfHour(t) Time`. Extends the calendar-bounds family to year-level and hour-level granularity. Use cases: annual range queries ("this year's totals"), hourly bucket aggregation. Test: `tests-internal/time_bounds_year_hour.volt` (28 cases). |
| ~~STDLIB.523~~ | DONE | `time.StartOfWeek(t) Time` + `time.EndOfWeek(t) Time`. ISO-week aligned (Monday → Sunday). StartOfWeek backs up to the prior Monday at midnight; EndOfWeek advances to the next Sunday 23:59:59.999999999. Sunday inputs treated as last day of week (no advance). Time-of-day dropped. Use case: weekly bucket aggregation. Test: `tests-internal/time_bounds_week.volt` (25 cases incl. month + year boundary). |
| ~~STDLIB.524~~ | DONE | `time.QuarterOf(t) int` + `time.StartOfQuarter(t) Time` + `time.EndOfQuarter(t) Time`. Calendar-quarter labels (1..4) and bounds. Q1=Jan-Mar, Q2=Apr-Jun, Q3=Jul-Sep, Q4=Oct-Dec. EndOfQuarter uses DaysInMonth on the final month of the quarter (handles Feb leap-quarter correctly). Use case: quarterly report ranges. Test: `tests-internal/time_quarter.volt` (30 cases). |
| ~~STDLIB.525~~ | DONE | `time.DayOfYear(t) int`. 1-indexed day-of-year (1..365/366). Sums DaysInMonth for months before t.Month(), then adds t.Day(). Leap-year aware (Feb 29 = day 60 in 2024, Mar 1 = 60 in non-leap). Use case: astronomy, agriculture, year-progress reporting. Test: `tests-internal/time_day_of_year.volt` (18 cases incl. `DayOfYear(Dec 31) == DaysInYear` cross-property). |
| ~~STDLIB.526~~ | DONE | `time.AgeInYears(birth, ref) int`. Integer age via year-subtract + month/day adjustment for birthday-not-yet-reached. Birth-after-ref → 0 (no negatives). Handles Feb 29 birthdays in non-leap reference years (March 1 considered "after" birthday). Use case: age-gated content, "happy birthday" check. Test: `tests-internal/time_age.volt` (13 cases incl. leap-day birthday + centennial). |
| ~~STDLIB.527~~ | DONE | `time.IsBirthday(birth, ref) bool` + `time.DaysUntilBirthday(birth, ref) int`. IsBirthday matches month+day only (year & time-of-day ignored). DaysUntilBirthday returns ≥0 days to next occurrence, with Feb 29 birthdays rolling to Mar 1 in non-leap target years. Use case: "happy birthday!" notification + "X days to your birthday" countdown. Test: `tests-internal/time_birthday.volt` (15 cases incl. leap-day handling + year-rollover). |
| ~~STDLIB.528~~ | DONE | `time.DaysLeftInYear(t) int` + `time.DaysLeftInMonth(t) int`. Inclusive-of-t-day countdown (0 on last day). Cross-properties: `DayOfYear + DaysLeftInYear == DaysInYear`; `t.Day() + DaysLeftInMonth == DaysInMonth`. Leap-year aware. Use case: year/month progress bars, fiscal-year reports. Test: `tests-internal/time_days_left.volt` (20 cases). |
| ~~STDLIB.529~~ | DONE | `time.AddBusinessDays(t, n) Time` + `time.BusinessDaysBetween(a, b) int`. AddBusinessDays advances/retreats n Mon-Fri days, skipping weekends. BusinessDaysBetween counts business days strictly between two times (sign-preserving). Use case: SLA-deadline calculation. Test: `tests-internal/time_business_days.volt` (21 cases). Notable impl wrinkle: initial BusinessDaysBetween hit ownership-checker rejection on Time-struct swap (`lo = b` after `lo = a`) — rewrote using int ns fields instead of swapping Time values. |
| ~~STDLIB.530~~ | DONE | `slices.IndexOfMaxAbsInt(s) int` + `slices.IndexOfMinAbsInt(s) int`. Position-returning companions to MaxAbsInt / MinAbsInt — surface where the peak / closest-to-zero element lives in the slice. -1 on empty. Stable. Use case: signal-peak detection where the timestamp matters. Test: `tests-internal/slices_index_of_max_abs.volt` (21 cases incl. `abs(s[IndexOfMaxAbs]) == MaxAbs` cross-property). 🎯 680-test milestone. |
| ~~STDLIB.531~~ | DONE | `slices.UpsertInt(s, v) []int` + `slices.UpsertString(s, v) []string`. Append v iff not already present (idempotent set-like insert). Order preserved; doesn't mutate s. Use case: maintaining a tag set or a unique-IDs list across iterative updates. Test: `tests-internal/slices_upsert.volt` (26 cases incl. tag-set use case + Contains cross-property). |
| ~~STDLIB.532~~ | DONE | `slices.ReplaceFirstInt(s, old, repl) []int` + `slices.ReplaceFirstString(s, old, repl) []string`. Replace ONLY the first occurrence (in contrast to ReplaceAllInt). Length unchanged. Doesn't mutate. Use case: update one record in a unique-ID list. Cross-property: when `old` appears at most once, ReplaceFirst == ReplaceAll. Test: `tests-internal/slices_replace_first.volt` (25 cases). |
| ~~STDLIB.533~~ | DONE | `strings.SplitTrim(s, sep) []string` + `strings.SplitTrimNonEmpty(s, sep) []string`. SplitTrim runs Split then TrimSpace each piece (keeps empty pieces); SplitTrimNonEmpty additionally drops trimmed-empty pieces. Use case: CSV-row cleanup ("  alice  , 30 , engineer , " → ["alice","30","engineer",""] or three non-empty). Test: `tests-internal/strings_split_trim.volt` (33 cases incl. multi-char separator + tabs). |
| ~~STDLIB.534~~ | DONE | `strings.Tabularize(rows [][]string, sep) string`. Column-aligned table rendering — pad each cell to the widest in its column, join with sep, rows joined with '\n'. Last column unpadded (no trailing space). Uneven row widths supported (missing cells → ""). Use case: CLI report tables, debug dumps. Test: `tests-internal/strings_tabularize.volt` (9 cases incl. uneven rows + various separators). |
| ~~STDLIB.535~~ | DONE | `strings.Hexdump(s) string` + helpers (`FormatHexPad2`, `FormatHexPad8`, `hexNibbleByte`). xxd-style 16-byte-per-row dump with 8-digit offset column, two 8-byte hex groups separated by extra space, and ASCII column with non-printable bytes shown as '.'. Use case: binary payload diagnosis, error-context dumps. Test: `tests-internal/strings_hexdump.volt` (20 cases). |
| ~~STDLIB.536~~ | DONE | `bytes.Hexdump(s) string` + private hd_* hex helpers. Byte-slice mirror of strings.Hexdump; output is BYTE-FOR-BYTE identical for the same content (test verifies `bytes.Hexdump([]byte(s)) == strings.Hexdump(s)`). Test: `tests-internal/bytes_hexdump.volt` (12 cases). Notable: hit a self-reference gotcha — bytes.volt couldn't say `bytes.NewBuilder()` since it IS bytes; replaced bulk `bytes.Builder` → bare `Builder` in three pre-existing call sites. |
| ~~STDLIB.537~~ | DONE | `strings.Excerpt(s, at, radius) string` + `strings.LineColAt(s, at) (int, int)`. Excerpt shows ≤radius bytes around byte position `at` with "\|<=here\|" marker and ellipses when truncated. LineColAt returns 1-indexed (line, col). Use case: compiler/parser error context display ("syntax error at line 2 col 9: …var x = \|<=here\| oops…"). Test: `tests-internal/strings_excerpt.volt` (27 cases incl. position clamping + multi-line). |
| ~~STDLIB.538~~ | DONE | `strings.LineRange(s, from, to) string`. 1-indexed line slice — extract lines `from` through `to` rejoined with '\n'. Bounds clamped to [1, lineCount]; from > to → "". Use case: 3-line context window around an error line (`LineRange(prog, errLine-1, errLine+1)`). Note: 1-indexed because it pairs with LineColAt — distinct from the pre-existing 0-indexed `LineAt`. Test: `tests-internal/strings_line_range.volt` (18 cases). |
| ~~STDLIB.539~~ | DONE | `strings.CaretLine(s, lineNo, col) string`. Renders a source line followed by a `^`-pointer line aligned to `col`. lineNo/col both 1-indexed; out-of-range lineNo → "", col<1 clamps to 1. Composes naturally with LineColAt for a full compiler-error display. Test: `tests-internal/strings_caret_line.volt` (17 cases incl. full LineColAt → CaretLine end-to-end). |
| ~~STDLIB.540~~ | DONE | `strings.PrefixOf(s, prefixes) string` + `strings.SuffixOf(s, suffixes) string`. Return the actual MATCHED prefix/suffix from a list (or "" on miss) — distinct from HasAnyPrefix/Suffix which only return a bool. First-match wins; caller controls order (longest-first to handle ".tar.gz" before ".gz"). Use cases: URL routing, file-extension classification. Test: `tests-internal/strings_prefix_suffix_of.volt` (16 cases). 🎯 690-test milestone. |
| ~~STDLIB.541~~ | DONE | `strings.LongestPrefixOf(s, prefixes) string` + `strings.LongestSuffixOf(s, suffixes) string`. Longest-match-wins variant — order-independent. Stable tie-break (earlier wins on equal length). Use case: routing-precision (where /admin/users/ should beat /admin/ regardless of list order). Test: `tests-internal/strings_longest_prefix_of.volt` (14 cases). |
| ~~STDLIB.542~~ | DONE | `strings.TrimAnyPrefix/TrimAnySuffix(s, list) string` + `strings.TrimLongestPrefix/TrimLongestSuffix(s, list) string`. Strip the first-/longest-matching prefix/suffix. Use cases: URL scheme normalization, archive-stem extraction (`TrimLongestSuffix("data.tar.gz", [".gz",".bz2",".tar.gz"])` → "data"). Test: `tests-internal/strings_trim_any.volt` (17 cases). |
| ~~STDLIB.543~~ | DONE | `slices.UpsertSortedInt(s, v) []int` + `slices.UpsertSortedString(s, v) []string`. Sorted-slice idempotent insert — if v already present, returns copy unchanged; else InsertSortedInt's position. Companion to UpsertInt for sorted invariants. Use case: maintaining sorted unique-IDs/usernames list. Test: `tests-internal/slices_upsert_sorted.volt` (31 cases incl. sortedness cross-property). |
| ~~STDLIB.544~~ | DONE | `sort.StringsByLen(s) []string` + `sort.StringsByLenDesc(s) []string`. In-place sort by byte length, ascending / descending. Stable (equal-length order preserved). Saves the `sort.StringsBy(s, fun(a,b) bool { ret len(a) < len(b) })` ceremony. Use case: longest-prefix-first ordering for trie / route table; shortest-first column rendering. Test: `tests-internal/sort_by_len.volt` (27 cases). |
| ~~STDLIB.545~~ | DONE | `sort.IntsByAbs(s) []int` + `sort.IntsByAbsDesc(s) []int`. Stable in-place sort by absolute value. Use case: "closest-to-zero first" ranking — residuals, deviations, signed errors. Test: `tests-internal/sort_by_abs.volt` (26 cases incl. stable-tie verification on ±3 pairs). |
| ~~STDLIB.546~~ | DONE | `sort.StringsAscCI/StringsDescCI(s)` + private `stringLessCI`. Case-insensitive ASCII sort ('A'..'Z' folded to 'a'..'z') — stable on equal-fold pairs ("Alice"/"alice"/"ALICE" keep input order). Use case: human-facing user-name list ordering where "alice", "Bob", "Carol" should appear in that order instead of "Bob", "Carol", "alice" under raw byte sort. Test: `tests-internal/sort_ci.volt` (27 cases). |
| ~~STDLIB.547~~ | DONE | `slices.BucketCountInt(s, lo, hi, n) []int`. Distribute s into n equal-width buckets covering [lo, hi]; out-of-range elements dropped; hi maps into last bucket. Use cases: latency-percentile bins, score-distribution histograms. Cross-property: sum of buckets == CountInRangeInt(s, lo, hi). Test: `tests-internal/slices_bucket_count.volt` (21 cases). |
| ~~STDLIB.548~~ | DONE | `strings.BulletList(items, marker)` + `strings.NumberedList(items)`. CLI-friendly list rendering. BulletList prefixes each line with `marker + " "` (empty marker → naked list). NumberedList emits 1-indexed `N. item`. Both rejoin with '\n'. Use cases: command help, recipe steps, status reports. Test: `tests-internal/strings_bullet_list.volt` (15 cases). |
| ~~STDLIB.549~~ | DONE | `slices.PercentOfTotalInt(s, scale) []int`. Rescale s so the sum equals `scale` (100 for percent, 1000 for permille). Integer truncation per cell — output sum may be 1-2 short of `scale` due to rounding. All-zero / empty inputs → all-zero / empty outputs. Use cases: budget split visualization, vote-share display. Test: `tests-internal/slices_percent_of_total.volt` (26 cases). |
| ~~STDLIB.550~~ | DONE | `slices.PercentOfMaxInt(s, scale) []int`. Rescale so the max element equals `scale`. Sign preserved. Use case: ASCII bar charts where longest bar should fill a fixed character width. Built on MaxAbsInt so a negative-magnitude outlier saturates correctly. Test: `tests-internal/slices_percent_of_max.volt` (27 cases). 🎯 700-test milestone. |
| ~~STDLIB.551~~ | DONE | `strings.AsciiBar(count, width, fill, empty) string`. ASCII bar rendering primitive — `count` fill-bytes followed by `(width-count)` empty-bytes. Clamps count to [0, width]. Pairs naturally with PercentOfMaxInt for full bar-chart pipeline. Use cases: terminal dashboards, progress indicators, log-line graphs. Test: `tests-internal/strings_ascii_bar.volt` (16 cases). |
| ~~STDLIB.552~~ | DONE | `strings.AsciiHistogramRow(label, labelWidth, count, barWidth, fill, empty) string`. Composes PadRight + AsciiBar to render a single labeled bar row ("label  | ####.."). Use case: terminal histogram dashboards. Test: `tests-internal/strings_histogram_row.volt` (13 cases). |
| ~~STDLIB.553~~ | DONE | `strings.AsciiHistogram(labels []string, counts []int, barWidth, fill, empty) string`. Full-dashboard one-shot: auto-fits `labelWidth` to longest label, auto-scales counts so max bar fills `barWidth`, emits one row per label joined with `'\n'` (no trailing newline). Mismatched len(labels)≠len(counts) returns `""` defensively. Negative counts clamp to 0. Use case: terminal status dashboards, log-line graphs. Test: `tests-internal/strings_histogram.volt` (11 cases: basic 3-row, single-row, empty, mismatched, all-zero, negative-clamp, custom fill/empty, dominant-tall-bar, no-trailing-newline assertion, all-same, barWidth=0). |
| ~~STDLIB.554~~ | DONE | `slices.IndicesOfTopNInt(s []int, n int) []int`. Returns the indices (into the original s) of the n largest elements, ordered by value descending. Companion to `TopNInt` for callers who hold a PARALLEL slice (typical: labels []string + counts []int) — projecting `labels[idx]` over the result gives a top-N dashboard. Stable tie-break (earliest index wins). Empty/n<=0 → empty; n>len(s) clamps. Test: `tests-internal/slices_indices_top_n.volt` (31 checks across 11 scenarios: basic, empty, n≤0, clamp, single, all-equal tie-break, mixed neg/zero, full permutation, parallel-labels dashboard demo, no-mutation invariant). |
| ~~STDLIB.555~~ | DONE | `maps.TopNKeysStringInt(m map[string]int, n int) ([]string, []int)`. Returns top-n keys ordered by their values descending, alongside the matching values. Built on the existing snapshot-then-O(k·n)-selection pattern from `slices.TopNInt`. Pairs with `slices.HistogramString` (frequency map) + `strings.AsciiHistogram` (rendered bars) to form a one-pipeline word-count dashboard. n<=0 → empty; n>size clamps. Test `tests-internal/maps_top_n_keys.volt` (31 assertions: basic, empty, n≤0, clamp, single, all-equal, dashboard pipeline). |
| ~~STDLIB.556~~ | DONE | `strings.Sparkline(values []int) string`. Single-line ASCII trend graph using the 9-char ramp `.:-=+*#%@`. Each value maps to one ramp byte proportional to its magnitude vs max. Empty → "". All-zero → all-min-char `.`. Negative values clamp to 0. Use case: terminal one-line time-series preview, log-line numeric summary. Test `tests-internal/strings_sparkline.volt` (10 cases: triangle wave, empty, all-zero, single, neg-clamp, monotonic 0..8, all-same, dominant outlier, mid-range, length invariant). |
| ~~STDLIB.557~~ | DONE | `slices.NormalizeRangeInt(s []int, lo, hi int) []int`. Linear rescale: maps `[min(s), max(s)]` → `[lo, hi]`. Min→lo, max→hi, others linearly interpolated. Empty → empty. Constant input (min==max) → all `lo`. Composes with Sparkline to compress arbitrary value ranges into the 0..N magnitude the renderer wants. Integer division per cell so middle cells may be 1-2 short. Test `tests-internal/slices_normalize_isconstant.volt`. |
| ~~STDLIB.558~~ | DONE | `slices.IsConstantInt(s []int) bool`. All elements equal? Empty + single-element → true vacuously. Useful as a guard before sparkline/chart rendering (constant series = flat line) or change-detection in metric streams. Test included with STDLIB.557 (21 total assertions across both helpers + Sparkline composition demo). |
| ~~STDLIB.559~~ | DONE | `strings.Banner(text string, fill byte) string`. Renders text framed in a single-line ASCII box. Multi-line text auto-aligns to widest line. Empty text → border-only box. Use case: terminal section headers, "==== HEADER ====" log dividers. Test `tests-internal/strings_banner.volt` (10 cases). |
| ~~STDLIB.560~~ | DONE | `strings.RepeatByte(c byte, n int) string`. n copies of byte c. Faster than `Repeat(chr(c), n)` since it skips the heap-string-per-byte intermediate. n<=0 → "". Used internally by Banner; useful for ASCII borders/fills. Test bundled with Banner. |
| ~~STDLIB.561~~ | DONE | `slices.OutlierBoundsInt(s []int) (int, int)`. Returns Tukey fence: `(Q1 - 1.5*IQR, Q3 + 1.5*IQR)`. Values outside this range are conventionally considered outliers. Empty/single → (0, 0). 1.5 multiplier is fixed (Tukey standard); user multiplies for "extreme outliers" mode. Use case: anomaly filtering, metric pre-processing, dashboard auto-zoom. |
| ~~STDLIB.562~~ | DONE | `slices.IsOutlierInt(s []int, v int) bool`. Convenience: `v` outside Tukey fence of `s`. Used during streaming metric processing for the common is-this-anomalous check. Test `tests-internal/slices_outliers.volt` (17 assertions: basic uniform, empty, single, heavily-skewed dominant outlier, negative-value range). |
| ~~STDLIB.563~~ | DONE | `time.StartOfMinute(t Time) Time` + `time.EndOfMinute(t Time) Time`. Round t down/up to the minute boundary. Same family as StartOfHour/EndOfHour — useful for per-minute aggregation buckets in metric streams. Test `tests-internal/time_minute_bounds.volt` (11 assertions). |
| ~~STDLIB.564~~ | DONE | `slices.DropBlankStrings(s []string) []string`. Returns a fresh slice with only the non-empty elements of s. Order preserved. Useful after splitting strings on multiple delimiters (consecutive separators yield empty fields) or filtering user input. Different from `CompactString` (drops adjacent dupes of any value); this drops only `""`. Test `tests-internal/slices_drop_blank.volt` (13 assertions). |
| ~~STDLIB.565~~ | DONE | `slices.ClampInts(s []int, lo, hi) []int`. Element-wise clamp: values below lo become lo, above hi become hi. Equivalent to `MapInts(s, x -> math.ClampInt(x, lo, hi))` but inlined. Use case: bounding metric streams before render, sanitizing noisy sensor data. Test `tests-internal/slices_clamp_ints.volt` (18 assertions). |
| ~~STDLIB.566~~ | DONE | `strings.TitleCase(s string) string`. Capitalizes the first letter of each space-separated word, lowercasing the rest. ASCII-only; bytes outside [A-Za-z] pass through. Whitespace classes (space/tab/newline/CR) reset the "at word start" flag. Use case: book titles, header rendering, name normalization. Test `tests-internal/strings_title_case.volt` (12 cases). |
| ~~STDLIB.567~~ | DONE | `time.DiffMinutes(t, u) int` + `time.DiffSeconds(t, u) int`. Whole-minute and whole-second deltas from u to t, sign-preserving, truncates toward zero. Completes the DiffDays/DiffHours family for finer granularity. Test `tests-internal/time_diff_minutes_seconds.volt` (10 cases including negative direction, truncation, cross-hour). |
| ~~STDLIB.568~~ | DONE | `slices.FilledInt(v int, n int) []int` + `slices.FilledString(v string, n int) []string`. Returns a fresh slice of n copies of v. n<=0 → empty. Different from RepeatInt/String (which tile a SLICE n times); this fills with a single scalar. Use case: zero-padded buffers, default-value initialization, test fixtures. Test `tests-internal/slices_filled.volt` (16 assertions). |
| ~~STDLIB.569~~ | DONE | `time.WeekOfYear(t Time) int`. Returns the 1-based calendar week number — week 1 = Jan 1-7, week 2 = Jan 8-14, etc., regardless of which weekday Jan 1 lands on. Range 1..53. Simpler than ISO-8601 weeks-starting-Monday; matches what most reporting + UI bucketing wants. Test `tests-internal/time_week_of_year.volt` (8 cases: Jan boundaries, mid-year, year-end on leap + non-leap). |
| ~~STDLIB.570~~ | DONE | NEW PACKAGE `runtime` with `Compact()`, `ThreadCount()`, `RaceViolations()`, `ResetRaceViolations()`. Compact walks size-class freelists and merges adjacent free blocks (substance landed in Pass 753 — drains freelist[sc], sorts by address, merges adjacent same-class pairs into freelist[sc+1] with 16-byte trailing waste). ThreadCount returns the race-detector's registered thread count (0 without -race). RaceViolations is the cumulative race count surfaced from runtime; ResetRaceViolations zeros it for per-subtest accounting. Stub `.volt` source forwards to compiler intrinsics; embed registered in stdlib.go. Test `runtime_compact.volt` (mixed-size churn + Compact + post-compact verification). |
| ~~STDLIB.571~~ | DONE | `slices.ExpAvgInt(s []int, alpha100 int) []int`. Exponentially weighted moving average with alpha expressed as percent (0..100). First output = s[0]; each subsequent out[i] = (a * s[i] + (100 - a) * out[i-1]) / 100. Use case: smoothing noisy time-series, signal denoising, recency-weighted metric streams. Empty input → empty. alpha100 clamped to [0,100]. Test `slices_exp_avg.volt` (16 assertions). |
| ~~STDLIB.572~~ | DONE | `strings.FieldsAny(s string, separators string) []string`. Splits s into non-empty fields separated by any byte in `separators`. Consecutive separators collapse; empty fields skipped. Ergonomic alternative to FieldsFunc when separators are a known string. Empty separators → s as a single field; empty s → []. Test `strings_fields_any.volt` (20 assertions). |
| ~~LANG.7~~ | DONE | `delete(m, key)` builtin — Go-style map entry removal. Runtime `volt_map_delete(m, key_ptr, key_len)` does the bucket unlink under the existing mutex. Silent on missing key (matches Go's no-op semantics). Codegen `emitBuiltinDelete` dispatched from the existing `emitCall` IdentExpr switch (alongside len/cap/chr/append/close/read/write/clone) — validates that the first arg is a map symbol, the second is a `%string` key, then lowers to the runtime call. Test: `tests-internal/map_delete.volt` (13 cases: map[string]int round-trip with delete-then-other-keys-survive, delete on missing key is no-op, delete-then-reinsert, empty-the-map; map[string]string variant exercises the Pass-98 box-via-heap-ptr path through the delete code). |
| ~~STDLIB.29~~ | DONE | `slices.ConcatInts(a, b) []int` + `ConcatStrings(a, b) []string` + `CountInt(s, v) int` + `CountString(s, v) int`. Concat builds a new exact-sized slice (no append-grow churn); count is the obvious linear-scan tally. Tests cover empty-input concat (both sides), multi-element concat round-trips, counting with duplicates / no-match / empty slice. 11/11. |
| ~~STDLIB.30~~ | DONE | `strings.Fields(s) []string` — Go-style whitespace splitter (splits on runs of space / tab / newline / CR / VT / FF, collapses adjacent whitespace, never returns empty pieces). Two-pass impl (count then collect) so the result is exact-sized; per-field copy via `bytes.Builder`. New private helper `isWhitespace(b byte) bool`. Test: `tests-internal/strings_fields.volt` (14 cases — single-space split, multi-space collapse, mixed whitespace bytes, empty, all-whitespace, single token). |
| ~~STDLIB.31~~ | DONE | `bytes` package gains scan helpers — `Equal(a, b)`, `HasPrefix(s, prefix)`, `HasSuffix(s, suffix)`, `Index(s, sub)` (returns -1 if absent, empty-sub returns 0 per Go), `Contains(s, sub)`. Parallel surface to the corresponding `strings.*` helpers, but for `[]byte` slices. Naive O(n*m) Index scan (matches strings.Index complexity). Test: `tests-internal/bytes_scan.volt` (13 cases). |
| ~~STDLIB.32~~ | DONE | `strings.Cut(s, sep) (before, after, found)` + `CutPrefix(s, prefix) (rest, found)` + `CutSuffix(s, suffix) (rest, found)` — Go 1.18 / 1.20 stdlib pattern that combines Index + slicing into a single multi-return. Cut empty-sep returns ("", s, true) per Go. Each surviving portion is rebuilt via `bytes.Builder`. Test: `tests-internal/strings_cut.volt` (17 cases). |
| ~~STDLIB.33~~ | DONE | `time.Since(t)` + `time.Until(t)` + `time.Minute` + `time.Hour`. Since/Until are codegen intrinsics over `volt_mono_ns()` plus a single sub. Minute (60s) and Hour (3600s) added to the compile-time duration constants in `emitFieldAccess` (alongside Nanosecond/Microsecond/Millisecond/Second). Test: `tests-internal/time_since_until.volt` (10 cases). |
| ~~STDLIB.34~~ | DONE | `strings.ContainsAny(s, chars) bool` + `strings.Map(fn fun(byte) byte, s string) string`. ContainsAny wraps the existing `IndexAny ≥ 0`. Map walks each byte through fn into a `bytes.Builder`. Drop-on-negative-return behavior not modeled (no rune type). Test: `tests-internal/strings_map_containsany.volt` (10 cases including ROT13 closure roundtrip). |
| ~~STDLIB.35~~ | DONE | `strings.IndexFunc(s, fn fun(byte) bool) int` + `LastIndexFunc(s, fn) int` + `TrimFunc(s, fn) string`. Predicate-based byte scans: first/last position satisfying fn (returns -1 if absent); trim leading + trailing bytes for which fn returns true. Test: `tests-internal/strings_indexfunc.volt` (11 cases). |
| ~~STDLIB.36~~ | DONE | `strconv.Quote(s) string` — Go-syntax double-quoted string with `\a`/`\b`/`\t`/`\n`/`\v`/`\f`/`\r`/`\\`/`\"` for control and meta-bytes plus `\xNN` for non-printable bytes (< 0x20 or > 0x7E). Private helper `hexNibble(n int) byte`. Uses the `s[i] & 255` mask trick so high-bit bytes don't sign-extend through the printable-range check. Test: `tests-internal/strconv_quote.volt` (11 cases). |
| ~~STDLIB.37~~ | DONE | `strconv.Unquote(s) (string, error)` — inverse of Quote. Validates wrapping quotes, decodes the standard escapes plus `\xNN`. Error paths: too-short input, missing quote, dangling escape, short / bad `\x`, unknown escape. New private helper `nibbleValue(c byte) int`. Test: `tests-internal/strconv_unquote.volt` (15 cases including round-trips and error paths). |
| ~~STDLIB.38~~ | DONE | `sort.IntsBy(s, less fun(int, int) bool) []int` + `sort.StringsBy(s, less fun(string, string) bool) []string`. Generic comparator-based stable insertion sort — caller supplies the ordering. Break condition `!less(x, s[j])` (equals `s[j] <= x` in less-semantics) preserves stability. Unblocks descending sort, by-key sort, multi-criteria sort. Test: `tests-internal/sort_by.volt` (14 cases). |
| ~~STDLIB.39~~ | DONE | `slices.EqualInts(a, b) bool` + `EqualStrings(a, b) bool` + `MinString(s) string` + `MaxString(s) string`. Length-then-element compare; lex extrema (empty input → ""). Private helper `stringLess(a, b) bool` inlines byte-wise lex-compare since codegen doesn't support `<` on `%string` operands. Test: `tests-internal/slices_equal_minmax.volt` (12 cases). |
| ~~STDLIB.40~~ | DONE | `os.UserHomeDir() (string, error)` + `os.UserConfigDir() (string, error)`. UserHomeDir wraps `Getenv("HOME")`; UserConfigDir prefers `$XDG_CONFIG_HOME`, falls back to `$HOME/.config`. **Side**: codegen now does same-package intrinsic dispatch — a new switch at top of `emitUnqualifiedCall` maps `c.e.pkg + "." + fn.Name` so an unqualified `Getenv("HOME")` from inside os.volt resolves to the same `emitOsGetenv` codegen path that cross-package `os.Getenv` already uses. Test: `tests-internal/os_userdirs.volt` (6 cases). |
| ~~STDLIB.41~~ | DONE | `bytes.ToLower(s []byte) []byte` + `bytes.ToUpper(s []byte) []byte` + `bytes.Repeat(b []byte, count int) []byte`. Parallel `strings.*` byte-slice equivalents. ASCII-only case fold; non-letter bytes pass through. Repeat returns empty for count ≤ 0. Exact-size allocation. Test: `tests-internal/bytes_case_repeat.volt` (16 cases). |
| ~~STDLIB.42~~ | DONE | `bytes.TrimSpace(s []byte) []byte` + `bytes.TrimPrefix(s, prefix) []byte` + `bytes.TrimSuffix(s, suffix) []byte`. Parallel `strings.*` byte-slice equivalents. TrimSpace strips space/tab/newline/CR/VT/FF on both sides. TrimPrefix / TrimSuffix return unchanged copies when the affix is absent. Test: `tests-internal/bytes_trim.volt` (15 cases). |
| ~~STDLIB.43~~ | DONE | `slices.IndexFuncInt(s, pred fun(int) bool) int` + `slices.IndexFuncString(s, pred fun(string) bool) int`. Predicate-based linear search returning the first matching index (-1 if absent). Parallels `strings.IndexFunc` for slice element types. Test: `tests-internal/slices_indexfunc.volt` (8 cases). |
| ~~STDLIB.44~~ | DONE | `bytes.Split(s, sep) [][]byte` + `bytes.Join(parts, sep) []byte`. Companion to LANG.8 (slice-of-slice double indexing now works). Split walks s, finds each sep occurrence, accumulates per-piece slices via append; sep-not-found returns single-element result. Join computes exact size, allocates once, copies parts with sep interleaved. Test: `tests-internal/bytes_split_join.volt` (21 cases). |
| ~~STDLIB.45~~ | DONE | `bytes.Count(s, sep) int` + `bytes.Fields(s) [][]byte`. Count returns non-overlapping sep occurrences (empty sep → 0). Fields splits on runs of Go-style whitespace, collapsing adjacent runs. Test: `tests-internal/bytes_count_fields.volt` (16 cases). |
| ~~STDLIB.46~~ | DONE | `bytes.IndexByte(s, c) int` + `bytes.LastIndexByte(s, c) int` + `bytes.LastIndex(s, sep) int`. Single-byte and last-occurrence search; LastIndex with empty sep returns len(s), sep longer than s returns -1 (matches Go). Test: `tests-internal/bytes_indexbyte.volt` (11 cases). |
| ~~STDLIB.47~~ | DONE | `slices.InsertInt(s, i, v) []int` + `DeleteInt(s, i, j) []int` + `InsertString` / `DeleteString` variants. Insert returns fresh slice with v at position i; Delete returns fresh slice with [i, j) removed. Volt has no in-place variants (slices move on use). Test: `tests-internal/slices_insert_delete.volt` (23 cases). |
| ~~STDLIB.48~~ | DONE | `maps.EqualStringInt(a, b) bool` + `maps.EqualStringString(a, b) bool`. Same-length-then-same-entries check via `for k, v := range a` + ContainsKey lookup in b. Test: `tests-internal/maps_equal.volt` (7 cases). |
| ~~STDLIB.49~~ | DONE | `os.TempDir() string` + `bytes.Map(fn fun(byte) byte, s []byte) []byte`. TempDir returns `$TMPDIR` else `/tmp`. bytes.Map parallels strings.Map for byte slices. Test: `tests-internal/os_tempdir_bytes_map.volt` (13 cases). |
| ~~BUG.4~~ | DONE | **Per-package method dispatch lands; `strings.Builder` now coexists with `bytes.Builder`.** Parser preserves the package qualifier on `NamedType` (was previously dropped after LANG.3). `extMethods` rekeyed from `map[type]map[method]*entry` to `map[type]map[method][]*entry` so multiple pkgs can register methods on the same bare typeName. New `(*Emitter).lookupExtMethod(type, method, preferPkg)` filters by receiver qualifier; legacy single-entry callers go through `firstExtMethod`. `emitMethodCallWithRecv` extracts `recvPkg` from the receiver's AST and, when it differs from the current package, skips the same-pkg `methods` map (which could shadow with a same-bare-name local type) and dispatches via extMethods filtered to recvPkg. `strings.Builder` added with own `WriteByte`/`WriteString`/`Reset`/`Len`/`String`. Test: `tests-internal/bug4_both_builders.volt` (10 cases — alive-at-once, interleaved writes, Reset, WriteString, mixed lengths). Note: `structs`/`typeOwningPkg`/`interfaceDecls` are still bare-keyed; widen later if real-world collisions surface. |
| ~~STDLIB.50~~ | DONE | `bytes.Replace(s, old, repl) []byte`. Replace-all only (no count param). Two-pass: count matches → allocate exact-size output → fill. Empty old returns copy unchanged. Parallel to strings.Replace. Test: `tests-internal/bytes_replace.volt` (16 cases). |
| ~~STDLIB.51~~ | DONE | `bytes.IndexAny(s, chars) int` + `bytes.ContainsAny(s, chars) bool`. Byte-set lookup; empty chars → -1 / false. Parallel to strings.IndexAny / ContainsAny. Test: `tests-internal/bytes_indexany.volt` (8 cases). |
| ~~STDLIB.52~~ | DONE | `bytes.Trim(s, cutset) []byte` + `bytes.TrimLeft(s, cutset) []byte` + `bytes.TrimRight(s, cutset) []byte`. Cutset-based directional trim. Empty cutset returns unchanged copy. Test: `tests-internal/bytes_trim_cutset.volt` (16 cases). |
| ~~STDLIB.53~~ | DONE | `bytes.Cut(s, sep) ([]byte, []byte, bool)` + `bytes.SplitN(s, sep, n) [][]byte`. Cut returns (before, after, found) — three-value; empty sep matches at 0. SplitN caps result at n parts; n=0 empty / n=1 single / n<0 unbounded. Test: `tests-internal/bytes_cut_splitn.volt` (22 cases). |
| ~~STDLIB.54~~ | DONE | `slices.RepeatInt(s, count) []int` + `slices.RepeatString(s, count) []string`. Mirrors Go 1.23's slices.Repeat. count ≤ 0 → empty. Exact-size allocation. Test: `tests-internal/slices_repeat.volt` (15 cases). |
| ~~STDLIB.55~~ | DONE | `crypto/rand.Int(max) int` — bounded cryptographic random integer in [0, max). max ≤ 0 → 0. Reads 7 random bytes via `syscall.GetRandom` (56 bits — always positive in i64), accumulates LSB-first, mods by max. Mod bias negligible for max ≪ 2^56. Test: `tests-internal/crypto_rand_int.volt` (7 cases). |
| ~~STDLIB.56~~ | DONE | `slices.CompactInt(s) []int` + `slices.CompactString(s) []string`. Mirrors Go 1.21's slices.Compact — removes consecutive duplicates only (non-adjacent survive). Two-pass exact-size build. Test: `tests-internal/slices_compact.volt` (16 cases). |
| ~~STDLIB.57~~ | DONE | `strings.SplitAfter(s, sep) []string`. Sister to Split that retains sep at end of each preceding piece. Empty sep → single-element slice containing s. Each piece via `bytes.Builder`. Test: `tests-internal/strings_splitafter.volt` (14 cases). |
| ~~STDLIB.58~~ | DONE | `slices.BinarySearchInt(s, v) (int, bool)` + `BinarySearchString(s, v) (int, bool)`. O(log n) half-open-interval search. Returns (insertion-point, found). String variant uses byte-wise `stringLess`. Test: `tests-internal/slices_binarysearch.volt` (18 cases). |
| ~~STDLIB.59~~ | DONE | `slices.ChunkInt(s, size) [][]int` + `slices.ChunkString(s, size) [][]string`. Splits s into consecutive size-bounded chunks; last chunk may be shorter. Empty / size ≤ 0 → empty. Mirrors Go 1.23's slices.Chunk (materialized slice form). Test: `tests-internal/slices_chunk.volt` (19 cases). |
| ~~STDLIB.60~~ | DONE | `slices.LastIndexFuncInt(s, pred) int` + `LastIndexFuncString` + `CountFuncInt(s, pred) int` + `CountFuncString`. Predicate-based reverse scan and tally helpers. Test: `tests-internal/slices_lastindex_count.volt` (12 cases). |
| ~~STDLIB.61~~ | DONE | `slices.ConcatAllInts(parts [][]int) []int` + `ConcatAllStrings(parts [][]string) []string`. N-way concat — Go 1.22's `slices.Concat` shape (variadic in Go; here takes [][]T). Exact-size allocate-and-copy. Uses LANG.8 slice-of-slice widening. Test: `tests-internal/slices_concatall.volt` (12 cases). |
| ~~STDLIB.62~~ | DONE | `maps.MergeStringInt(a, b)` + `maps.MergeStringString(a, b)`. Defaults+overrides pattern — fresh map with entries from a then b; b wins on collision. Test: `tests-internal/maps_merge.volt` (14 cases). |
| ~~STDLIB.63~~ | DONE | `bytes.Compare(a, b) int` + `bytes.EqualFold(a, b) bool` + `strings.Compare(a, b) int`. Three-way -1/0/+1 byte-wise lex compare (ties broken by length); EqualFold is ASCII-case-insensitive byte equality. Test: `tests-internal/compare.volt` (15 cases). |
| ~~STDLIB.64~~ | DONE | `slices.RangeInts(start, end) []int` + `slices.RangeIntsStep(start, end, step) []int`. Python-style integer-range generators. Half-open; step can be negative; zero-step → empty. Two-pass exact-size build. Test: `tests-internal/slices_range.volt` (19 cases). |
| ~~STDLIB.65~~ | DONE | `slices.UniqueInts(s) []int` + `UniqueStrings(s) []string` + `AverageInt(s) int`. UniqueXxx drop all duplicates preserving first-occurrence order (vs Compact's adjacent-only). AverageInt integer-truncated mean; empty → 0. Test: `tests-internal/slices_unique_avg.volt` (18 cases). |
| ~~STDLIB.66~~ | DONE | `math.Sign(x) int` + `math.LcmInt(a, b) int` + `math.Pow2(n) int`. Sign returns -1/0/+1; LcmInt = |a|/gcd × |b| (divides first to avoid overflow); Pow2 = `1 << n` for n in [0, 62]. Test: `tests-internal/math_extras.volt` (19 cases). |
| ~~STDLIB.67~~ | DONE | `time.UnixMs() int` + `UnixUs()` + `MonoMs()` + `MonoUs()`. Pure-volt convenience wrappers that divide Now()/Mono() nanoseconds by 10^6 or 10^3. Required extending INTRINSIC.1 to route time.Now/Mono/Since/Until on same-package call too. Test: `tests-internal/time_ms_us.volt` (9 cases). |
| ~~STDLIB.68~~ | DONE | New `hash/crc32` package with `ChecksumIEEE(s)` — IEEE 802.3 reflected polynomial 0xEDB88320 (default Go variant). Pure volt; computes table values on the fly. Test: `tests-internal/crc32_basic.volt` (8 cross-checked vectors). |
| ~~STDLIB.69~~ | DONE | New `hash/adler32` package with `Checksum(s)` — Adler-32 (RFC 1950, used by zlib/gzip/PNG). a=1, b=0; each byte updates both mod 65521; result is `(b << 16) | a`. Test: `tests-internal/adler32_basic.volt` (7 cross-checked vectors). |
| ~~STDLIB.70~~ | DONE | `unicode.IsHex(b)` + `unicode.IsControl(b)` + `unicode.IsPunct(b)`. ASCII-byte classification — hex digits, control bytes (0x00..0x1F + DEL), and printable-non-alphanumeric. Test: `tests-internal/unicode_extras.volt` (26 cases). |
| ~~STDLIB.71~~ | DONE | `strconv.AppendInt(dst, n, base) []byte` + `strconv.AppendBool(dst, b) []byte`. Go-style Append-XYZ API extending an existing `[]byte`. Delegates to FormatInt/FormatBool then memcpys. Test: `tests-internal/strconv_append.volt` (16 cases). |
| ~~STDLIB.72~~ | DONE | `maps.FilterStringInt(m, keep fun(k, v) bool)` + `FilterStringString` + `MapValuesStringInt(m, fn)`. Higher-order map helpers. Test: `tests-internal/maps_higher.volt` (17 cases). |
| ~~STDLIB.73~~ | DONE | `path/filepath.IsAbs(path) bool` + `Split(path) (dir, file)` + `Clean(path) string`. Unix-only lexical normalization (collapse `//`, remove `.` and trailing `/`, eliminate `..` against preceding segment; rooted `..` drops, unrooted retains). Test: `tests-internal/filepath_extras.volt` (22 cases). |
| ~~STDLIB.74~~ | DONE | `time.Date(year, month, day, hour, minute, second, nanos) Time` — civil-calendar Time constructor delegating to existing `daysFromCivil`. UTC-only in v1. Test: `tests-internal/time_date.volt` (14 cases). |
| ~~STDLIB.75~~ | DONE | `slices.ShuffleInts(s) []int` + `ShuffleStrings(s) []string` — Fisher-Yates shuffle via `crypto/rand.Int`. Returns the same slice for rebinding. Test: `tests-internal/slices_shuffle.volt` (9 cases). |
| ~~STDLIB.76~~ | DONE | `slices.ChooseInt(s) int` + `ChooseString(s) string` + `PartitionInts(s, keep)` + `PartitionStrings(s, keep)`. Random-element pick via rand.Int (empty→zero). Partition returns (matches, rest) by predicate — two-pass exact-size. Test: `tests-internal/slices_choose_partition.volt` (17 cases). |
| ~~STDLIB.77~~ | DONE | `bytes.IndexFunc(s, pred) int` + `LastIndexFunc(s, pred) int` + `TrimFunc(s, pred) []byte`. Predicate-based byte-slice scan helpers parallel to strings.IndexFunc / LastIndexFunc / TrimFunc. Test: `tests-internal/bytes_indexfunc.volt` (14 cases). |
| ~~STDLIB.78~~ | DONE | `sort.MergeInts(a, b) []int` + `sort.MergeStrings(a, b) []string`. Linear merge of two pre-sorted ascending slices into one sorted slice. Stable: equal elements take from `a` first. Private helper `stringLeq` for byte-wise `<=`. Test: `tests-internal/sort_merge.volt` (22 cases). |
| ~~STDLIB.79~~ | DONE | `time.NowString() string` + `NowDate() string` + `(t Time) DateString()`. RFC3339 / YYYY-MM-DD convenience wrappers for log timestamps. Test: `tests-internal/time_nowstring.volt` (12 cases). |
| ~~STDLIB.80~~ | DONE | `os.Stdin() File` + `os.Stdout() File` + `os.Stderr() File`. Pinned File wrappers around fds 0/1/2 — Drop/Close are no-ops at the syscall level so the standard handles stay open. Added `pinned bool` field to File. Test: `tests-internal/os_stdout_stderr.volt` (8 cases). |
| ~~STDLIB.81~~ | DONE | `path/filepath.Rel(base, target) (string, error)`. Computes the relative path; both inputs must share rootedness else error. Uses new `pathSegments(p)` helper. Test: `tests-internal/filepath_rel.volt` (13 cases). |
| ~~STDLIB.82~~ | DONE | `path/filepath.Match(pattern, name) bool`. Glob matching for `*` (no-slash sequence) and `?` (one non-slash byte). Recursive-descent with backtracking. Character classes `[abc]` deferred. Test: `tests-internal/filepath_match.volt` (19 cases). |
| ~~STDLIB.83~~ | DONE | `slices.ToMapStringInt(keys, values)` + `ToMapStringString(keys, values)`. Zip two slices into a map; shorter-prefix on length mismatch; later-key-wins on duplicates. Test: `tests-internal/slices_tomap.volt` (13 cases). Slice-valued maps (`map[string][]int`) deferred — needs box-via-heap-ptr widening similar to the pre-IFACE.7 string-valued map case. |
| ~~BUG.3~~ | DONE | Printer dropped parentheses around BinaryExpr operands when re-emitting, silently changing operator precedence (`(b - 97 + 13) % 26` → `b - 97 + 13 % 26`). New `binaryPrec(op)` helper returns standard precedence ladder; the BinaryExpr arm now parenthesizes a left operand when its op has strictly lower precedence, and a right operand when ≤ (left-assoc rule). Discovered via the STDLIB.34 fmt-roundtrip; fixed in the same tick. No new test — the fmt_smoke.sh roundtrip is the regression guard. |
| ~~STDLIB.28~~ | DONE | `json.EncodePretty(v, indent) string` — indented multi-line JSON encoder built on `bytes.Builder` (Pass 108). Scalars match `Encode`; arrays + objects expand onto separate lines with `indent` per depth level. Custom indent works (`"  "`, `"\t"`, ...). Empty arrays/objects render flat. New private helpers `encodePrettyInto` (recursive driver) + `writeIndentNTimes`. **Move-tracking workaround**: passing the `indent` string to a recursive `encodePrettyInto` would consume the parameter; uses the `"" + indent` BinaryExpr trick (Pass 36 strings.Repeat pattern) at each call site to produce a fresh string and keep the loop's `indent` alive. Test: `tests-internal/json_pretty.volt` (10 cases — 4 scalar passthroughs, empty array, empty object, 3-element array with 2-space indent, 2-key object, nested array-in-object, tab-indent). |
| ~~C.5b~~ | DONE | `scripts/fmt_smoke.sh` — third regression script that catches `volt fmt` corruption. For each `tests-internal/*.volt`: get the original binary's exit code → run `volt fmt` to stdout → build the formatted source → compare exit codes. **135/135 round-trip identically; 9 expected-negative tests still fail to build correctly after format.** Catches the failure mode where a formatter change silently mangles syntax but the unformatted tests keep passing in the old regression. Three-script set (build / run-no-crash / fmt-roundtrip) is now what "the regression is green" means. |
| ~~C.2b18~~ | DONE | LSP hover for stdlib package members. Previously hovering on `strings.HasPrefix` returned nil because `lookupDecl` only searched the local file. Now `hoverResult` falls through to a stdlib lookup when local fails: new `selectorPkgPrefix(text, namePos)` walks backwards from the ident's column on the same line, recognizes the `.` immediately before and the preceding identifier-run as the package qualifier; `lookupStdlibDecl(pkgShort, name)` resolves the path via `stdlibImportPaths`, reads source via `stdlib.Source`, and reuses the existing local `lookupDecl` to render the signature. Three whitebox tests: `TestStdlibHoverStrings` (hover on `strings.HasPrefix` returns its signature), `TestStdlibHoverCryptoSHA256` (multi-segment-path resolution via the `sha256` → `crypto/sha256` map), `TestStdlibHoverBareIdentFallsThrough` (local-ident hover still works — fallthrough doesn't break existing behavior). |
| ~~STDLIB.27~~ | DONE | `bytes.Builder.WriteInt(n int)` — base-10 decimal formatter that appends straight into the builder, avoiding the `strconv.Itoa(n)` + concat round-trip when accumulating mixed text+numbers. Buffers digits LSB-first via 20-byte scratch slice (the max int64 takes 19 digits + optional sign), emits MSB-first. Handles 0 / single-digit / multi-digit / negative / **MinInt** (the only value whose absolute can't be represented as positive int64; algorithm operates on the negative form and emits `-(n%10)` per digit so no overflow). Test: `tests-internal/bytes_writeint.volt` (15 cases: 0/1/9/10/99/100/12345, negative -1/-99/-100, MaxInt/MinInt boundaries, 1M/-1M, plus a mixed WriteString+WriteInt+WriteString roundtrip). |
| ~~LANG.6~~ | DONE | Reading a struct's Copy-primitive field no longer moves the parent. Previously `var x int = r.idx` consumed all of `r` even though `idx int` is a Copy primitive — this required `unpack(r) (int, string)` helper functions to read multiple fields. The checker now tracks struct definitions (new `structs map[string]*ast.StructType` populated from local TypeDecls + AddExternal), and `maybeMoveBareIdent`'s SelectorExpr branch consults `fieldIsCopy(rootType, sel)`: if the field's declared type is a Copy primitive (`isCopyPrimitive(name)`), the move is skipped. Helper `bareStructName(t)` peels &/* wrappers. Movable fields (string, slice, map) still move the parent as before. **AssignStmt order swap**: LHS-read processing now happens BEFORE the RHS evaluation's move side effect — so `hashes[r.idx] = r.hash` works (r.idx is a Copy read at LHS-index time, doesn't get blocked by the subsequent r.hash move). DEMO.2 simplified: the `unpack` helper is gone and the natural `hashes[r.idx] = r.hash` form works. Test: `tests-internal/field_copy_no_move.volt` (6 cases: two consecutive Copy reads, mix of Copy + movable reads, single-statement index-via-Copy-field with hash-write-from-Copy-field). |
| ~~C.2b17~~ | DONE | LSP stdlib-package completion — `pkg.<Tab>` now lists the exported top-level decls (FuncDecl + TypeDecl + ConstDecl, capitalized-first-letter) of any stdlib package. Wired by extending the existing dotted-completion path: if `resolveReceiverType` doesn't recognize `recv` as a local type, fall through to a new `stdlibPackageCompletions(pkgShort, prefix)` that looks up the path via `stdlibImportPaths`, reads source via `stdlib.Source(path)`, parses, and emits CompletionItem entries (kind 3 = Function, 7 = Class, 21 = Constant). Updated `stdlibImportPaths` map to cover the 13-package gap that had accumulated since Pass 70 — now lists all 28 stdlib packages (was missing bytes/hmac/md5/rand/sha1/sha256/base64/math/filepath/slices/sort/unicode/maps). Five whitebox tests in `cmd/volt/lsp_stdlib_completion_test.go`: strings.* returns the well-known names, prefix narrows correctly, sha256.* exercises the multi-segment-path resolution, unknown pkg returns nil for fallthrough, lowercase-leading names are filtered out. |
| ~~DEMO.3~~ | DONE | Word-frequency histogram demo `tests-internal/demo_wordcount.volt` — exercises `strings.Split` (tokenize), `map[string]int` (frequency table), `maps.KeysStringInt` (extract keys), `sort.StringsAsc` (alphabetical), `bytes.Builder` (O(n) histogram-line accumulator). Verifies 7 individual word counts, distinct-word count (10), and a fully-deterministic histogram string of all 10 entries in `word:count\n` form. 9/9. Proves the stdlib's read/map/sort/build chain composes end-to-end in a realistic small-CLI shape. |
| ~~STDLIB.26~~ | DONE | `bytes.Builder` type — O(n) string accumulator. Previously the only way to build a string from bytes was `out = out + chr(b)` in a loop, which is O(n²) because each concat allocates a fresh buffer. Now `bytes.Builder` accumulates into a `[]byte` that grows geometrically (doubles capacity each grow, starting at 16 bytes) and finalizes via a new `syscall.BytesToString(buf, n)` intrinsic that calls runtime `volt_string_from_bytes(ptr, n)` — one heap allocation, one copy, returns the %string. Surface: `NewBuilder()`, `Len()`, `WriteByte(c)`, `WriteString(s)`, `Reset()`, `String()`. Test: `tests-internal/bytes_builder.volt` (14 cases: empty + zero-len, WriteByte single bytes, WriteString append, Reset clears len-but-keeps-buf, reuse after reset, 100-byte build crosses the initial 16-byte capacity multiple times, exact-16-byte fill then grow). Will unlock O(n) rewrites of the hex/base64/sha256 inline string builders if perf becomes a hotspot. |
| ~~DEMO.2~~ | DONE | Concurrency demo `tests-internal/demo_parallel_crypto.volt` — spawn 4 OS threads via `run`, each computes SHA-256 of a distinct input string and posts a `Result{idx, hash}` struct over a `chan1N Result` back to main. Main reads 4 results, indexes them back into a 4-slot string slice via `unpack(r Result) (int, string)` helper (which sidesteps the "field read consumes parent" rule). An `atomic int` done-counter is incremented after each write; main spin-waits on `done.Read() < 4` to confirm all workers finished. Verifies four canonical SHA-256 hex digests across the indexed output slice. Exercises `run` + `chan1N struct` + `atomic int` + crypto + struct-in-channel + multi-return unpacking, proving the concurrency surface composes with the rest of the stdlib. 5/5. |
| ~~DEMO.1~~ | DONE | End-to-end integration test `tests-internal/demo_file_hash.volt` — composes five stdlib packages in one run: write a pangram to `/tmp` via `os.WriteFile`, read it back with `os.ReadFile`, compute `sha256.SumHex` (checks against the canonical RFC vector `d7a8fbb3...e592`), compute `hmac.Sum256Hex("secret-key", ...)` (Python-cross-checked `affee3b4...a2dc`), base64-encode the raw 32-byte HMAC tag (length contract: 44 chars with `=` padding), decode the base64, hex-encode the decoded bytes, verify it round-trips back to the original HMAC tag, then `os.Remove` + `os.Exists` cleanup. 9/9. Proves the os/crypto/encoding surfaces compose cleanly. |
| ~~STDLIB.25~~ | DONE | `os.ReadFile(path) (string, error)` + `os.WriteFile(path, data, mode) error` — the convenience pair that nearly every program needs. Mirrors Go's `os.ReadFile` / `os.WriteFile`. ReadFile = Open + Read + Close (always closes, even when Read errors). WriteFile = Open(O_WRONLY\|O_CREAT\|O_TRUNC=577, mode) + WriteAll + Close. No new runtime/syscall surface — both compose existing primitives. Test: `tests-internal/os_readwrite_file.volt` (12 cases — fresh write + exists + read-round-trip + length check, overwrite truncates, empty content, ReadFile on missing path errors with empty-string post-condition). |
| ~~STDLIB.24~~ | DONE | `os.Remove(path) int` — deletes file or empty directory, mirroring Go's `os.Remove`. Runtime `volt_remove` tries `unlinkat(AT_FDCWD, path, 0)` first (file path); on EISDIR (-21) retries with `AT_REMOVEDIR` (0x200) (empty-dir path). Non-empty directories fail with -39 (ENOTEMPTY). Reuses the already-defined `SYS_UNLINKAT` constants from the file-write path. Syscall package gets `Remove(path) int`; codegen `emitSyscallRemove` mirrors the Mkdir shape. Test: `tests-internal/os_remove.volt` (11 cases — dir create+remove+vanish round-trip, file create+write+remove+vanish round-trip, /no/such/path errors, non-empty dir errors with ENOTEMPTY, cleanup of inner file then now-empty dir). |
| ~~STDLIB.23~~ | DONE | `os.Mkdir(path, mode) int` + `os.Exists(path) bool` — directory creation and existence check via two new syscalls. Runtime adds `volt_mkdir` (mkdirat 258/34) and `volt_path_exists` (faccessat 269/48, F_OK), both with the standard NUL-copy-into-scratch-buffer + inline-asm pattern. Syscall package gains `Mkdir(path, mode) int` and `PathExists(path) bool` stubs; codegen dispatches `emitSyscallMkdir` / `emitSyscallPathExists` (the latter widens the runtime's i64 1/0 to LLVM i1). User-facing `os.Mkdir` returns the raw syscall result so callers can distinguish 0 (success) from -EEXIST (-17), -ENOENT (-2), etc. Test: `tests-internal/os_mkdir.volt` (8 cases: /tmp + / pre-exist, /no/such/path doesn't, fresh dir is created, repeat-create returns non-zero, mkdir under non-existent parent fails). |
| ~~STDLIB.22~~ | DONE | `crypto/rand` package — cryptographically-strong randomness via the Linux `getrandom(2)` syscall. `Bytes(n int) string` (n random bytes), `Hex(n int) string` (2n lowercase hex chars). Three-layer wire-up: (a) **runtime** — new `volt_getrandom(buf, n)` in `runtime.c` calls `SYS_GETRANDOM` (318 on amd64, 278 on arm64) with flags=0; returns the actual byte count or -errno. (b) **syscall package** — new `syscall.GetRandom(n) string` intrinsic. (c) **codegen** — `emitSyscallGetRandom` allocates n bytes via `volt_alloc`, calls the runtime helper, clamps negative (error) returns to 0 via `select i1`, packs the {ptr, len} into a fresh `%string`. Test: `tests-internal/crypto_rand.volt` (8 cases: 16-byte + 32-byte length contracts, n=0 + n=-5 edge cases, independence of two 32-byte calls, Hex output length is 2× bytes, Hex output charset is `[0-9a-f]`, two Hex calls differ). |
| ~~STDLIB.21~~ | DONE | `slices` higher-order helpers extended to `[]string` — `FilterStrings(s, keep fun(x string) bool) []string`, `MapStrings(s, fn fun(x string) string) []string`, `AnyString` / `AllString`. Same two-pass exact-size Filter, same vacuous-truth `AllString` on empty. First-class fn-value params over a movable type (string) confirm the closure-arg pipeline doesn't depend on the parameter being a Copy primitive. Test: `tests-internal/slices_higher_str.volt` (15 cases: filter by length predicate + prefix predicate + no-survivor, Map via ToUpper/ToLower round-trip + TrimSpace, AnyString/AllString incl. empty-slice edges, breaking the all-long streak with a 4-char element). |
| ~~STDLIB.20~~ | DONE | `slices` higher-order helpers — `FilterInts(s, keep fun(x int) bool) []int` (two-pass: count survivors, then collect exact-size), `MapInts(s, fn fun(x int) int) []int` (same-size projection), `AnyInt(s, pred) bool` (short-circuits on first match), `AllInt(s, pred) bool` (vacuous truth on empty — matches Go's `slices.All`-style helpers). All four take volt-style first-class function-value params. Test: `tests-internal/slices_higher.volt` (15 cases: even / odd / no-survivor / empty-input Filter, Map with square + double + length checks, Any/All including empty-slice edges). |
| ~~LANG.5~~ | DONE | Range-over-int: `for i := range N` iterates i from 0 to N-1, matching Go 1.22. Negative or zero N runs zero iterations. Bind `_` for the index when only the count matters. Parser already accepted the syntax (range form is type-agnostic at parse time); the codegen widening adds a new `case "i64"` in `emitRangeFor` that calls `emitRangeOverInt`, which lowers to the equivalent C-style `for i := 0; i < N; i++ { body }`. Value bindings are rejected with a helpful error. Test: `tests-internal/range_int.volt` (6 cases: sum 0..9 = 45, N=0 + N=-3 both yield zero iters, dynamic limit from a var, blank-`_` index, nested loops). |
| ~~LANG.4~~ | DONE | `map[string]string` support across the codegen — string-valued maps now work end-to-end. Previously `m["k"] = "v"` would truncate the 16-byte %string to i64 (silent corruption), `m["k"]` returned i64 (type mismatch at IR-link), and `for k, v := range m` over a string-valued map errored at IR-link with `v` typed as i64. Fix: codegen now consults `mapValueIsString(sym)` to dispatch a box-via-heap-ptr path at every map-touch site: emitMapSet heap-allocates a 16-byte %string struct, copies v in, passes `ptrtoint` as the runtime's i64 value slot. emitMapGet `inttoptr`s the i64 back and loads %string (zero-key → empty %string via a small alloca+branch dance, no phi). emitNewMap (literal form `new {"a": "b"}`) does the same boxing at construction time. emitRangeOverMap consults the symbol's AST map type and allocates `alloca %string` (vs `alloca i64`) + double-deref (load i64 → inttoptr → load %string) when iterating string-valued maps. The runtime stays unchanged — it still treats values as opaque i64; the codegen is what makes them mean different things. Companion stdlib widening: `maps` package gained `KeysStringString` / `ValuesStringString` / `ContainsKeyStringString` / `ContainsValueStringString` mirroring the existing `*StringInt` quartet. Test: `tests-internal/maps_string.volt` (20 cases: get by key, missing-key returns "" zero value, overwrite, insert, ContainsKey/Value, Keys + Values sorted-compare against the expected set, post-overwrite values visible). |
| ~~STDLIB.19~~ | DONE | `strings` package — four more commonly-needed helpers: `LastIndex(s, sub)` (largest position; empty-sub returns len(s) per Go), `IndexAny(s, chars)` (first byte from chars; empty-anything returns -1), `SplitN(s, sep, n)` (cap result at n parts; last part holds remainder; n=0→empty, n<0→full Split), `EqualFold(s, t)` (ASCII case-insensitive compare; non-letters compared literally; length-mismatch fails fast). Test: `tests-internal/strings_more.volt` (22 cases: 6 LastIndex incl. empty-sub / no-match / start-position; 5 IndexAny incl. empty / first-of-multiple; 5 SplitN incl. n=0/-1/1 / n>parts / n=2-cap; 6 EqualFold incl. all-letter / mixed / non-letter / empty / different-length). |
| ~~STDLIB.18~~ | DONE | `strconv.ParseBool(s)` (returns `(bool, error)`) and `strconv.FormatBool(b)` (returns `"true"` / `"false"`). ParseBool accepts the Go-compatible forms: `1/t/T/true/TRUE/True` → true; `0/f/F/false/FALSE/False` → false. Anything else (incl. unusual capitalizations like "TrUe") errors with `"strconv: invalid bool literal"` and returns `false`. Test: `tests-internal/strconv_bool.volt` (19 cases: 2 FormatBool, 6 true-forms, 6 false-forms, 3 invalid forms, 2 round-trips). |
| ~~STDLIB.17~~ | DONE | `maps` package — `KeysStringInt(m) []string` / `ValuesStringInt(m) []int` / `ContainsKeyStringInt(m, k) bool` / `ContainsValueStringInt(m, v) bool` for the v1 most-common shape (`map[string]int`). All four iterate via `for k, v := range m`; reading m through range doesn't move it, so the caller's binding stays usable. Test: `tests-internal/maps_basic.volt` (14 cases: Contains in/not-in, Keys + Values + sortedness against the expected set, len=3, empty-map edges). **Codegen gap surfaced**: `for k, v := range m` on `map[string]string` lowers v as i64 instead of %string — type-errors at IR-link. The StringString helpers are deferred until that's fixed; this pass docs the limitation in the package comment. |
| ~~STDLIB.16~~ | DONE | `crypto/hmac` extended with HMAC-MD5 (RFC 2104). `SumMD5(key, msg)` (16-byte tag) + `SumMD5Hex(key, msg)` (32-char lowercase hex). Same ipad/opad construction as Sum1/Sum256 — third inner-hash variant. New `normalizeKeyMD5` (oversized keys collapse via MD5's 16-byte output, then zero-pad to the shared 64-byte block size). Test: `tests-internal/hmac_md5.volt` (5 cases — RFC 2104 Tests #1 + #2, empty-both, short-key, oversized-100-byte key; all 5 Python-cross-checked). HMAC now covers the full crypto trio. |
| ~~STDLIB.15~~ | DONE | `crypto/md5` package — RFC 1321 MD5 in pure volt. `Sum(msg) string` (16-byte binary) + `SumHex(msg) string` (32-char lowercase hex). Same 32-bit-on-int64 emulation as sha1/sha256 (mask32 + rotl32) but the spec is **little-endian** for both block-word reads AND the trailing bit-length encoding (vs SHA's big-endian) — `padded` and `word32le` flip the byte order. Four per-round Boolean functions (`fF`/`fG`/`fH`/`fI`) dispatched via `fnFor(i, ...)`; per-round shift amount `rotS(i)` is a 4-cycle within each 16-round group; message-word index `kIdx(i)` cycles 1→`(5i+1)%16`→`(3i+5)%16`→`(7i)%16` per group; `tConst(i)` returns one of 64 precomputed `floor(abs(sin(i+1)) * 2^32)` values via a flat if-ladder. **MD5 is broken** — kept for cache keys / ETags / content-addressed lookups / legacy compat, not for new collision-resistance work. Test: `tests-internal/md5_basic.volt` (8 cases: RFC 1321 §A.5 full suite — empty, "a", "abc", "message digest", a-z, A-Z+a-z+0-9, 80-digit, plus a 64-byte exact-block boundary; Python-cross-checked). |
| ~~STDLIB.14~~ | DONE | `slices` package — type-specific helpers since volt has no generics yet. Paired `ContainsInt`/`ContainsString`, `IndexInt`/`IndexString` (returns -1 if absent), `ReverseInts`/`ReverseStrings` (in-place + return-for-rebind so callers do `a = slices.ReverseInts(a)`), plus int-only `SumInts` (silent overflow), `MinInts`, `MaxInts` (empty slice returns 0). Read-only helpers leverage the Pass-82 slice-index non-move widening (Contains / Sum / Min / Max take a slice without consuming the caller's binding). Test: `tests-internal/slices_basic.volt` (22 cases: 5 Contains/Index + 3 Sum/Min/Max + 5 empty-slice edges + 4 Reverse incl. odd/even-length, 4 string-variant Contains/Index, 2 ReverseStrings). |
| ~~STDLIB.13~~ | DONE | `crypto/hmac` extended with HMAC-SHA1 (RFC 2202). `Sum1(key, msg)` (20-byte tag), `Sum1Hex(key, msg)` (40-char lowercase hex). Same ipad/opad construction as Sum256 — only the inner hash differs. `normalizeKey` was renamed/split into `normalizeKey256` / `normalizeKey1`: oversized keys collapse via the matching hash (sha1.Sum gives 20 bytes for SHA-1, sha256.Sum gives 32 for SHA-256), then zero-pad to the shared 64-byte block size. New shared `hexDigest(d string) string` factored out of the two Hex helpers. Test: `tests-internal/hmac_sha1.volt` (5 cases — RFC 2202 Tests #1 + #2 + short-key + empty-both + oversized-100-byte-key, Python-cross-checked). |
| ~~STDLIB.12~~ | DONE | `time.ParseRFC3339(s) (Time, error)` — inverse of `Time.Format()`. Accepts the fixed 20-char `YYYY-MM-DDTHH:MM:SSZ` form (UTC only; fractional seconds + explicit offsets deferred). New `daysFromCivil(y, m, d)` (Howard Hinnant's inverse of `civilFromDays`) and `parseDigits(s, off, width)` helper. Validates separator bytes ('-' / 'T' / ':' / 'Z'), digit shape, and numeric ranges (month 1..12, day 1..31, hour 0..23, minute 0..59, second 0..60 for leap-sec tolerance). Test: `tests-internal/time_parse_rfc3339.volt` (21 cases: epoch + Y2024 + Y2100 boundary + pre-epoch round-trips; 5 error paths — wrong length, missing T, non-digit, out-of-range month, plus the zero-Time post-condition on every error). |
| ~~STDLIB.11~~ | DONE | `crypto/sha1` package — FIPS 180-4 SHA-1. `Sum(msg) string` (20-byte binary) + `SumHex(msg) string` (40-char lowercase hex). Same 32-bit-on-int64 emulation as crypto/sha256: `mask32` clamps every step; new `rotl32` (left-rotate) is the SHA-1 mirror of sha256's `rotr32`. 80-round main loop with the four-phase `roundF(t,b,c,d)` (Ch / Parity / Maj / Parity per FIPS §6.1.2) + four `roundK(t)` constants. W expansion uses `rotl32(W[t-3]^W[t-8]^W[t-14]^W[t-16], 1)`. Useful for git-blob compatibility, HMAC-SHA1, and other legacy formats (don't use for new collision-resistance work — that's what crypto/sha256 is for). Test: `tests-internal/sha1_basic.volt` (6 FIPS / Python-cross-checked vectors: empty, "abc", §A.2 long, "hello world", 64-byte exact block, 56-byte padding-spillover). |
| ~~STDLIB.10~~ | DONE | `crypto/hmac` package — HMAC-SHA256 message authentication (RFC 2104). `Sum256(key, msg)` (32-byte binary tag), `Sum256Hex(key, msg)` (64-char lowercase hex). Implements the standard ipad/opad construction: oversized keys (>64 bytes) are hashed down via `crypto/sha256.Sum` then zero-padded to block size; ipad/opad use 0x36 / 0x5C XORed across the normalized key. First stdlib package to depend on another stdlib package (sha256). Test: `tests-internal/hmac_basic.volt` (5 cases: RFC 4231 vectors #1+#2, single-byte key, empty key+msg, oversized 100-byte key — Python-cross-checked). |
| ~~STDLIB.9~~ | DONE | `crypto/sha256` package — FIPS 180-4 SHA-256. `Sum(msg) string` (32-byte binary digest), `SumHex(msg) string` (64-char lowercase hex). Pure-volt implementation: 32-bit ops emulated on signed-int64 via `& mask32` (0xFFFFFFFF) after every arithmetic step; `rotr32`/`shr32` synthesize the unsigned rotate/shift the spec needs (volt's `>>` is arithmetic); `bigSigma0/1`, `smlSigma0/1`, `ch`, `maj` per FIPS §4.1.2. `kRound(i)` returns the per-round constant via a long if-ladder (no package-level array literal). Padding follows §5.1.1 — append 0x80, zero-pad to 56 mod 64, then 8-byte big-endian bit-length. Test: `tests-internal/sha256_basic.volt` — 6 NIST/Python-cross-checked vectors (empty, "abc", 56-byte block-boundary, 64-byte exact-block, "hello world", and the §C.2 56-char prefix). |
| ~~STDLIB.8~~ | DONE | `strings` package extensions — `Repeat(s, n)` (returns `""` for n ≤ 0; empty `s` is a noop), `TrimPrefix(s, prefix)` / `TrimSuffix(s, suffix)` (return s unchanged if the affix doesn't match; uses existing `HasPrefix`/`HasSuffix`), `Trim(s, cutset)` (strips any byte in cutset from both ends; empty cutset returns s unchanged). New private helper `containsByte(s, b)`. Test: `tests-internal/strings_extra.volt` (23 cases: 6 Repeat incl. n=0/-1/empty, 6 TrimPrefix incl. empty/shorter-than-prefix, 5 TrimSuffix, 6 Trim incl. empty cutset). |
| ~~STDLIB.7~~ | DONE | `math` package — integer-focused numeric helpers. Constants `MaxInt`/`MinInt` (bounds of signed int64; `MinInt` expressed as `-MaxInt - 1` to dodge the lexer's int64 overflow on the positive literal). Functions `AbsInt(x)` (returns MinInt unchanged on overflow), `MinInt2(a,b)`/`MaxInt2(a,b)`/`ClampInt(v,lo,hi)`, `PowInt(base,exp)` (exponentiation by squaring; non-positive exp → 1), `IsqrtFloor(x)` (Newton-iteration integer sqrt; negative → 0), `GcdInt(a,b)` (Euclidean with absolute-value pre-norm), `ModInt(a,m)` (always-non-negative remainder; m≤0 → 0). Float-only (Sqrt/Pi/Sin) deferred — runtime has no float intrinsics yet. Test: `tests-internal/math_int.volt` (35 cases across all 7 functions + MinInt overflow contract). |
| ~~STDLIB.6~~ | DONE | `sort` package — `IntsAsc(s) []int`, `StringsAsc(s) []string` (insertion sort in-place; return the slice so callers can rebind across the ownership move: `a = sort.IntsAsc(a)`). `IntsAreSorted` / `StringsAreSorted` predicates. **Checker widening landed alongside**: indexing any slice (`s[i]`) no longer moves the slice handle — matches Go's "header copy" semantics. Previously only `string[i]` and `[]Copy[i]` were exceptions; now all `[]T[i]` are non-moving on pureIndex shape. New helper `isSliceType`. Test: `tests-internal/sort_basic.volt` (10 cases: 5-element random, already-sorted, reverse-sorted, 20-element with duplicates, single, empty, string lexicographic, prefix-shorter-wins, AreSorted true + false). |
| ~~STDLIB.5~~ | DONE | `unicode` package — byte-level (ASCII) classification + case folding. `IsDigit` ('0'..'9'), `IsLower` ('a'..'z'), `IsUpper` ('A'..'Z'), `IsLetter` (either case), `IsAlpha` (alias), `IsAlphanumeric`, `IsSpace` (' '/'\t'/'\n'/'\r'/'\v'/'\f'), `IsPrint` (0x20..0x7E — newline / DEL excluded per Go), `ToUpper(b)` / `ToLower(b)` (±32 offset; non-letter pass-through). Volt has no rune type so the surface is byte-focused — adequate for parsers, JSON, strconv. Test: `tests-internal/unicode_basic.volt` (29 cases across all 9 predicates + both mappers, with boundary chars '/'/':' before/after digit range, '['/'`' adjacent to letter ranges). |
| ~~STDLIB.4~~ | DONE | `path/filepath` package — Unix path-manipulation utilities. `Base(path)` (last element, "/" or "."), `Dir(path)` (everything before the last element), `Ext(path)` (leading-dot extension, "" if no dot in basename), `Join(a, b)` (two-arg with single-slash normalization — collapses multiple trailing slashes on `a` and leading slashes on `b`). `slash` = 47, `dot` = 46 module-level consts. Test: `tests-internal/path_filepath.volt` (24 cases: 6 Base, 6 Dir, 5 Ext, 7 Join; covers root, empty, trailing-slash, hidden files, dot-in-dirname). |
| ~~STDLIB.3~~ | DONE | `encoding/base64` package — standard base64 (RFC 4648 §4) with `=` padding. `EncodeToString(src string) string` (3-byte chunks → 4 base64 chars; partial trailing groups pad with `=`/`==`), `DecodeString(src string) (string, error)` (errors on non-multiple-of-4 length, over-padding, bad char, stray-padding mid-group). Pure volt over bitwise/shift ops; uses the same byte-mask trick as encoding/hex (`& 255`) to dodge sign-extension. Test: `tests-internal/encoding_base64.volt` (15 cases: empty + RFC §10 vectors `f`/`fo`/`foo`/`foob`/`fooba`/`foobar`, high-bit-byte encode `\xff`/`\xab\xcd`, decode + roundtrip a binary payload, odd-length + bad-char error paths; 15/15). |
| ~~STDLIB.2~~ | DONE | `encoding/hex` package — `EncodeToString(src string) string` (bytes → 2-char lowercase hex per byte) and `DecodeString(src string) (string, error)` (hex → bytes, errors on odd length / non-hex char). Pure volt over the new bitwise/shift ops. **Two adjacent landings**: (a) **Lexer extension** — `\xNN` hex-escape support in double-quoted string literals (`"\x00\x01\x02"` → 3 bytes); added `hexNibble` helper + a new branch in the escape switch; bad/short escapes yield `Illegal` tokens with helpful messages. Matches Go syntax. (b) **Sign-extension fix in hex encode** — `src[i]` returns the byte sign-extended to `int`, so `0xff` was treated as `-1` and `b >> 4` (arithmetic) gave `-1` → wrong hex char. Fixed by masking: `var b int = src[i] & 255` before nibble split. Test: `tests-internal/encoding_hex.volt` (10 cases: empty/low-bytes/ASCII/high-bit-bytes encode, decode + mixed-case + roundtrip, odd-length + non-hex error paths; 10/10). |
| ~~CLI.2~~ | DONE | `volt env` subcommand — diagnostic dump of the toolchain state. Lists volt version, host arch (via go's `runtime.GOARCH`), default `--target`, paths to `clang` and `mold` (or "not found"), the `~/.volt/pkg` cache dir + entry count, the stdlib package list, and (when `./volt.mod` exists) the parsed module/version/require/replace summary. Useful for "what does volt see right now" when debugging build issues. |
| ~~CLI.1~~ | DONE | `volt version` / `--version` / `-v` subcommand — prints volt's release tag (`voltVersion` const) + the host `clang --version` first line. Rewrote `usage()` (also accessible via `help` / `--help` / `-h`) into a categorized listing covering all subcommands and flags actually wired up today: build/run/test, fmt/doc/dump-ir/dump-tokens, mod {init,get,tidy,verify}, lsp, version, help. Replaces the stale "build, run, dump-ir, dump-tokens" stub. |
| ~~C.2b14~~ | DONE | LSP `textDocument/codeAction` — first quick fix: "Remove unused declaration". `codeActionProvider: {codeActionKinds: ["quickfix"]}` capability; handler reads the editor-provided diagnostics, filters to those whose message matches `"declared but not used"`, and emits a `CodeAction{title, kind: "quickfix", diagnostics, edit: {changes: {uri: [TextEdit deleting the whole line]}}}`. New `wholeLineRange(text, lineNo)` helper computes the LSP Range covering one source line + its trailing newline. Smoke-tested: a buffer with `var unused int = 42` produces the diagnostic, then `textDocument/codeAction` returns "Remove unused declaration" with an edit covering L3:C0..L4:C0. |
| ~~C.2b15~~ | DONE | LSP `textDocument/documentHighlight` — when the cursor sits on an identifier, the editor underlines / boxes every other occurrence of that name in the same file. `documentHighlightProvider: true` capability; handler reuses `identAt` + `collectIdentPositions` (same as the workspace refs walker, but restricted to the open buffer). Returns LSP `DocumentHighlight{range, kind: 1}` entries — kind 1 = Text (default). Smoke-tested: cursor on `greet` in a file with one decl + two call sites returns 3 highlights at correct positions. |
| ~~C.2b16~~ | DONE | Codegen-level diagnostics — all six sub-items landed (Passes 67-72): UTF-16 code-unit math in `offsetForPos`, method completion via receiver type, remove-unused-import diagnostic + quick fix, add-missing-import diagnostic + quick fix, typo-in-import-path diagnostic + quick fix (Levenshtein), scope-aware references for locals. |
| ~~C.3a~~ | DONE | DWARF emission: `!DICompileUnit` + `!DIFile` + per-function `!DISubprogram` + a shared `!DILocation` per function used to satisfy LLVM's "inlinable call must carry !dbg" rule. Each `define` line gets `!dbg !<sp>` and a post-pass over the function body rewrites every `call` instruction to append `, !dbg !<loc>` (with the leading comma — clang's format). New `(*Emitter).SetSourceFile` threads the source path through the driver. Tested manually with gdb: `info functions` lists volt-side funcs with `File strconv.volt:LINE`, `break strconv_pkg.volt:9` hits inside `main () at strconv_pkg.volt:9`. No CI test (gdb output format is hard to assert against). |
| ~~C.3b~~ | DONE | Per-statement `!dbg` attachment — line-stepping (`n`/`s`) in gdb now follows volt source lines. `emitStmt` drops a `  ; .line N\n` comment at the head of each statement; the post-pass scans those markers and attaches `, !dbg !<line-loc>` to subsequent calls. New `Emitter.locForLine(spIdx, line)` lazily allocates a DILocation per (function, line) pair, all emitted in the module's DI tail. Verified manually: `break strconv_pkg.volt:14`, then `n; bt; n; bt; n; bt` advances `main () at strconv_pkg.volt:14 → :15 → :16`. |
| ~~C.3c~~ | DONE | `!DILocalVariable` + `llvm.dbg.declare` so `info locals` / `print x` work in gdb. Each parameter (with `arg:` index 1+) and each local var gets a fresh `!DILocalVariable` allocated from a 500000+ id pool; immediately after the alloca+store we emit `call void @llvm.dbg.declare(metadata ptr %x.addr, metadata !N, metadata !DIExpression())`. Existing per-statement `!dbg` rewriter already attaches a `!dbg` to the dbg.declare call so LLVM accepts it. Type metadata is a single shared `!250 = !DIBasicType("int", 64, signed)` — gdb shows ints correctly, prints strings/pointers as their underlying integer (good enough for v1). Verified manually: `break X.volt:20; info locals` shows `pass = 4`, `a = <ptr>`, etc.; `print pass` returns the int value. **C.3 split**: a + b + c DONE. Phase C debugger track complete for v1. |
| ~~C.3d~~ | DONE | Per-LLVM-type DI metadata. New `Emitter.dbgTypeFor(llT) int` lazily allocates a metadata id (700000+ for primitives, 800000+ for composites) and caches by LLVM type string. `dbgTypeEmit` writes the literal: distinct `DIBasicType` for `i1`/`i8`/`i16`/`i32`/`i64`/`double`/`float`/`ptr`; `DICompositeType` for `%string` (ptr+len) and `%slice` (ptr+len+cap), each with proper `DIDerivedType` members; fallback to unsigned int64 for unknown named types. `dbgVar` carries the LLVM type to resolve at tail-emit time. The tail loop iterates until the type set is stable, since emitting a composite calls `dbgTypeFor` recursively for member types. Verified manually: `print a` (where a is `var a string = "0"`) shows `{ptr = 0x… "0-", len = 1}` with gdb auto-deref of the byte pointer. `ptype a` reports `struct string`. **Phase C debugger track**: a + b + c + d all DONE. |
| ~~C.4a~~ | DONE | Minimal `testing` stdlib package: `T` struct (name/failed/fatal), `Error`/`Errorf`/`Fatal`/`Failed`/`Name` methods, `Run(name, fn)` harness that runs one test and prints PASS/FAIL. Tests still declare their own `main` that calls `testing.Run` per test (no auto-discovery yet). Test: `tests-internal/testing_basic.volt` — 4 tests, 2 intentional fails, summary line. |
| ~~C.4b1~~ | DONE | `volt test <file>` CLI: auto-discovers BOTH legacy `fun TestX() int` and harness `fun TestX(t *T)` functions, synthesizes a wrapper `main` that runs each (legacy: directly; harness: via `testing.Run`), inserts `import "log"` / `import "testing"` as needed. Files with their own `main` are run as-is so user-orchestrated tests still work. `buildAndRun` defaults `buildTarget` and treats exit-42 as pass (matches tests-internal convention). Two-line synthesis goes through a tempfile so the original source is untouched. Tests verified with three /tmp test files (harness, legacy, intentional-fail) — exit 0 on pass, exit 1 on any failure. |
| ~~C.4b2~~ | DONE | Subtests via `(t *T).Run(subname, fn fun(t *T)) bool`. Each subtest gets a fresh T with hierarchical name "parent/sub" and prints an indented `    --- PASS: parent/sub`. Subtest failure propagates to the parent. Table-driven helpers fall out naturally (caller loops over a slice of cases, calls t.Run per case). Test: `tests-internal/testing_subtests.volt` (3-passing arithmetic group + 1-passing/1-failing mixed group). |
| ~~C.4b3~~ | DONE | Benchmarks: `B` type (name + N + nsElapsed) + `BenchTimeNs()` budget (~1 sec default) + `RunBenchmark(name, fn)` calibrator (ramps N by 10× up to a 10B-iter ceiling, reports `ps/op` since ns/op underflows for sub-ns ops). `volt test` auto-discovers `fun BenchmarkX(b *B)` alongside Tests; synthesized main calls `testing.RunBenchmark` per benchmark. Test: `tests-internal/testing_bench_type.volt` (B type smoke — full RunBenchmark loop is ~1 sec/bench, too slow for the regression suite; exercised via `/tmp/bench_test.volt` manually). |
| ~~C.4b4~~ | DONE | `-race` flag — sync-level + heap-data race-detector instrumentation. Pass 746: codegen wired to emit `volt_race_acquire`/`volt_race_release` around every channel send/recv (legacy + `read(ch)`/`write(ch,v)`/`ch <- v`), mutex/rwmutex guard acquire/release, and atomic Read/Write/Add/CompSwap. Main() gains a leading `volt_race_enable()` so the detector turns on at startup. **Pass 747 polish**: `volt_race_read/write` calls now wrap (1) slice element loads and stores, (2) heap-resident struct field reads (auto-deref load), and (3) heap-resident struct field writes (the nested-target path + the direct path when baseAddr is a borrowed pointer rather than a stack alloca). Stack-local struct field writes are skipped because volt's borrow checker prevents stack locals from crossing thread boundaries. Test `race_detector_clean.volt`: 2 threads × 100 mutex-protected bumps + `-race` → no warnings, exit 42. ~~Parallel test runner: `testing.RunParallel([]NamedTest{...})` dispatches each test on its own OS thread + waitgroup; `(t *T).Parallel()` is a marker field (today only used informationally; future passes may auto-dispatch based on it).~~ DONE (Pass 78). |
| ~~C.5~~ | DONE | Formatter (`volt fmt`) widened to cover the whole language surface. `formatType` now handles `atomic`/`mutex`/`rwmutex`/`waitgroup`/`once`/`fun(...)R` types and emits chan direction + multiplicity (`chan11 read T` etc.). `formatExpr` gained `FloatLit` and `FuncLit` (inline closure body via new `formatStmtInline` helper). `formatSimpleStmt` now covers `MultiVarStmt` (`v, ok := f()`) and `MultiAssignStmt` so `if init; cond` and `for init; cond; post` round-trip. `printIfStmt` re-emits the optional `Init` clause that was silently dropped. `printForStmt` now also emits the range form (`for k, v := range expr`). Interface method signatures (both inline type literals and `type X interface { ... }` declarations) round-trip with parameter + return types. Format-then-build roundtrip on tests-internal: 99/99 real tests survive (only the 9 expected-negative tests fail, and they fail with their own intentional errors). Still skips `docs/design/*.volt` per project rule. |
| ~~D.1~~ | DONE | Race detector — happens-before tracker. Pass 746: real implementation lands in `internal/runtime/asm/runtime.c`. Per-thread vector clocks (`VOLT_RACE_MAX_THREADS=64`, indexed by SYS_GETTID → dense-slot mapping registered on first use), striped-mutex hash tables for sync-location clocks (`VOLT_RACE_SYNC_BUCKETS=1024`, 16 stripes) and per-address memory metadata (`VOLT_RACE_MEM_BUCKETS=16384`, 64 stripes). `volt_race_acquire(ptr)` merges the published vclock at ptr into the calling thread; `volt_race_release(ptr)` bumps the caller's epoch and publishes its full vclock at ptr. `volt_race_read/write(ptr, size)` check the last-writer/last-reader epoch against the caller's view and emit `WARNING: DATA RACE (<kind>)\n  addr=0x... t<a>@e<ae> vs t<b>@e<be>` to stderr on a happens-before violation. Detector is OFF by default; codegen turns it on with a single `volt_race_enable()` call at the head of main when `-race` is set. Test `race_detector_clean.volt` exercises the path (2 threads + mutex + chan, no warnings expected, exit 42). |
| ~~D.2~~ | DONE | Lock-free MPMC channel queue. Pass 746: Vyukov-style bounded MPMC ring lands in `internal/runtime/asm/runtime.c` as `chan_lf_t`. Each slot carries a sequence number; producers/consumers race on `enq_pos`/`deq_pos` via CAS, then read/write the slot and bump its sequence. Capacity rounded up to power-of-2 for the wrap mask. Closed flag and drain semantics match the mutex backend (recv returns 0 on closed+empty with destination zeroed). Channel header gains a leading `i32 backend` discriminator; `volt_chan_send/recv/close/try_send/try_recv` dispatch to the lockfree path when set. Codegen emits a one-shot `volt_channels_set_lockfree(1)` at the head of main when `--channels lockfree` is on; the flag is process-global (not per-channel). cap=0 unbuffered channels keep the mutex backend regardless (no useful lockfree rendezvous). Test `chan_lockfree_mpmc.volt`: 3 producers × 100 + 2 consumers × 150 → exact-sum check (614,850). Re-tested all 9 buffered-chan regression tests against `--channels lockfree`: every one passes with identical semantics to the mutex backend. `-race` + `--channels lockfree` composes correctly. |
| D.3 | READY | macOS port: different syscall ABI; amd64+arm64. |
| D.5 | READY | Self-hosting compiler (volt-in-volt). Long-term. |
| ~~UX.1~~ | DONE | Friendly type-mismatch errors at `var` declarations. Previously `var x int = "literal"` produced a cryptic clang LLVM IR error (`'%t1' defined with type '%string' but expected 'i64'`); now `emitVar` runs a pre-store check via new `typeMismatchMessage(astType, valLLVM, targetLLVM)` helper and surfaces `<pos>: cannot assign string value to int variable` at the volt source position. The check is intentionally narrow: same-type and both-numeric paths return "" (so convertInt still handles widening); target=`ptr` returns "" (upstream boxing paths for error/any/interface/`*T`-Box/closures run first and trust their result). Negative test: `tests-internal/type_mismatch_reject.volt` (added to the 10-test expected-negative set in CLAUDE.md). |
| ~~UX.2~~ | DONE | Friendly type-mismatch errors at assignments (`x = "string"` where x is int) and field assignments (`p.X = "string"` where X is int). Same shape as UX.1 — `emitIdentAssign` checks `typeMismatchMessage(sym.AstType, val.Type, sym.Type)` before the store; `emitFieldAssign` checks against the field's declared AST type. Borrow assignments (`*p = v`) skip the check (sym.Elem != "" path). Negative test: `tests-internal/assign_type_mismatch_reject.volt` (expected-negative set grows to 11 in CLAUDE.md). |
| ~~UX.3~~ | DONE | Friendly type-mismatch errors at `ret` and function call arguments. `emitRet` now checks each returned value against the declared `fd.Results` (plumbed through new `funcCtx.retAstTypes` field); the multi-return aggregate loop checks per-slot. `emitCallArg` checks non-borrow params against the param's declared AST type. Helper message reworded from the var-decl-specific "cannot assign … value to … variable" to the universal "type mismatch: cannot use … value where … is expected" so it reads naturally at all four sites (var-decl, assign, field-assign, ret, call-arg). Negative test: `tests-internal/ret_type_mismatch_reject.volt` (expected-negative set grows to 12 in CLAUDE.md). |
| ~~UX.4~~ | DONE | Friendly type-mismatch errors at composite literals, slice index assignments, and map sets. Three more `typeMismatchMessage` sites: `emitNewStruct` + `emitCompositeStruct` check each provided field expr against the declared field AST type; `emitSliceIndexAssign` recovers the slice's element AST type from the source variable's `sym.AstType.(*ast.SliceType).Elem` when the LHS is an ident; `emitMapSet` reads `sym.AstType.(*ast.MapType).Value` for the target. Negative test: `tests-internal/composite_type_mismatch_reject.volt` (expected-negative set grows to 13 in CLAUDE.md). |
| ~~UX.5~~ | DONE | "Did you mean ...?" suggestions for undefined identifiers via Levenshtein edit-distance matching. New `funcCtx.suggestIdentifier(name) string` scans `c.symbols` + `c.e.funcs` for the closest candidate within a tight threshold (≤ 2 edits, or ≤ 1/3 of the longer length for longer names). Wired into the two hot paths: `emitIdent` (`undefined identifier "cuonter" (did you mean "counter"?)`) and `emitIdentAssign` (same suggestion shape on the LHS of `cuonter = 10`). Independent `levenshteinDistance` helper in codegen (duplicated from cmd/volt/lsp.go because internal/ can't import main). Negative test: `tests-internal/ident_typo_suggest_reject.volt` (expected-negative set grows to 14 in CLAUDE.md). |
| ~~UX.6~~ | DONE | "Did you mean ...?" suggestions for missing fields and methods. Refactored `suggestIdentifier` to delegate to a shared `closestName(name, cands []string)` helper; added `(*Emitter).suggestField(typeName, name)` and `(*Emitter).suggestMethod(typeName, name)` which scan `e.structs[t].Fields` and `e.methods[t]` + `e.extMethods[t]` respectively. Wired into the three sites: field-access (`emitFieldExpr`), field-assign (`emitFieldAssign`), and method-call (`emitMethodCall`). Negative test: `tests-internal/field_typo_suggest_reject.volt` (`Point has no field "cuonter" (did you mean "counter"?)`); expected-negative set grows to 15. |
| ~~UX.7~~ | DONE | Friendlier "undefined function" diagnostics: (a) when the name resolves to an in-scope local that just isn't callable, the error now says `cannot call "x" — it has type int, not a function` instead of the misleading "undefined function"; (b) when the name is truly undefined, the `suggestIdentifier` heuristic kicks in to add a "did you mean …?" hint. All three undefined-function sites updated (multi-return path, `run` statement, plain call). Negative test: `tests-internal/call_nonfn_reject.volt` (expected-negative set grows to 16). |
| ~~UX.8~~ | DONE | Friendly errors for cross-package function typos. `strings.ToUper("x")` used to fall through `emitForeignCall`'s legacy 1-arg-void branch, silently emit `declare void @strings_ToUper(%string)`, then explode with a cryptic clang IR error when the void result was bound to a string variable. Now when the package is registered (`extPkgs[pkgName]` exists) but the symbol isn't found, we error out immediately with `package strings has no function "ToUper" (did you mean "ToUpper"?)` — the suggestion uses the shared `closestName` helper over the package's registered fn names. Negative test: `tests-internal/pkg_func_typo_reject.volt` (expected-negative set grows to 17). |
| ~~UX.9~~ | DONE | Friendly type-mismatch errors in binary expressions. `5 + "hello"` (or any `int op string`) used to fall through `emitBinary`'s harmonize switch and emit `add i64 5, %t1` with mismatched types, producing a cryptic clang IR error. Now after the harmonize block we check `x.Type != y.Type` and bail out with `type mismatch in binary "+": int and string` (using `llvmTypeFriendlyName` to map LLVM types back to user-facing names). Covers all binary ops (arithmetic, comparison, bitwise, logical). Negative test: `tests-internal/binop_type_mismatch_reject.volt` (expected-negative set grows to 18). |
| ~~UX.10~~ | DONE | Did-you-mean suggestions for unknown package imports. `import "strngs"` previously emitted `unknown package "strngs" (try: volt mod get strngs)` which is unhelpful for typos of stdlib packages. New `suggestStdlibImport` walks both `stdlib.List()` (full paths) and `stdlib.ShortNameToPath()` keys (short names) and returns the closest within the threshold; the unknown-import error now reads `unknown package "strngs" (did you mean "strings"? — or try: volt mod get strngs)` when a close match exists. Whitebox tests: `cmd/volt/import_typo_test.go` (full-path typo `strngs` → `strings`, short-name typo `hxx` → `hex`, unrelated `wxyzabc` → no suggestion, exact `strings` → not suggested as itself). |
| ~~UX.11~~ | DONE | Readable diagnostics for field access / assign on non-struct receivers, plus prettier pkg-qualified arity errors. (a) `x.field` (x is int) used to read `field is not a struct (type i64)` — looked like `field` itself was the type; now reads `cannot read field "field" on "x" — it has type int, not a struct`. (b) `x.field = 10` analogously now reads `cannot assign field "field" on "x" — it has type int, not a struct` instead of `cannot assign field on non-struct type i64`. (c) Cross-package arity errors used the mangled symbol (`strings_ToUpper takes 1 arg(s), got 2`); now use the source-level `pkg.Func` form (`strings.ToUpper takes 1 arg(s), got 2`). Negative test: `tests-internal/field_on_nonstruct_reject.volt` (expected-negative set grows to 19). |
| ~~UX.12~~ | DONE | `volt doc <typo>` now gives a friendly did-you-mean suggestion instead of a Go runtime file-not-found error. `volt doc strngs` previously emitted `open strngs: no such file or directory`; now reads `volt doc: no stdlib package "strngs" (did you mean "strings"?)`. When the argument has no `.volt` extension and no `/`, the doc CLI treats it as a stdlib package and runs through `suggestStdlibImport` (added in UX.10); unrelated names emit `no stdlib package "wxyzabc" (and no file wxyzabc)`. Reuses the existing `suggestStdlibImport` helper and its tests — no new test surface needed. |
| ~~UX.13~~ | DONE | **Compiler bug**: self-referencing const (`const X int = X + 1`) caused the Go runtime to blow the stack with `goroutine stack exceeds 1000000000-byte limit` from infinite recursion in `emitIdent`'s const-substitution path. Added a per-funcCtx `expandingConsts map[string]bool` cycle guard that flips on entry and clears on exit (via `defer delete`); a recursive lookup of a const that's already being expanded surfaces `constant "X" is self-referential (depends on its own value)`. Catches both direct cycles and mutual cycles (`X = Y + 1; Y = X + 1`). Negative test: `tests-internal/const_cycle_reject.volt` (expected-negative set grows to 20). |
| ~~UX.14~~ | DONE | Friendly errors for recursive struct definitions by value. `type Node struct { val int; next Node }` used to produce the cryptic clang IR error `identified structure type 'Node' is recursive` from the first `define` line. New `Emitter.findStructValueCycle(file)` runs a pre-emission DFS over every struct's `NamedType` fields (pointer / borrow fields skip — they break the cycle as expected); a back-edge in the DFS stack means a by-value cycle. Surfaces `struct "Node" contains a field of type "Node" by value — recursive structs need *T (pointer) or &T (borrow) indirection` at the type-decl position. Catches direct cycles (`Node` → `Node`) and mutual cycles (`A.b: B`, `B.a: A`); pointer-mediated lists still compile fine. Negative test: `tests-internal/recursive_struct_reject.volt` (expected-negative set grows to 21). |
| ~~UX.15~~ | DONE | Duplicate-declaration detection (funcs, methods, consts, types, struct fields). Three correctness bugs found while exploring crash inputs: (a) duplicate top-level function silently lasted to clang as `invalid redefinition of function 'pkg_X'`; (b) duplicate `const X = 1; const X = 2` silently overrode — second value wins, no error; (c) duplicate struct field `x int; x int` silently kept only the second (field-index map overwrote on insert). Fixed at registration in `Emit`: four `seen` maps (funcs, methods-per-type, consts, types) plus a per-struct field check error out on the *second* sighting and cite the position of the first. Negative test: `tests-internal/dup_field_reject.volt` (the struct branch — exercises the same `Emit` pre-pass that gates the others). Expected-negative set grows to 22. |
| ~~UX.16~~ | DONE | Friendly error for `package main` missing a `fun main()`. Previously the missing-entry-point only surfaced as a mold linker error: `mold: error: undefined symbol: main`. `resolveAndCompile` now scans the root file's top-level FuncDecls after parsing and emits `volt: <file>: package main has no `fun main()` — add an entry-point or change the package name` when none is found. Reusable from `volt build`, `volt run`, and `volt test`'s synthesis path — they all flow through `resolveAndCompile`. The existing `tests-internal/has_tests.volt` negative test (no main; `volt test` is supposed to synthesize one before building, but a direct `volt build` doesn't) now produces the friendly message rather than the linker error. |
| ~~UX.17~~ | DONE | **Latent bug**: `fun main() string` used to produce the misleading `type mismatch: cannot use string value where string is expected` because main is force-lowered to `i64` (for the kernel's exit-code ABI) but the AST kept the declared `string` return type — both sides said "string" in the error wording. Fixed by hoisting main-signature validation to `resolveAndCompile`: rejects parameters (`fun main(x int)` → "use os.Argc / os.ArgAt to read argv"), rejects multi-return, and rejects non-`int` returns ("`fun main` must return `int` (the exit code), not another type"). Allow `fun main()` (no return) — already lowers to `ret i64 0`. Negative test: `tests-internal/main_signature_reject.volt`; expected-negative set grows to 23. |
| ~~UX.18~~ | DONE | Unknown-type detection in var declarations. `var x Foo = ...` (where `Foo` isn't defined) used to silently lower to LLVM `void`, then surface only as a misleading downstream "cannot use int value where Foo is expected" — wording suggested `Foo` exists. New `Emitter.validateNamedType` recursively walks pointer / borrow / slice / map / chan / sync wrappers looking for unresolved `NamedType` references, surfacing `unknown type "Foo"` at the type's position; close matches against `e.structs` + `e.interfaces` get a did-you-mean hint. Wired into `emitVar` (the highest-traffic site). Negative test: `tests-internal/unknown_type_reject.volt` (`Pont` typo for `Point`); expected-negative set grows to 24. |
| ~~UX.19~~ | DONE | Extended unknown-type detection to function signatures and struct field declarations. New post-registration validation pass in `Emit` walks every `FuncDecl`'s params + results + receiver, plus every `TypeDecl` `StructType`'s fields, and calls `validateNamedType` on each — so typos in `fun f(x Foo) Bar` or `type T struct { ref Pont }` are rejected at the declaration site rather than at first use. Runs after all structs/interfaces are registered so cross-references resolve correctly. Negative test: `tests-internal/unknown_type_param_reject.volt`; expected-negative set grows to 25. |
| ~~UX.20~~ | DONE | Interface-not-satisfied errors now list the missing method names. Previously `type Cat does not implement interface Animal (missing one of its methods)` left the user to guess which. `emitIfaceBox` now walks `interfaceDecls[name].Methods` and reports the names that have no matching entry in either `e.methods[t]` or `e.extMethods[t]` (so both local + cross-package method registrations count). Falls back to the old phrasing only if the diff comes up empty (shouldn't happen but defensive). Negative test: `tests-internal/iface_missing_method_reject.volt` (Cat declares no methods; Animal needs Speak + Name); expected-negative set grows to 26. |
| ~~UX.21~~ | DONE | Signature-strict interface impl matching. Previously a method with the right NAME but wrong signature (different param types or return type) counted as implementing the interface — so `fun (f FakeWriter) Write(s string) string` was accepted for `interface { Write(s string) int }` and the user only saw a confusing downstream call-site error. The interface-impl scan now uses new `methodMatchesIfaceSig(concrete, ift)` to check params + results structurally via `typesStructurallyEqual` (recursive shape compare over NamedType, pointer, borrow, slice, map, chan). `emitIfaceBox`'s diagnostics now distinguish "missing" methods from "signature mismatch" methods; both surface in the error when both happen at once. Negative test: `tests-internal/iface_sig_mismatch_reject.volt`; expected-negative set grows to 27. |
| ~~UX.22~~ | DONE | **Silent-bug fix**: `new T{x: 1, z: 2}` (where `z` isn't a real field of `T`) used to silently accept the bad entry and zero-initialize the real fields the user *meant* to populate — a quiet subtle-bug source. Both `emitCompositeStruct` and `emitNewStruct` now validate every provided key against `info.Index` before building the `provided` map; unknown keys error with `T has no field "z" (did you mean "x"?)` using the existing `suggestField` helper. Negative test: `tests-internal/unknown_field_lit_reject.volt`; expected-negative set grows to 28. |
| ~~UX.23~~ | DONE | **Silent-bug fix**: `new T{x: 1, x: 2}` (same field listed twice in a struct literal) used to silently overwrite — the second value won via the provided-map's last-write-wins. Now both literal-emission paths track `provided` keys for duplicates and error at the second sighting with `field "x" listed twice in Point composite literal`. Catches copy-paste bugs where the user meant to change the field name on the second entry. Negative test: `tests-internal/dup_field_lit_reject.volt`; expected-negative set grows to 29. |
| ~~UX.24~~ | DONE | Duplicate-parameter detection. `fun add(a int, a int) int` used to surface only as `error: redefinition of argument '%a'` from clang when the IR `define i64 @f(i64 %a, i64 %a)` was rejected. Added a per-FuncDecl `paramPos` map in the post-registration validation pass: every named parameter is checked against earlier siblings, with errors citing both positions. Anonymous params (Name == "") skip the check. Negative test: `tests-internal/dup_param_reject.volt`; expected-negative set grows to 30. |
| ~~UX.25~~ | DONE | Duplicate-interface-method detection. `interface { Foo() int; Foo() string }` used to silently accept both — the impl-scan would then match whichever came last (the iteration order of `iface.Methods`), causing surprising "does not implement" errors at use sites. Added a `methodPos` map inside the InterfaceType arm of the TypeDecl registration; second sighting errors with `interface "X" has duplicate method "Foo" (first at ...)`. Negative test: `tests-internal/dup_iface_method_reject.volt`; expected-negative set grows to 31. |
| ~~UX.26~~ | DONE | Duplicate-parameter detection inside interface method signatures. `interface X { Foo(a int, a int) int }` used to be silently accepted (the Pass 154 FuncDecl-paramPos check only covered concrete functions). Now the interface-decl arm of registration also walks each method's `FuncType.Params` for duplicates, citing the offending parameter and the first occurrence: `interface "X" method "Foo" has duplicate parameter "a"`. Negative test: `tests-internal/dup_iface_param_reject.volt`; expected-negative set grows to 32. |
| ~~UX.27~~ | DONE | **Silent-bug fix**: local `var X = 10` declared in a function whose name collides with a top-level `const X = 5` used to *silently still resolve to 5* on every later read, because `emitIdent` consults `c.e.consts` before the local symbol table. So the user's assignment was invisible — the worst kind of correctness bug. `emitVar` now rejects the collision at the declaration site: `local variable "X" collides with top-level constant of the same name — rename one of them`. Negative test: `tests-internal/const_var_collide_reject.volt`; expected-negative set grows to 33. |
| ~~UX.28~~ | DONE | **Silent-bug fix**: `var x; var x` in the same flat scope used to silently overwrite the first symbol's table entry (any drops it registered would never fire, and reads of `x` returned the second value with no diagnostic). New `funcCtx.declaredAt map[string]int` tracks each declaration's scope depth; `emitVar` rejects same-scope redeclaration; `popScope` and `discardDropsAbove` prune entries above the leaving floor so sibling scopes (sequential for-loops, multiple if/else branches each binding the same temp) still get to reuse names. The `for` statement also pushes its own scope around the init+body+post so `for i := 0; ... { } ` followed by `for i := 0; ... { }` remains legal. Negative test: `tests-internal/dup_var_same_scope_reject.volt`; expected-negative set grows to 34. |
| ~~UX.29~~ | DONE | **Silent-bug fix**: `fun add(a int) { var a int = 10; ... }` used to silently overwrite the parameter's symbol-table entry — the caller's argument was lost. Parameters are now registered in `declaredAt` at depth 0 alongside their `c.symbols` entry, so the Pass 158 same-scope-redeclare check catches the var-vs-param collision. Also added a parallel `declaredAtPos` map so the redeclaration error cites the original declaration's position. Negative test: `tests-internal/var_shadows_param_reject.volt`; expected-negative set grows to 35. |
| ~~UX.30~~ | DONE | Duplicate-parameter detection in closure literals. `fun(a int, a int) int { ret a }` used to slip past the volt-level checks (Pass 154 covered only named FuncDecls) and surface only as a downstream IR error or a misread return-type causing a "log.Println takes a string" misfire. `emitFuncLit` now runs the same `paramPos` dup-check at the closure literal site: `closure has duplicate parameter "a" (first at file:L:C)`. Negative test: `tests-internal/dup_closure_param_reject.volt`; expected-negative set grows to 36. |
| ~~UX.31~~ | DONE | Duplicate-name detection in multi-value declarations. `a, a := pair()` used to lower to LLVM IR with two `%a.addr` allocas and surface as the cryptic clang `multiple definition of local value named 'a.addr'`. `emitMultiVar` now does an upfront O(n²) pairwise check on `s.Names`, with `_` ignored — same shape as Go's `_, _ := f()` allowance. Also registers each new name in `declaredAt`/`declaredAtPos` so cross-decl collisions (`var a; a, b := f()`) hit the Pass 158 redeclare check. Negative test: `tests-internal/dup_multivar_reject.volt`; expected-negative set grows to 37. |
| ~~UX.32~~ | DONE | **Silent-bug fix**: a top-level function and constant sharing a name used to silently coexist — calls resolved to the function, `var x = Foo` (value reference) resolved to the constant because `emitIdent` consults consts before funcs. Same name, two answers depending on context. New `crossNs` map in the registration loop tracks every top-level identifier; the function/const/type arms each call `checkCrossNs` and reject the second sighting with `function "Foo" collides with earlier top-level declaration (first at …)`. Negative test: `tests-internal/fn_const_collide_reject.volt`; expected-negative set grows to 38. |
| ~~UX.33~~ | DONE | Friendlier file-read errors at the CLI boundary. `volt build /tmp/missing.volt` used to bubble the raw Go `open /tmp/missing.volt: no such file or directory` to the user. `resolveAndCompile` now branches on `os.IsNotExist` to surface `volt: source file %q does not exist`, falls back to `volt: cannot read %q: %v` for other I/O failures. Also added a `.volt`-extension check: passing a non-volt file produces `volt: %q does not have a .volt extension — expected a volt source file` instead of letting the parser stumble through arbitrary bytes. No expected-negative test added (CLI-layer change). |
| ~~UX.34~~ | DONE | LSP `publishDiagnostics` now also runs `codegen.Emit` (discarding the IR output) so the recent codegen-level validation errors — unknown types, duplicate decls, cross-namespace collisions, recursive struct cycles, signature-strict interface impl checks, etc. — surface in the editor on every didOpen / didChange / didSave. Previously those only fired at `volt build` time, making the LSP feel underpowered for real bugs. Skipped when the parse already produced errors so cascading codegen noise doesn't drown the parse errors. Whitebox tests: `cmd/volt/lsp_codegen_diags_test.go` (unknown-type → diagnostic; duplicate `fun main` → diagnostic). |
| ~~SCOPE.1~~ | DONE | **Semantics fix**: shadowed outer-scope vars now properly restore on scope exit. `var i = 100; for i := 0; ... { } ; ret i` used to return 3 (the for-loop's final inner i overwrote the outer in `c.symbols`) instead of 100. Pass 158 added scope-aware duplicate detection but didn't actually stack/restore the symbol table. Added `funcCtx.shadowed []shadowEntry` save stack: `emitVar` records the prior `symbols[name]` + `declaredAt[name]` + `declaredAtPos[name]` state whenever an outer-scope binding is being shadowed (i.e., prior declaredAt depth < current scopeDepth). `popScope` walks shadowed entries at the leaving depth in reverse-push order and restores; entries for names that were truly new at this scope (no prior binding) are deleted from `c.symbols` (previously they leaked). Positive test: `tests-internal/scope_shadow_restore.volt` (outer i preserved after for-init shadow, nested if-blocks each restore correctly). Buildable test count: 140. |
| ~~SCOPE.2~~ | DONE | Shadow restoration extended to range bindings. Pass 165 fixed the var-declaration path; range bindings (`for i, v := range s`) created `c.symbols` entries directly, bypassing the shadow tracking — so an outer `i` was still being clobbered. Extracted the bind-with-shadow logic into a reusable `c.bindLocal(name, sym, pos)` helper that handles the redeclare check, shadow save, and symbol-table write uniformly. Wired into all 5 range sites (slice / string / int / map-key / map-value) plus `emitVar`. Extended `tests-internal/scope_shadow_restore.volt` to cover the range case: outer `j` preserved across `for j, v := range s`. |
| ~~SCOPE.3~~ | DONE | Shadow restoration extended to the remaining binding sites: multi-value declarations (`a, b := f()` and `v, ok := <-ch`) and mutex/rwmutex guard variables (`var g T = m.Lock()`). Both now flow through `bindLocal` so the unified redeclare check + shadow save applies. With this, every place that introduces a new local name in the symbol table goes through one helper. Regression still green (140 buildable tests, 38 expected-negative). |
| ~~TEST.1~~ | DONE | Unit tests for the codegen-level validation helpers. The `internal/codegen` package had zero direct tests despite recent passes (131-167) adding many critical helpers (`typeMismatchMessage`, `closestName`, `levenshteinDistance`, `typesStructurallyEqual`, `isBuiltinTypeName`, `llvmTypeFriendlyName`). New `internal/codegen/helpers_test.go` covers the pure-function helpers: edit-distance edge cases (empty strings, transpositions), did-you-mean threshold (no false positives for wildly different names, no self-suggestion), type-mismatch message for compatible / mismatched LLVM type pairs, structural equality for primitive / slice / nil shapes, and the LLVM→friendly name mapping. 6 test functions, all green. |
| ~~TEST.2~~ | DONE | Performance benchmarks for `publishDiagnostics`. The Pass 164 LSP integration added a full `codegen.Emit` per diagnostic refresh, which fires on every didOpen / didChange / didSave — i.e., potentially every keystroke. Wanted to confirm we're not introducing a stall. New `cmd/volt/lsp_perf_test.go` has two benchmarks: tiny file (Println + hello world) and medium file (`internal/stdlib/json/json.volt`, ~440 lines). Results on Intel Core Ultra 7 265U: 31µs and 1.5ms respectively. Both well within human typing perception — no caching needed. Benchmark stays as regression-prevention. |
| ~~IFACE.1~~ | DONE | **Semantics bug**: a methodless concrete type used to fail to satisfy a zero-method `interface {}`. Cause: the impl-scan built its candidate-type set from `e.methods` + `e.extMethods` — types that own at least one method — so methodless structs were excluded entirely. Empty interfaces in volt should be trivially implemented by every type (matching Go's `interface{}` behavior). Fix: also include every entry in `e.structs` in the candidate set. Now `var a Any = c` works for `type Cat struct { ... }` with zero methods. Positive test: `tests-internal/empty_iface_satisfy.volt`. Buildable test count: 141. |
| ~~IFACE.2~~ | DONE | **Missing feature**: passing a concrete value to an `any`-typed parameter used to produce a cryptic clang IR mismatch — the var-decl path (`var x any = concrete`) auto-boxed via `emitAnyBox`, but `emitCallArg` had no equivalent. Added an `isAnyType(paramType)` branch to `emitCallArg`: when the param is `any`, the concrete argument flows through `emitAnyBox` (heap-copy + pass-ptr). Already-ptr values pass through. Works for structs, ints, strings, etc. Positive test: `tests-internal/any_call_arg.volt` (Cat + int + string all pass through three `any`-param functions). Buildable test count: 142. |
| ~~IFACE.3~~ | DONE | **Missing feature**: same shape as IFACE.2 for the built-in `error` interface. A concrete struct with `Error() string` passed to an `error`-typed parameter now flows through `emitErrorBox` (data ptr + Error fn vtable), matching the var-decl `var e error = concrete` boxing path. Already-ptr / `%error_box` values pass through unchanged. Positive test: `tests-internal/error_call_arg.volt`. Buildable test count: 143. |
| ~~IFACE.4~~ | DONE | **Missing feature**: auto-boxing at `ret` sites. `fun makeErr() error { ret myErr }` (where myErr is a concrete value) used to fail with the same cryptic clang IR error. The three boxing paths (`emitIfaceBox`, `emitErrorBox`, `emitAnyBox`) are now unified behind a new `c.maybeBoxForInterface(pos, val, targetAst)` helper. `emitRet` calls it once per return value (single-return path + multi-return per-slot loop), so `ret myErr` from an `error`-returning function and `ret 5, myErr` from `(int, error)` both flow through. Positive test: `tests-internal/ret_iface_autobox.volt` (single + multi-return). Buildable test count: 144. |
| ~~REFAC.2~~ | DONE | Consolidated the three interface-boxing branches (error / user-iface / any) in `emitVar` and `emitCallArg` to share the IFACE.4 helper `maybeBoxForInterface`. Before: each site had its own 8-12 line predicate-and-box-and-update block (the var-decl path had three sequential blocks, the call-arg path had three separate if-statements). After: each site is a 5-line single call. Same behavior, but new interface-boxing kinds (e.g. future type-asserting interfaces) only need editing the helper. Regression at end of REFAC.2: still 144 buildable, 38 expected-negative, 0 crashes; all whitebox tests pass. |
| ~~IFACE.5~~ | DONE | **Missing feature**: auto-boxing at struct field stores. `new Result{err: myErr}` (composite literal) and `r.err = myErr` (field assignment) used to fail clang with the same struct-vs-ptr mismatch when the field's declared type was an interface. Wired the IFACE.4 helper `maybeBoxForInterface` into `emitCompositeStruct` / `emitNewStruct` and `emitFieldAssign`. Now the same boxing logic covers all six AST shapes that store into a typed slot: var-decl, ident-assign (covered by var-decl path implicitly via aliasing), field-lit, field-assign, call-arg, ret. Positive test: `tests-internal/iface_field_autobox.volt` (composite literal + field assignment, both with concrete MyErr → error). Buildable test count: 145. |
| ~~IFACE.6~~ | DONE | **Missing feature**: auto-boxing at slice element + map value stores. `var s []error; s[0] = myErr` and `var m map[string]error; m["k"] = myErr` rounded out the remaining store sites. Wired `maybeBoxForInterface` into `emitSliceIndexAssign` (using the AST element type recovered from the slice symbol's `AstType`) and `emitMapSet` (using `mt.Value`). The map runtime uses an i64 value slot, so a new ptr→i64 branch in emitMapSet's coercion casts the boxed ptr via `ptrtoint` before passing it. The read path doesn't auto-unbox yet — assignment-only smoke test for now. Positive test: `tests-internal/iface_slice_map_autobox.volt`. Buildable test count: 146. |
| ~~IFACE.7~~ | DONE | **Missing feature**: auto-unboxing on map read for interface-typed values. `m["k"] == nil` on a `map[string]error` used to compare i64 against ptr (the runtime stores values as i64, the nil-literal lowers to ptr). `emitMapGet` now detects interface-typed value (error / any / user-iface via `c.userInterfaceName(mt.Value)`) and casts the i64 result back to ptr via `inttoptr`. Missing keys (i64 0) → null ptr, which compares cleanly against nil. Extended `tests-internal/iface_slice_map_autobox.volt` to cover the read path. |
| ~~IFACE.8~~ | DONE | **Missing feature**: auto-boxing at channel sends. `write(ch, myErr)` and `ch <- myErr` (where `ch` is `chan error`) used to fail clang with the same struct-vs-ptr mismatch. New `chanArgAstElem(arg)` helper recovers the channel's element AST type from the symbol table (parallel to the existing `chanArgElem` which returns the LLVM type). Both `emitBuiltinWrite` and `emitSend` now route the value through `maybeBoxForInterface` against the recovered element type. The runtime stores channel payloads via memcpy from a stack slot, so the slot's type matches the boxed ptr — no extra coercion needed. Positive test: `tests-internal/iface_chan_autobox.volt`. Buildable test count: 147. |
| ~~IFACE.9~~ | DONE | **Missing feature**: ranging over an interface-typed map. `for k, v := range m` on a `map[string]error` used to type `v` as `int` (i64) — so `v.Error()` failed with "type int has no method Error". `emitRangeOverMap` now detects interface-typed value (error / any / user-iface), allocates the `v` binding as `ptr` instead of `i64`, and stores via `inttoptr` from the i64 storage slot. The binding's AstType is set to the interface type so method dispatch finds the right vtable. Positive test: `tests-internal/iface_map_range.volt` (range over `map[string]error`, call `v.Error()` per iteration). Buildable test count: 148. |
| ~~IFACE.10~~ | DONE | **Missing feature**: ranging over an interface-typed slice. `for _, v := range s` on a `[]error` used to bind `v` with no AstType — codegen saw the LLVM ptr but didn't know the interface vtable shape, so `v.Error()` failed with "cannot call method on ptr". `emitRangeOverSlice` now recovers the slice's element AST type from the source ident's symbol (`sym.AstType.(*ast.SliceType).Elem`) and threads it into the binding's `AstType`. Method dispatch now resolves correctly via the interface's vtable. Positive test: `tests-internal/iface_slice_range.volt`. Buildable test count: 149. |
| ~~IFACE.11~~ | DONE | **Missing feature**: composite-literal auto-boxing for slices and maps of interfaces. `new(2) []error{e1, e2}` and `new map[string]error{"a": e}` both failed clang at the per-element store. Wired `maybeBoxForInterface` into `emitNewSlice`'s element-store loop and `emitNewMap`'s entry loop. The map literal path also needed a new `v.Type == "ptr"` branch in its i64-coercion that goes through `ptrtoint` (matching the change in IFACE.6 for `emitMapSet`). Positive test: `tests-internal/iface_collection_lit.volt`. Buildable test count: 150. |
| ~~IFACE.12~~ | DONE | **Missing feature**: closures returning concrete values from interface-typed signatures. `fun() error { ret myErr }` (closure literal returning error) failed clang because `emitClosureBody`'s child `funcCtx` didn't set `retAstTypes` — so the IFACE.4 chain at `emitRet` couldn't see the declared interface return and skipped the auto-box. Fix: set `child.retAstTypes = ex.Results` on the child funcCtx whenever the closure has a non-empty result list. Also initialized `declaredAt` + `declaredAtPos` on the child for consistency with the recent scope-tracking work. Positive test: `tests-internal/iface_closure_ret.volt`. Buildable test count: 151. |
| ~~DBG.1~~ | DONE | Stopped LLVM "invalid #dbg record DILocation" warnings on closure bodies. Closure bodies emit into the trampoline buffer (which has no DISubprogram of its own), so the dbg.declare calls inside them referenced the *parent's* subprogram metadata — which LLVM correctly rejected and printed a warning for. Added a `noDebugInfo` bool on `funcCtx` and set it `true` for the closure child; `emitVar` skips its dbg.declare attachment when set. The variable still has a valid alloca + store, just no IDE-debuggable name metadata for the closure body. Verified: `iface_closure_ret.volt` now runs clean with no warnings. |
| ~~MSG.1~~ | DONE | Friendlier error message for the chained-method-call restriction. `r.err.Error()` (where `r.err` is a field, not a bare local) used to error with the bare `method receiver must be a variable in v0.4` — terse and unactionable. New wording: `method-call receiver must be a bare variable name in v0.4 — assign the receiver to a local first (e.g. \`var r T = expr; r.Method()\`)`. Surfaces the workaround inline. The underlying restriction remains a known v0.4 limitation; widening it is a separate (larger) feature. |
| ~~FEAT.1~~ | DONE | **Widened restriction**: chained method calls on struct fields now compile (`r.err.Error()` works without a temp local). `emitMethodCall` was refactored into a dispatcher (Ident receiver → existing path; SelectorExpr receiver → new spill path; otherwise → still error) plus a shared `emitMethodCallWithRecv` body. The spill path calls a new `spillFieldAsLocal` helper that emits the field value into a fresh stack slot, recovers the field's declared AST type from the struct registry, and returns a synthetic symbol. The dispatch body also got a receiver-loading fix: pointer/borrow methods on a borrow-or-pointer symbol need an extra deref load (caught while debugging a sha256 regression from the refactor — the original `emitCallArg` path handled this nuance, the new path didn't). Positive test: `tests-internal/chained_method_call.volt`. Buildable test count: 152. |
| ~~FEAT.2~~ | DONE | **Widened restriction**: method calls on call results now compile (`makeBox(42).GetX()` works without a temp local). Added `spillCallResultAsLocal` mirroring FEAT.1's `spillFieldAsLocal`: emits the inner call's value into a fresh stack slot and recovers the AST return type from either the local `c.e.funcs` map (`IdentExpr` callee) or the cross-package `c.e.extPkgs` map (`SelectorExpr` callee). Only single-return calls are supported as method receivers — multi-return results couldn't be the receiver of a method-call anyway. Positive test: `tests-internal/method_on_call.volt`. Buildable test count: 153. |
| ~~FEAT.3~~ | DONE | **Widened restriction**: method calls on slice/map index results now compile (`s[0].GetX()`, `m["k"].Method()`). Added `spillIndexResultAsLocal` mirroring the FEAT.1/FEAT.2 spill pattern: emits the index access into a fresh slot and recovers the element/value AST type from the collection symbol's `SliceType.Elem` / `MapType.Value`. Four receiver shapes now compile cleanly: Ident, Field, Call, Index. Positive test: `tests-internal/method_on_index.volt`. Buildable test count: 154. |
| ~~FEAT.4~~ | DONE | **Two-part fix**: (a) composite struct literals now auto-box concrete struct values flowing into pointer-typed fields, matching the existing `var f *T = new T{...}` boxing in emitVar. Added new helper `c.maybeBoxForPointer(val, targetAst)` and called it from the struct field-init loop right after `maybeBoxForInterface`. (b) The chained-method-call spills (FEAT.1/FEAT.2/FEAT.3) needed to set the synthetic recvSym's `Elem` field via `c.e.elemType(astT)` — without it, pointer-receiver methods saw a synthesized symbol that looked like an "owned" struct value, and dispatch passed the alloca-of-pointer instead of loading the pointer first. Positive test: `tests-internal/method_ptr_field.volt` (nested struct with `*Inner` field, method dispatched via `o.inner.Get()`). Buildable test count: 155. |
| ~~FEAT.5~~ | DONE | **Widened restriction**: passing a struct literal to a `*T` parameter now auto-boxes. `take(new Box{x: 42})` where `take` takes `*Box` used to error with "can only borrow from a variable in v0.3"; now `emitCallArg`'s non-ident branch routes the struct value through `maybeBoxForPointer` (heap-allocate + pass ptr) before reporting the error. Matches the var-decl `*T` boxing. Positive test: `tests-internal/call_ptr_arg_autobox.volt`. Buildable test count: 156. |
| ~~FEAT.6~~ | DONE | **Two-part fix**: (a) `ret` sites auto-box a struct value into a `*T` return slot (`fun make() *T { ret new T{...} }`); wired `maybeBoxForPointer` into both the single-return and multi-return slot paths in `emitRet`, alongside the existing `maybeBoxForInterface` call. (b) Multi-return decl `a, c := pair()` (where pair returns `(*Box, *Box)`) didn't set `Elem` on the resulting symbols — so `a.x` field-access didn't auto-deref. Fix: set `Elem: c.e.elemType(astT)` in `emitMultiVar`'s symbol-creation alongside the AstType. Positive test: `tests-internal/ret_ptr_autobox.volt` (both single-return and multi-return paths exercised). Buildable test count: 157. |
| ~~FEAT.7~~ | DONE | **Widened feature**: field access on a ptr-to-struct value (from a call result, slice/map element, or struct field) now auto-derefs. `make().x` (where `make` returns `*Box`) and `s[0].x` (slice of `*Box`) used to error with "cannot read field x on value — it has type pointer, not a struct". New `c.pointeeStructName(expr)` helper recovers the bare struct type name from CallExpr / IndexExpr / SelectorExpr receivers; `emitFieldExpr` calls it when `xv.Type == "ptr"` and inserts a `load %T, ptr xv` before the field-extract. Also extended `emitNewSlice`'s element loop to call `maybeBoxForPointer`, so `new(2) []*Box{new Box{...}, ...}` heap-allocates each element. Positive test: `tests-internal/ptr_field_autoderef.volt` (covers `make().x` + slice-of-ptr field access). Buildable test count: 158. |
| ~~FEAT.8~~ | DONE | **Widened feature**: `map[K]*T` end-to-end. (a) `emitMapGet` extended the IFACE.7 auto-unbox to also cover `PointerType` value types — inttoptr the i64 storage back to ptr. (b) `emitMapSet` and `emitNewMap` (literal form) added `maybeBoxForPointer` calls so concrete struct values flowing into `*T` map slots heap-allocate and ptrtoint into the i64 storage. Combined with FEAT.7's pointee-auto-deref, `m["a"].x` on a `map[string]*Box` works end-to-end. Positive test: `tests-internal/map_ptr_value.volt`. Buildable test count: 159. |
| ~~FEAT.9~~ | DONE | **Widened feature**: range over `map[K]*T` now exposes `v` as the declared ptr, not as the raw i64 storage slot. `emitRangeOverMap`'s interface-detection branch (from IFACE.9) extended to also recognize `*ast.PointerType` value types — same `inttoptr` load + ptr-typed binding shape. Also set `Elem: c.e.elemType(astT)` on the v binding so subsequent field-access (`v.x`) auto-derefs via the FEAT.7 machinery. Positive test: `tests-internal/range_ptr_map.volt` (sums `v.x` across two pointer-valued entries). Buildable test count: 160 — a project milestone. |
| ~~FEAT.10~~ | DONE | **Widened feature**: field access on a bare-Ident ptr-to-struct variable now auto-derefs through the FEAT.7 machinery. Extended `pointeeStructName(expr)` to also recognize `IdentExpr` — looks up the symbol's AstType and unwraps `*T` to recover `T`. Closes the last gap from FEAT.7: previously `v.x` worked when `v` was a struct-typed local (existing auto-deref path), and when `v` was a CallExpr/IndexExpr/SelectorExpr returning `*T` (FEAT.7), but NOT when `v` was a bare ident of type `*T` (e.g. a range binding). Positive test: `tests-internal/range_ptr_slice.volt` (sums `v.x` across `[]*Box`). Buildable test count: 161. |
| ~~FEAT.11~~ | DONE | **Widened feature**: arbitrarily nested ptr-to-struct chains (`w.o.i.x` where each intermediate field is a *T) now resolve. `pointeeStructName(expr)`'s SelectorExpr arm used to bail when `e.X` wasn't an IdentExpr; now it recurses into `pointeeStructName(e.X)` to find the outer struct, then looks up the next field's declared type in that struct. The recursion naturally walks any depth — `a.b.c.d.e` works as long as each step has a known struct type. Positive test: `tests-internal/nested_ptr_chain.volt` (three-level `Wrap{*Outer{*Inner{x: 42}}}` chain). Buildable test count: 162. |
| ~~FEAT.12~~ | DONE | **Widened feature**: method dispatch through nested ptr-to-struct chains. `w.o.i.Get()` (method on the inner-most *T after a deeply-nested SelectorExpr) used to fail because `spillFieldAsLocal`'s AST-type recovery only handled bare-Ident `sel.X`. Switched to the same FEAT.11 recursion: when `sel.X` is itself a SelectorExpr, use `pointeeStructName(sel.X)` to find the outer struct, then look up `sel.Sel` in its field list. Field-access AND method-dispatch now both walk any-depth nested chains uniformly. Positive test: `tests-internal/method_nested_chain.volt`. Buildable test count: 163. |
| ~~FEAT.13~~ | DONE | **Widened feature**: nested field assignment (`o.i.x = 42`, `w.o.i.x = 35`). `emitFieldAssign`'s "must be `var.field`" restriction now has a nested-LHS branch: when `lhs.X` is a SelectorExpr, the container struct name is recovered via `pointeeStructName` (works at any depth), `lhs.X` is emitted to get the container ptr (which auto-derefs intermediate `*T` fields via FEAT.7/11), then GEP into the container for the final field-store. Same auto-boxing for the RHS (interface + pointer) as the bare-Ident path. Positive test: `tests-internal/nested_field_assign.volt` (one-level + two-level nesting both write and read back correctly). Buildable test count: 164. |
| ~~FEAT.14~~ | DONE | **Widened feature**: field assignment through slice/map index and call results (`s[0].x = 42`, `m["k"].x = 42`, `f().x = 42`). Extended the FEAT.13 nested-LHS branch to dispatch on IndexExpr and CallExpr in addition to SelectorExpr — same `pointeeStructName` → `emitExpr` → GEP-store pattern works uniformly. Positive test: `tests-internal/idx_field_assign.volt` (slice + map shapes). Buildable test count: 165. |
| ~~FEAT.15~~ | DONE | **Widened feature**: method dispatch on the result of a method call (`m.Make().Error()`). `spillCallResultAsLocal`'s AST return-type recovery covered bare-function and pkg-qualified-function shapes, but missed `recv.Method()` — when the SelectorExpr's X is a local var (not a package name), we need to look up the method in the var's type's method registry. Added a fallback: if pkg-fn lookup fails, find the receiver symbol's struct type, then search `e.methods[tn]` and `e.extMethods[tn]` for the method. Positive test: `tests-internal/method_chained_method.volt` (`m.Make().Error()` where Make returns an interface). Buildable test count: 166. |
| ~~FEAT.16~~ | DONE | **Widened feature**: method dispatch on method-call results where the inner method's receiver is itself a complex expression (`s[0].Doubled().Get()`). `spillCallResultAsLocal` previously required `fn.X` to be an IdentExpr; extended to also handle non-Ident receivers via `pointeeStructName(fn.X)` (works for IndexExpr, CallExpr, SelectorExpr). Factored out a `lookupMethodReturn(typeName, methodName)` helper that searches both local and cross-package method registries. Combined with FEAT.3/FEAT.7, fully arbitrary chained method calls now resolve. Positive test: `tests-internal/method_chain_complex_recv.volt`. Buildable test count: 167. |
| ~~BUG.2~~ | DONE | **Silent-bug fix**: methods on `*T` range bindings returned wrong values. `for _, b := range s` over `[]*Box`, then `b.GetX()` produced garbage because the range-binding symbol was created with `Type: "ptr"` but no `Elem` — emitMethodCall's pointer-receiver branch keyed off `recvSym.Elem != ""` to know whether to load the stored ptr (vs. pass alloca address as-is for owned values). Without Elem, it took the wrong branch and the method received a pointer-to-pointer instead of the pointer. Fix: set `Elem: c.e.elemType(elemAst)` in `emitRangeOverSlice`'s binding. The methods now receive the correct *T receiver. Positive test: `tests-internal/range_method_call.volt`. Buildable test count: 168. |
| ~~FEAT.17~~ | DONE | **Widened feature**: channel sends of `*T` now auto-box. `write(ch, new Box{...})` and `ch <- new Box{...}` (where `ch` is `chan *Box`) used to fail with the struct-vs-ptr mismatch. Both `emitBuiltinWrite` and `emitSend` now call `maybeBoxForPointer` alongside the existing `maybeBoxForInterface`. Positive test: `tests-internal/chan_ptr_autobox.volt` (send + receive + method-call round trip). Buildable test count: 169. |
| ~~UX.20~~ | DONE | Friendly error for struct equality. `p1 == p2` where both are struct values used to produce the cryptic clang `icmp requires integer operands` from the icmp instruction. New check in `emitBinary` after the harmonize-and-check block: when both operands have the same type AND it's a user struct type (LLVM `%T`, not `%string`/`%slice`/`%error_box`/`%fn_value`) AND the op is `==` or `!=`, surface `struct equality (==) is not supported in v0.7 — compare field-by-field instead` at the source position. Negative test: `tests-internal/struct_eq_reject.volt` (expected-negative count bumps to 39 in CLAUDE.md). |

---

## Ownership / borrow checker

### Closed
- (a) Move tracking for owned structs, slices, maps — enforced.
- (b) Writes through `&T` are rejected by the checker.
- (c) Writes through `*T` for primitives compile correctly.
- **(2)** Field/index move tracking — `consume(p.a)` / `consume(s[i])` now conservatively mark the root container as moved.
- **(3)** `run f(args)` rejects borrow / pointer args (`&T` / `*T`).
- **(4)** Recursive Drop — struct fields with their own `Drop()` now fire in reverse declaration order after the parent's `Drop`.
- **(5)** `clone()` for slices and maps — slices byte-clone their backing storage; maps copy every entry via `volt_map_clone` runtime helper. Shallow at the element level (pointer-valued elements still alias).

### Open

| # | Severity | Gap |
|---|----------|---|
| ~~1~~ | ~~HIGH~~ | ~~**Memory is never actually freed.**~~ Partially closed: allocator now supports free (A1). Wiring it into Drop for owned slice/map/string vars is tracked as A3 (needs move-tracking → codegen integration). |
| 6 | LOW      | **Gap (d): cross-statement aliasing-XOR-mutation.** Enforced within a single call (`f(*x, &x)` is rejected). Outstanding read/write access across statements not yet tracked. Currently unreachable because no syntax exists for `var r &T = …`. Lands together with item 8. |
| ~~7~~ | ~~LOW~~ | **`error` dispatch landed.** Concrete types with `Error() string` auto-implement `error`; conversion boxes into `%error_box = {data, error_fn}`; `e.Error()` does indirect call through the stored fn pointer. User-defined interfaces and `any` typed-storage are the remaining gaps (separate item below). |
| 8 | LOW      | **No `&x` / `*x` expression to hold a borrow in a variable.** Borrows are call-site-only today via bare-name inference. Adding `&x`/`*x` as expressions is a design decision; if accepted, gap 6 must land with it. |
| ~~9~~ | ~~LOW~~ | **Slice deep-clone landed** (Pass 206). `cloneSlice` consults `typeIsPodForClone(elem)` after the byte-copy; when the element type carries inner heap data (string / slice / map / non-POD struct), an emitted IR loop walks the new buffer and overwrites each slot with `cloneValue(elem)`. Map deep-clone deferred (needs runtime callback or codegen post-pass over iterator). |
| ~~10~~ | ~~LOW~~ | **Branch-local unused vars** landed — `checkBlock` now snapshots entry names and reports any newly-declared var that wasn't read by block end. |
| ~~11~~ | ~~LOW~~ | **User-defined interfaces** landed — per (concrete T, interface I) vtable `@<T>_<I>_vtable = constant [N x ptr]`. Method-set matching is name-based at v0.7 (signature-strict matching is a polish item once parser captures sigs strictly). Test `iface_user.volt`. |
| ~~12~~ | ~~LOW~~ | **`any` storage** landed — heap-copy + ptr; no method dispatch (none to dispatch), no type recovery (no type tag yet — future addition). Test `any_struct.volt`. |
| 13 | LOW     | **Type-assertion / unboxing for `any`.** Today values stuffed into `any` can't be read back as their original type — no type tag. Add a runtime type-id alongside the data ptr, and an `assert[T](v any) T` builtin or similar. |
| ~~14~~ | ~~LOW~~ | **`for k, v := range map` — DONE (duplicate of A.4 in the top queue).** Map range works end-to-end; runtime iterator (`volt_map_iter_new` / `volt_map_iter_next` / `volt_map_iter_done`) plus codegen lowering. Bookkeeping: this row was a stale leftover, the work landed earlier under A.4. |

---

## Allocator / runtime

| # | Severity | Item |
|---|----------|---|
| ~~A1~~ | ~~HIGH~~ | **Real allocator** landed — mmap-backed, size-classed freelists, `volt_free` + huge-block munmap. No more 16 MB BSS cap. |
| ~~A2~~ | ~~MEDIUM~~ | **Map auto-resize** landed — initial 16 buckets, doubles when load > 0.75. Test `map_grow.volt`. |
| ~~A3~~ | DONE | **Auto-free on Drop for owned slice/map/string values** + reassignment cleanup + stdlib heap-string whitelist (Pass 747+748+750). Runtime: `volt_slice_free`, `volt_map_free`, `volt_str_free` in `internal/runtime/asm/runtime.c` — all null-safe; `volt_str_free` additionally consults a runtime heap-range tracker (`heap_range_record` on every mmap, `heap_range_contains` on every free) so freeing a literal pointer becomes a silent no-op rather than corrupting the freelist. Map-free walks bucket linked-list freeing entries (keys NOT freed — borrowed from caller storage). Codegen: `dropKindSlice` / `dropKindMap` register when RHS is `*ast.NewExpr`; `dropKindString` registers when RHS is a known heap producer (binary `+`, `chr`, or `stdlibHeapStringProducers` whitelist). Move-pre-scan (`scanMovedNames`) skips drops for vars used as call args / returns / channel sends. Sub-scope drops fire at each loop iter end via existing depth-based machinery. **Reassignment cleanup**: `s = newValue` frees the old buffer before storing the new one; the drop is re-registered at the ORIGINAL declaration's depth (not the assign-site's depth) so loop bodies don't accidentally double-free. **Stdlib whitelist**: 38 functions across `fmt`/`strings`/`strconv`/`json` whose returns are auto-freed at the caller; the runtime range check protects against the literal-return edge cases inside those functions. Tests: `a3_auto_free.volt`, `a3_subscope_drops.volt`, `a3_string_concat_free.volt`, `a3_reassign.volt`. |
| A4 | LOW     | Open-addressing hash maps. Switch from chaining if chaining ever becomes a hotspot. |
| A5 | LOW     | Small-string optimization (SSO) for `string` — inline ~14 bytes in the header. Won't pay off until allocator + string-mutation story is mature. |

---

## Language surface — design decisions pending

| # | Item |
|---|---|
| ~~D1~~ | **Slice `cap` field — landed.** `%slice = {ptr, i64 len, i64 cap}` (24 bytes). `cap()` builtin returns the third field; `append(s, v)` routes to `volt_slice_grow` runtime helper that doubles cap when full (min 8). Sub-slicing not yet exposed but the header now supports it. |
| ~~D2~~ | DONE | Bare `new map[K]V` (no parens, no braces) now accepted, matching `new chan T` and `new []T`. Resolves the inconsistency by accepting the bare form rather than rejecting it — simpler for users, no surface ergonomic loss. Lowers to the same empty-map ctor as `new map[K]V{}`. Test: `tests-internal/bare_new_map.volt`. |
| D3 | **Held-borrow syntax** (see ownership #8). `var r &T = ...` and its tracking. |

---

## Language features — planned, not yet implemented

| # | Item |
|---|---|
| ~~F1~~ | ~~**Float literal lexing.**~~ Landed: lexer already produced Float tokens; parser now accepts them (new `*ast.FloatLit`), codegen emits literal verbatim, `convertInt` extended with `fpext` / `fptrunc`, `emitBinary` routes to `fadd` / `fcmp` etc. for float operands and auto-promotes int literals via `sitofp` in mixed expressions. |
| ~~F2~~ | ~~**Unused-var error like Go's.**~~ Landed: `state.used` flag, set by `checkExprUse` on IdentExpr (and on the receiver of selector/index assigns). `reportUnused` at end of `checkFunc` flags unused non-param locals; `_`-prefix opt-out. `mergeSyms` propagates `used` across branches. Coverage extended to `RunStmt`, `SendStmt`, `SelectStmt`, `MultiVar`/`MultiAssign`, `DeferStmt`. |
| ~~F3~~ | **`log.Fatal` / `log.Fatalf` removed (2026-05-25).** Originally landed as Println-then-`syscall.Exit(1)` intrinsics; rolled back because hiding a process exit behind a logging call obscures control flow. Use `log.Println` followed by an explicit `syscall.Exit(code)` instead. |
| F4 | **Auto-reorder struct fields to minimize padding.** Would require a `repr(volt)` vs `repr(c)` distinction. Marginal until users hit padding pain. |
| F5 | **Bit-packed `bool` arrays / `bitset` library type.** Only if dense-boolean perf matters. |
| ~~F6~~ | ~~**Select-case name collision.**~~ Landed: per-case alloca naming uses `%name.caseN.addr`, so two cases binding `v := read(...)` no longer collide. |
| ~~F7~~ | ~~**`i++` / `i--` statements.**~~ Landed: desugared in `parseSimpleStmt` to `i = i + 1` / `i = i - 1`. Works as a for-post or standalone statement. |
| ~~F8~~ | ~~**`for i, v := range EXPR`.**~~ Landed for slice + string (slice: i = index / v = element; string: i = byte-index / v = byte). 1-token vs 2-token parser lookahead distinguishes range-form from C-style init. Map range deferred (TODO 14). |

---

## Concurrency primitives

Six sync primitives ship: `chan T`, `mutex T`, `rwmutex T`, `atomic T`, `waitgroup`, `once`. The mutex/rwmutex family uses guard-with-Drop; atomic uses per-width lock-free ops (`int/int64`, `int32`, `int16`, `int8`/`byte`, `bool`, `ptr`); waitgroup/once are pure coordination primitives (no payload). Channels: **cap=0 is the default** (unbuffered, true rendezvous — sender blocks until paired receiver consumes); `new(N)` for buffered. Channel handles can narrow to a single direction via `chan read T` / `chan write T` — many readers and many writers may share the same channel. Channel values can also carry a **multiplicity contract** (`chan11 T` / `chan1N T` / `chanN1 T` / `chanNN T` — notation `chan<readers><writers>`, 1=exactly one, N=one or more) verified at compile time at the declaring scope. C1–C7 closed in May 2026; cap=0 default + directional types + multiplicity contracts closed 2026-05-25. Leftover items below.

| # | Item |
|---|---|
| ~~C8~~ | DONE (phase 1+2+3) | **Held-borrow syntax** lands in Pass 751-753. Parser accepts `&x` (take address) and `*p` (deref) as value expressions; `var b &T = &x` binds a held borrow that survives across statements. Phase 3 (Pass 752+753): rejects multiple simultaneous held borrows of the same var via `borrowedBy` tracking; clears the flag when the borrow var goes out of scope at block end. Combined with the single-var conflict rule this gives users the "borrow → use → drop → borrow again" idiom. Codegen: `&x` returns the alloca pointer of x (Value{Type: "ptr"}); `*p` loads through the pointer. Tests `c8_held_borrow.volt`, `c8_block_scoped_borrow.volt`. Negative test `c8_double_borrow_reject.volt` (expected-negative #42). Remaining: cross-statement / cross-function borrow alias tracking, mutable-vs-shared distinction (volt currently treats all borrows as read+write). |
| ~~C9~~ | ~~**`once` with closure / function-value argument.**~~ Landed: first-class function values (bare fn ptrs, anonymous literals, closures with by-move/by-copy captures) + `o.Do(fn)` runtime hook. See item below for what landed and what's still gated. |
| ~~C10~~ | DONE | **Conditional variable user-facing primitive** lands in Pass 750. New `condvar` keyword type (KwCondvar) — parsed as `new()` (no init), method-dispatched `Wait(m mutex T) / Signal() / Broadcast()`. Wait passes the mutex handle to runtime `volt_cond_wait(c, m)` which calls cond_wait on the embedded mutex_t at offset 0 of the mutex wrapper — atomic unlock/sleep/relock per pthread semantics. The guard pattern composes correctly: inside the guarded block, calling c.Wait(m) releases the lock during sleep and reacquires on wake; the guard's auto-drop at scope exit unlocks once. Caller must release the mutex before waiting on `done` channels from broadcaster threads (volt's "wrap in `if 1 < 2 { ... }`" pattern since bare blocks aren't supported). Test `condvar_basic.volt` — single-signal producer/consumer + 3-waiter Broadcast demo. |
| ~~C11~~ | DONE | `run f(...)` with >6 args works via the existing struct-pack thunk path (the same machinery A.2 added for oversized single args). `emitRun` triggers `emitRunViaThunk` when `len(sig.Params) > spawnMaxArgs`; the thunk heap-allocates a pack struct, stores each arg, and tail-calls f. Positive test: `tests-internal/run_many_args.volt` (8 args, 7 ints + chan write int). No code change — verification only. |
| ~~C12~~ | **Closure escape across thread boundaries** landed — `%fn_value` consumes 2 spawn slots (SysV passes the 16-byte struct in 2 GPRs anyway); the receiver function reassembles via its formal-arg convention. Test `run_closure.volt`. |
| ~~C13~~ | DONE (phase 1+2) | **Capture-by-borrow + escape proofs** lands in Pass 752-753. Phase 1 (752): closures capture held borrows; env stores the ptr. Phase 2 (753): closures that capture borrows are REJECTED from escaping their borrowed scope — `ret closure_with_borrow` and `run closure_with_borrow` both emit clear errors. New `CapturesBorrow` flag on FuncLit AST + symbol; set in emitFuncLit when any capture is borrow-typed; checked in emitRet and emitRun (both ident-form and inline-funclit-form). Tests `c13_capture_borrow.volt` (positive — 6 scenarios), `c13_closure_escape_reject.volt` (negative #43 — returning closure with borrow capture). Remaining: closure stored in struct field / slice element (rarer escape paths) — future work. |
| ~~C14~~ | **Function values returned from a function** landed — the env is heap-allocated, survives function exit. Test `fn_return.volt` builds three different closures from one factory, each with its own env. |
| ~~C15~~ | **Unbuffered channels (cap=0 default) + directional `chan read T` / `chan write T`** landed. Runtime tracks `has_handoff` + `receivers_parked` for true rendezvous semantics with `select` support; direction is type-only (handle still lowers to ptr) and is enforced at `read`/`write`/`close` builtin sites. Tests `chan_unbuffered.volt`, `chan_dir.volt`. |
| ~~C16~~ | DONE | Strict channel-direction check at call sites. New `checkChanDirCompat` helper in codegen, wired into `emitCallArg` (so it covers regular calls + `run f(ch)` + method calls). Rejects narrowed → bidi (can't widen) and read↔write (incompatible) at the call site rather than deferring until the callee tries the disallowed op. Bidi → narrowed still works (established narrowing case). Negative test: `tests-internal/chan_dir_param_reject.volt` (expected-negative count bumps to 40 in CLAUDE.md). |
| ~~C17~~ | **Channel multiplicity contracts (`chan11 T` / `chan1N T` / `chanN1 T` / `chanNN T`)** landed — notation `chan<readers><writers>` with 1 = exactly one, N = one or more. Compile-time checks at the declaring scope. Same runtime as `chan T`; the checker walks the function body counting reader/writer endpoints (direct `read`/`write`/`close`, `run f(ch)` classified by callee param direction, `ch <- v` sends). A `run f(ch)` inside a `for` is treated as "many" of that endpoint kind. Tests `chan_11.volt`, `chan_1n.volt`, `chan_n1.volt`, `chan_nn.volt`. |
| ~~C18~~ | DONE | Parameter-side multiplicity contract enforcement landed (Pass 208). `checkChanContracts` now seeds the contracts map from FuncDecl.Params (any `chanXY T` param) using at-most semantics — the function body's endpoint usage gets validated against the contract cap, but count == 0 is OK because the caller may be supplying additional endpoints. Var-decl sites still use exactly-N semantics. Tests: `chan_contract_param.volt` (positive), `chan_contract_param_reject.volt` (in-loop spawn through chanN1 param). Cross-function flow tracking through transitive `run` spawns remains future work — typically contracts are declared and the immediate caller flow is what matters. |

---

## Design constraints to preserve through all additions

- Borrows (`&T`, `*T`) cannot cross thread boundaries. `run f(args)` should reject borrow-typed args. The `mutex`/`atomic`/`rwmutex` wrappers themselves are the only way two threads touch the same memory.
- No exceptions, no GC. Guards for `mutex T` and `rwmutex T` release via the existing block-scoped `Drop` machinery.
- No libc. Atomic intrinsics lower to `__atomic_*` builtins in runtime.c, not C library calls. Mutex uses the existing in-runtime futex helpers.

---

## Suggested ordering when work resumes

Smallest → biggest within each batch:

**Quick wins (1 day each):**
- F1 float literal lexing
- F2 unused-var error
- Ownership #3 `run f(&x)` rejection

**Medium (1 week each):**
- Ownership #2 field/index move tracking
- Ownership #5 `clone()` for slices/maps
- Ownership #4 recursive Drop
- A2 map auto-resize

**Large (multi-week):**
- A1 real allocator (unblocks long-running programs)
- C6 atomic bool/i8/i16 (smaller widths)
- Ownership #7 `error`/`any` dispatch (blocked on full interfaces work)
- Ownership #8 + #6 held-borrow syntax + cross-statement tracking
